# CLAUDE.md — أداة مزامنة المستودع مع متجر زد (ASJ / zid-sync)

أداة صفحة واحدة (`index.html`) تعمل بالكامل داخل المتصفح (static site على GitHub Pages، مستودع `mrbadraljohani-gif/zid-sync`). لا يوجد خادم؛ كل المعالجة JavaScript في الصفحة، والمكتبة الوحيدة هي SheetJS (xlsx) لقراءة/كتابة إكسل.

## الخطوط (ثلاثة توكنات)
- `--sans: 'IBM Plex Sans Arabic', 'Segoe UI', Tahoma, system-ui` — **كل نص الواجهة** (`body`). اختير لأن عربيه ولاتينيه من عائلة واحدة، وشاشات الأداة تخلط عربية مع SKU وباركود وبريد LTR في السطر نفسه.
- `--serif: 'Amiri', Georgia` — الشعار والعناوين فقط (`h1`, `.section-head h2`, `.page-head h2`, `.login-title`, `.side-brand-txt b`).
- `--mono: ui-monospace, Consolas, monospace` — الأكواد والـSKU (`.mono`, `.bt-sku`).
- يُحمَّل الخطان من Google Fonts (`Amiri:400;700` ＋ `IBM Plex Sans Arabic:400;500;600;700`). **أُزيل Playfair Display** لأنه بلا محارف عربية إطلاقاً.
- `body { font-synthesis-weight: none }` **ضروري**: Amiri له وزنان فقط وPlex Arabic يقف عند 700، فبدونه تُصنّع المتصفحات وزن 800 المطلوب في بعض القواعد ويخرج العربي عريضاً مشوّهاً. عند تغيير الخطوط أبقِ هذا السطر أو أزِل كل `font-weight: 800`.
- عند انقطاع الشبكة يسقط النص على `Segoe UI/Tahoma` بلا كسر.

## الألوان
**التطبيق داكن فقط** (أُزيل الوضع الفاتح كلياً — لا `data-theme` ولا زرّ تبديل ولا `zid-theme`). كل الألوان الدلالية توكنات في `:root`: `--text` · `--muted` · `--muted-2` · `--gold` · `--gold-2` · `--green` · `--green-2` · `--danger` · `--info` · `--amber` ＋ الأسطح `--bg`/`--panel`/`--panel-2`/`--border`/`--border-strong`.
- **لا تكتب لوناً صلباً لنص أو حدّ**؛ استعمل التوكن. الاستثناءات المقصودة: نصوص فوق تدرّجات ثابتة (`#0a1622` على الذهبي · `#08160d` على الأخضر).
- **الشارات مقادة بالتوكن عبر `color-mix`**: خلفية `color-mix(TOKEN 12-16%)` · حدّ `26%` · نصّ بالتوكن الكامل — فتغيّر التوكن يسري على الشارة كلها. لا `rgba` مثبّتة لعائلة لونية.
- **الأصفر عند R≈G؛ أي تعبئة صفراء منخفضة الإضاءة تنزلق إلى الزيتوني حتماً. الهوية الصفراء تُحمل بالحدّ اللامع والنصّ، لا بالتعبئة. قِيس بالأرقام (R−G والصبغة) لا بالنظر.** لذا `--amber-tint` (اشتقاق `oklch(from var(--amber) 0.90 c h)` أفتح بنفس الصبغة 51°) للحدود/الأشرطة الأمبرية البارزة، والتعبئة alpha منخفض (10-12%) فتصير حيادية لا زيتونية. العائلات الأخرى (verdigris/danger/info/brass) لا تحتاجه: عند alpha منخفض إمّا تثبت صبغتها (info) أو تنهار إلى **حياد** بـchroma ≤14 (danger/brass) لا إلى لون خاطئ — مبدأ واحد، وamber هو ما يستوفي شرط تطبيقه (مقيس).
- **عند تدقيق الألوان: الإيموجي تحمل ألواناً يرسمها النظام ولا تظهر في مسح CSS. أي محرف إيموجي في الواجهة هو لون شارد افتراضاً.** (مثال واقعي: «+» البنفسجية في التنقّل وزرّ إنشاء الحساب لم تكن في CSS إطلاقاً — كانت لون محرف `➕`؛ حُلّت باستبداله بأيقونة SVG.)
- أيقونات الواجهة **SVG خطّية** (`.ico`: viewBox 24 · `stroke-width: 1.7` · `currentColor` · `aria-hidden="true"`) فتتبع لون الثيم وحالة العنصر. الإيموجي داخل النصوص والشارات والجداول تبقى كما هي.
- التحقّق آلي: أداة الفحص تقيس التباين على **البكسل المرسوم فعلاً** عبر 17 مشهداً × ثيمين، والحدّ 4.5:1 (3:1 للنص الكبير).

## لوحة معلومات المخزون (إعادة تصميم — قيد التنفيذ بالدفعات)
`#page-inventory` أُعيد تصميمها من أقسام مرقّمة إلى **لوحة معلومات** بأربعة صفوف (تخطيط مستوحى من مرجع، بتوكناتنا حصراً — بلا شريط جانبي، التنقّل يبقى في الشريط العلوي):
- **صفّ ١ — KPI «أين أنا الآن»** (`.dash-kpis`، أربع بطاقات `.kpi` بشريط لون علوي دلالي): **منتجات زد** (`zidDbMeta.product_count`／`stData`، info) · **المخزن الموحّد** (`lastMerge.total`، green) · **بلا ربط** (`lastUnmatchedRaw.length`، amber) · **الروابط** (`manualMap`، gold). `renderInvKPIs()` يملؤها من مصادر حيّة؛ **تبدأ «—» في الترميز** ولا تُظهر رقماً قبل توفّر مصدرها (بلا ربط: بعد `lastRunSummary` فقط؛ الموحّد: بعد `lastMerge`) — حارس b2 «لا أرقام قبل المطابقة» يغطّيها. `#kpiEmpty` رسالة «ارفع ملف المستودع» عند غياب كل المصادر.
- **صفّ ٢** (`.dash-3`): ملخّص سريع (`#mergeResult` تركيبة الدمج ＋ **سطر الملخّص الأخضر المنقول `#invSummaryLine`** ＋ قائمة الانتظار `#invWaiting`) · **توزيع المنتجات** (`#invPie` — رسم دائري) · سجل النشاط «آخر ٥» (`#activityMini` عبر `loadActivityInto(...,5)`).
- **الرسم الدائري (`renderInvPie`/`PIE_SLICES`):** SVG donut مضمّن (stroke-dasharray، بلا مكتبة) من **`lastClassify`** — التقاط حالة بحت في نهاية `run()` (كل منتج في دلو واحد عند فرعه النهائي: matched/unmatched/collided/absent/parent) فالمجموع == الإجمالي **تماماً بلا بقايا** («أخرى» الوهمية استُبدلت بـ«غائب» الحقيقية، و«غير محدودة» أُسقطت). الألوان `color-mix` من التوكن بمعناها: مربوطة green · بلا ربط amber · متجاهلة muted · متصادمة danger · غائب info · أب/خيار gold (الفوشيا تسري). **أسطورة نصّية** بالأعداد والنسب في `<bdi>` بجانبه (تتكدّس على الجوال). **الوصولية:** الـSVG `aria-hidden` والأسطورة هي المصدر المقروء (أوضح من aria-label مختزل). **ساكن بلا حركة** (prefers-reduced-motion مُستوفى بنيوياً). **الحالة الفارغة**: بلا `lastClassify` ⇒ رسالة لا دائرة صفرية. حارس `tests/perf/pie-sum.mjs`: مجموع الأسطورة == مركز الإجمالي (يرسب على فرق مصطنع) ＋ الحالة الفارغة رسالة.
- **صفّ ٣** (`.dash-2`): الاتصال والجلسة (`#dbBadge`/gh-menu/`#fingerprint`) · «يحتاج انتباهك» (`#invAttention` + `.inv-details`: `#collisionWarn`/`#newAlert`/`#variantInfo`/`#parentPublishMsg`).
- **صفّ ٤ — آخر الأحداث (`#invActTable`/`renderActivityTable`):** جدول نحيف من `activity_log`: **الوقت** (نسبي في `<bdi>`) · **النوع** (`ACT_META` أيقونة SVG ＋ تسمية) · **SKU زد** (`zid_sku` mono LTR) · **الطريقة** (`details.source`: يدوي/مستورد/تلقائي/—) · **الحالة** (مشتقّة من `event_type` عبر `actStatus` — شارة `.act-st` ملوّنة color-mix: معتمد green · انتظار amber · متجاهَل/أُزيل muted · مكتمل info · إلغاء نشر danger؛ **لا عمود جديد ولا مصدر ثانٍ**). فلتر `#actFilter` بالنوع. علاج `.upd-table` (auto+nowrap+تمرير أفقي داخل `.twrap`). **الحالة الفارغة/عدم الاتصال: رسالة لا جدول برؤوس فارغة.** `ACT_META` وُسِّع بـ`ignored_added`/`ignored_removed`. الحارس `table-mobile` يغطّي المشهد (عبر `actTableRow` الحقيقي)؛ خلاياه قصيرة فلا تنكسر بشدّة (أسنان الحارس على «تم تحديثه»).
- كل المعرّفات القديمة محفوظة (صفر تغيير منطق)؛ حُذفت أقسام الترقيم (١٢٣) وبطاقات `.acc-stat`/`.kpi-row` (دُمجت «نظرة عامة» في KPI + الملخّص السريع). القرارات المعتمدة: KPI حالة لا تركيبة · `lastClassify` للدائري (التقاط بحت، دفعة ٢) · الجدول نحيف بلا SQL جديد. اللوحة بلا أرقام أقسام عربية (اصطلاح الترقيم يبقى في الرئيسية/التحديث).

## الثيم الزجاجي — §٢ الزجاج و§٤ الأزرار
- **§٢ الزجاج (وصفة موحّدة، مصدر واحد في نهاية الـCSS):** الحاويات **الثابتة** فقط — `.card`/`.stat`/`.notif-panel`/`.bt-results-float`/`.login-box`/`.signup-card`/`.off-box`/`.toast` ＋ `.tn-bar` (فوق topnav المعتم) = `var(--glass)` ＋ `backdrop-filter: blur(24px) saturate(150%)` ＋ حدّ `var(--hair)` ＋ نصف قطر 20 ＋ ظلّ عميق ＋ لمعة `::before` على الأسطح الكبيرة (لا `.stat` لتعارض شريط اللون، ولا القوائم القابلة للتمرير). **ممنوع على صفوف القائمة** (`.batch-card`/`.mcard` — 300×backdrop-filter تدهور) **والأشرطة اللاصقة** (`.uni-bar`/`.un-chips`/`.exp-bar`/`th` تبقى مصمتة). **الهيرو و`.dl-card` سطوح دلالية** (نحاسي/أخضر) بلا زجاج. حارس `tests/glass-rows.mjs` يرسب إن حمل `.batch-card`/`.mcard`/`.mc-btn`/`.mini-btn` أي `backdrop-filter` (self-check `--broken`).
- **§٤ الأزرار:** **الأساسي** `.btn-run` = نحاس مصمت `var(--gold)` · نصّ `#1A1206` (تباين ≈8.5:1) · **pill (99px)** · ظلّ نحاسي مخفّف (18%→24% hover، لا توهّج). **الثانوي (ghost)** = `rgba(255,255,255,.03)` ＋ حدّ `--hair` ＋ نصّ `--text`؛ أزرار الأيقونات المدمجة (`.hamburger`/`.notif-btn`) ghost pill ＋ `blur(10px)` (خارج الصفوف فالضبابية آمنة). **الخطر** = بنية ghost بـ`--danger` (`.mini-btn.danger`). **استثناءات نصف القطر مقيسة (لا pill):** صفوف القائمة `.mc-btn` وأزرار الجدول `.mini-btn` ← **10px** (ثلاثتها متراصّة تُقرأ «شارات» وتزاحم `.mc-tag` الشارات الحقيقية)؛ بطاقات التنزيل `.dl-card` ← **14px** (بطاقة محتوى لا CTA).
- **هدف اللمس 44px بلا تضخيم بصري:** القاعدة العامة تبقى `min-height:32px`؛ على `@media (pointer: coarse)` فقط: `.btn-run.sm`→`min-height:44px`، والصغيرة (`.mc-btn`/`.mini-btn`) تُبقي ارتفاعها البصري وتوسّع الإصابة بـ`::after` شفّاف ارتفاعه 44px، و`.mc-actions` gap يُرفَع 8→12px فلا تتداخل مناطق اللمس (مقيس: فسحة +6.5px في التراصّ العمودي، والجوال صفّ أفقي فلا جار عمودي).
- **`:focus-visible`:** حلقة `outline: 2px var(--gold)` ＋ `offset: 2px` — تتبع الشكل (pill/معتدل) لأن outline يحترم border-radius، وoffset يضعها على الخلفية الداكنة فتُرى حتى على النحاس. على كل عائلات الأزرار.
- **توكن `--hair`** = `rgba(255,255,255,.12)` (حافّة زجاجية تلتقط الضوء) — بين `--border` و`--border-strong`.
- **توكن `--cost` (سعر التكلفة، أحمر مستقلّ):** `#FF6B7A` — أحمر ورديّ **منفصل عن `--danger`** (‏`#FF9B8A`، الخطأ) دوراً وقيمةً؛ 🚫 لا يُخلطان كي لا يُفقَد الخطأُ معناه (لونان أحمران متقاربان بدورين منفصلين). يُستعمل في **المواضع الثلاثة للتكلفة** (نصّاً فقط، بلا خلفية حمراء — الخلفية تبقى السطح الخفيف): شارة `.q-chip.cost` (منسدلة البحث) · عمود `.s4-tbl td.cost-c` («الراكدة») · رقم `.kpi[data-k="sinv"] > b` («قيمة المخزون (تكلفة)»). التباين مقيس ≥6.4:1 على أسطح التكلفة (الأحمر أضعف الألوان على الداكن — عُدّل حتى مرّ contrast-guard، بلا استثناء). حارس `tests/perf/cost-color.mjs` (G-COST-COLOR): `--cost≠--danger` · المواضع الثلاثة تستعمله · الشارة نصّها `--cost` وخلفيتها سطح خفيف · الخطأ يبقى بلونه؛ `--broken` يجعل الشارة `var(--danger)` ⇒ يرسب.
- **⚠ لا يوجد توكن `--frost`** — لون «frost» هو `--muted` (#8C9CBE). أي `var(--frost)` يسقط إلى `currentColor` (نصّ فاتح = أبيض صارخ). حدّ بطاقات الجاهزية `.ready-card`/`.ready-card.off` يستعمل `color-mix(var(--muted) 35%/50%)` (حدّ إفلات ملف متقطّع frost مرئي، لا أبيض).
- **§ب-٤ الإيموجي → أيقونات SVG:** كل إيموجي تصويري في **عنصر واجهة دائم** (عنوان · شارة · زرّ · تبويب · stat · placeholder · aria-label · سجل النشاط · نائب الصورة) استُبدل بأيقونة `.ico` (SVG خطّي، `currentColor`). **المبقاة نصّاً:** `↩ ⚠ ⚙ ✓ ✕` (الأخيران غير تصويريّين). **خريطة الألوان الدلالية** (بمعناها لا تنويعاً بصرياً، عبر أصناف `.ico-ok/warn/bad/info` التي تضبط `currentColor`): **green** (✅ تم تحديثه · 🔆 نشر) · **amber** (🔶 بدون ربط · 🆕 يحتاج قرار · 📭 غائب · ⏳ انتظار · 📉 صُفّر) · **danger** (🚫 تجاهل/إلغاء نشر · 🔴 عدم الاتصال ⇒ `.sdot`) · **info** (🧩 أب/خيار · 🔀 توزيع · 🔗 ربط) · **محايد يرث `currentColor`** (🔍 📚 ⬇ 🔄 🏬 🏪 🌿 📄 + ℹ️ 🔐 📦 ⬆ ⏸). مُولّد `ic(name, mod)` للقوالب؛ و`ICO`/`ACT_META` تخزّن مفاتيح تُحلّ في `actDescribe` (بعد تعريف `ICO`، تفادياً لـTDZ). **يُترك كما هو:** الإيموجي في التعليقات ونصوص الرسائل العابرة (`showToast`/`setMsg`/`textContent`/`innerHTML` اللحظية) و`title=` (لا يقبل SVG). حارس `tests/perf/emoji-guard.mjs` (ثابت): أي `\p{Extended_Pictographic}` (عدا `↩⚠⚙`) في سياق قالب/HTML — بعد تجريد التعليقات و`title=` والرسائل العابرة — ⇒ رسوب. مثبت رسوبه على النسخة قبل الاستبدال (54 محرفاً).
- **§٦ الشريط المتحرّك (`#marquee`/`renderMarquee`):** في أعلى `#result` (الرئيسية) بعد شريط الحالة. **المجتمع = «يحتاج قرارك + غائب» فقط** (`green+yellow+red` من `analyzeBatch` = بعد `okItem` الذي يستبعد المتجاهَل/⏳/المُدار/الآباء) — **ليس عدّاد بطاقة «بدون ربط»** الذي يجمع كل هؤلاء (قد يكون 464 بينما الفعّال 3). **قرار مقصود: الشريط أداة تنبيه لا عرض إحصائي — لا يمرّر المحسوم (المُدار/المتجاهَل) أمام العين.** كل عنصر ثلاثة حقول: الاسم · الباركود (mono، عرض فقط) · **شارة الحالة = `pubBadge(z)` نفسها**. الترتيب: يحتاج قرار ← غائب، ثم الأعلى سعراً؛ سقف **30**. **التسمية بالعدد الحيّ** «تحتاج قرارك (N)» حيث N = `needCount+absentCount` (المجتمع الفعلي، لا المرسوم المسقوف) — والنصّ ＋ `aria-label` يُحدَّثان في `renderMarquee`؛ العدد في التسمية يجعل «3 عناصر» مفهوماً لا مريباً.
  - **القلّة (≤6) ساكنة — لا إخفاء**: `if (reduce || items.length<=6)` ⇒ **نسخة واحدة بلا تكرار ولا انزلاق** (`items.slice(0,6)` = كلها حين ≤6) + صنف `.mq-static` (`animation:none; transform:none`). السبب: التكرار عند القلّة يُنزلق العنصرين بلا انقطاع فيوهم بالعطل؛ ولا نُخفي صنفاً فعّالاً (شريط تنبيه لا يُخفي تنبيهه). الحدّان: **6 بالضبط ⇒ ساكن · 7 ⇒ انزلاق**. **0 ⇒ الشريط مخفيّ كليّاً** (لا تنبيه بلا محتوى — يختلف عن إخفاء الفعّال).
  - **>6 تكرار ديناميكي** (يقيس عرض نسخة ويبني «نصفاً» يملأ الحاوية ثم نصفين متطابقين ⇒ `translateX(-50%)` سلس). **⚠ فخّ القياس والحاوية مخفيّة:** `run` يستدعي `renderMarquee` و`#result` لا يزال `display:none` ⇒ `width=0` ⇒ لو حُسب التكرار لانفجر (مسار ~165كpx يتجاوز حدّ طبقة GPU 16384px فيُرسَم فارغاً). **الحلّ:** `wCont≤1` ⇒ لا نقيس، نرسم نسختين مؤقتاً ونسجّل `ResizeObserver` (`mqPendingRO`) يعيد الرسم بقياس صحيح؛ ＋ سقف `half≤12`.
  - **⚠ تموضع RTL (حاسم):** `.mq-viewport { direction: ltr }` (علامة `/* mq-fill */`) ＋ `.mq-item { direction: rtl }`. **السبب:** اتجاه الـviewport يحكم موضع المسار داخله؛ `rtl` يلصق المسار بالحافّة اليمنى **بلا فائض يمينيّ**، فالانزلاق يساراً (`translateX` سالب) يُخرج المحتوى ولا تدخل النسخة الثانية من اليمين ⇒ **الـviewport يفرغ معظم الدورة** (إشغال عبر الدورة `[5,0,0,0,0]` — «٤-٥ ثم فجوة طويلة»). `ltr` يبدأ المسار يساراً والفائض يميناً فتدخل النسخة الثانية وتملأ دائماً (`[5,5,5,5,5]`). والعنصر يعيد `rtl` فيبقى ترتيب الاسم·الباركود·الشارة والنصّ العربي صحيحاً. **الـDOM والحركة يبدوان سليمين (60 عنصراً، `translateX` يتحرّك) بينما لا شيء يعبر** — فالمقياس يجب أن يكون **العبور الفعلي عبر الـviewport** لا البنية.
  - البنية: `.marquee`(حدّ علوي/سفلي + `.mq-label` بالعدد الحيّ) > `.mq-viewport`(overflow+mask، **ltr**) > `.mq-track`(46s linear **reverse** ⇒ الانزلاق يسار←يمين، transform فقط؛ `reverse` يعكس ترتيب مواضع الدورة نفسها فالالتفاف سلس والإشغال كما هو). حارس `tests/perf/marquee-visible.mjs`: يخطو `translateX` عبر دورة كاملة (0→-setW) ويجمع الـSKUs العابرة الـviewport فعلاً — يرسب إن (أ) أول رسم بلا عناصر، (ب) المتمايزة < `min(30, needCount+absentCount)`، (ج) أي خطوة أظهرت 0 (فجوة). أسنانه `--broken`: يزيل `/* mq-fill */` فيعود عطل التموضع ⇒ يرسب. حارس `tests/perf/marquee-loop.mjs` (§٦): **يقيس التنوّع لا الطول** — الفريدة (dedup بأسماء `.mq-nm`) **== `min(30, needCount+absentCount)`** عبر 0·1·3·6·7·30·50؛ ＋ ≤6 ⇒ نسخ==1 (صفر تكرار) ＋ ساكن · >6 ⇒ نسخ≥2 والمسار ضمن [2×الحاوية , 40000px] · 0 ⇒ مخفيّ · التسمية تحمل `(N)`. **أسنانه `--broken`:** يقتطع مخرج `buildMarqueeData` (القائمة 2، العدّاد 30) فيُملأ العرض بالتكرار لكن الفريدة 2≠30 ⇒ يرسب (الحارس القديم كان يعدّ الطول فيمرّ — هذا العطل الذي مرّ ثلاث مرّات). يتوقّف: تحويم · نقرة (`mqTapToggle`→`.mq-paused`) · خارج الشاشة (`IntersectionObserver`→`.mq-off`) · تبويب مخفيّ (`.mq-hidden`). النقر (`mqGoto`) ينقل للقائمة الموحّدة ويُبرز (`flushUnified`＋`.mq-flash`). **الوصولية:** `aria-live="off"`، النسخة الثانية `aria-hidden`. يتوقّف: تحويم (سطح المكتب، CSS) · نقرة على الشريط لا على عنصر (`mqTapToggle`→`.mq-paused`، الجوال) · خارج الشاشة (`IntersectionObserver`→`.mq-off`) · تبويب مخفيّ (`visibilitychange`→`.mq-hidden`). النقر على عنصر (`mqGoto`) ينقل إلى القائمة الموحّدة ويُبرز البطاقة (`data-sku`＋`flushUnified`＋`.mq-flash`). **الوصولية:** `aria-live="off"`، النسخة الثانية `aria-hidden`. **مقيس:** تمرير الـ300 مع الشريط عاملاً = median/p95 مطابق لبلا شريط (~8.3/9.1ms، طبقة تركيب منفصلة) · تجاوز أفقي 0px (60 عنصراً داخل `overflow:hidden`).
- **جدول «تم تحديثه» (تمرير أفقي لا انكسار حرفي):** `table { table-layout: fixed; width:100% }` يضغط أعمدة الأسماء (‏`overflow-wrap:anywhere`) إلى ~عرض حرف فيلتفّ الاسم العربي **عمودياً حرفاً حرفاً** (صفّ 478-551px، خلية 9-36px). **يحدث عند كل عرض لا يتّسع للأعمدة لا الجوال وحده** — بوابة `@media(max-width:720px)` وحدها لا تكفي (العطل يظهر عند 768/900 أيضاً). الحلّ (أ) **بلا بوابة عرض، محصور في `.upd-table`** (الوسم على `<table>` وخلية اسم زد `.upd-nm`؛ لا يمسّ ignored/explorer): `table-layout: auto; width: auto; min-width: 100%` ＋ بقيّة الخلايا `white-space: nowrap`، وعمودا الاسم `white-space: normal` (يلتفّان بالكلمات لا الحروف): `.wh-name` `min-width:150px; max-width:230px`. ⇒ سطح المكتب يتّسع بلا تمرير (جدول ~1104px)، والأضيق يفيض و`.twrap` يمرّر أفقياً (تجاوز الصفحة 0px). حارس `tests/perf/table-mobile.mjs` (يغطّي «تم تحديثه» عبر **360/390/768/900**): صفّ >260px أو خلية <40px ⇒ رسوب — **مثبت رسوبه على النسخة المحدودة بـ720px: 551px/9px عند 768**.
  - **⚠ انهيار اسم زد (عطل متصفح):** عمود زد `.upd-nm` يحوي `.prodcell` (flex: صورة 56px + كتلة نصّ). الصورة تلتهم ~66px والنصّ **ينكمش لأضيق كلمة (56px)** فيصير **كلمة/سطر**. و`table-layout:auto` **يتجاهل `max-width`** ولا يوسّع بـ`min-width` على الخلية إن كان محتواها أضيق. **الحلّ الحاسم — عرض صريح على كتلة النصّ نفسها:** `.upd-table td.upd-nm .prodcell > .pc-txt { flex: 0 1 auto; min-width: 175px }` فيلتفّ الاسم بالكلمات داخل ≥175px. **الحارس وحده (صفّ ≤260) لم يمسكه** — وُسِّع بقياس **عرض اسم زد ≥120px**. **الدرس: قِس ما ينهار (عرض الاسم) لا وكيله (ارتفاع الصفّ).**
  - **دفعة ٢ — دمج عمودَي SKU والاسم في «تم تحديثه» وحده:** كان SKU عموداً مستقلاً ثابتاً (`col.c-sku` 120px) والاسم عموداً مضغوطاً فالصورة تُشوَّه. الآن خلية `.upd-nm` واحدة = صورة 56×56 (`.thumb`، `object-fit:cover`) + `.pc-txt` (كتلة عمودية: `.pc-name` يلتفّ فوق · `.pc-sku` أصغر `var(--muted)` `dir=ltr` تحته · ثمّ الشارات). حُذف عمود SKU (‏٨→٧ أعمدة في `colgroup`/`thead`، الرأس «المنتج (زد)») فتحرّر عرضه للاسم. **حُلّ تشويه الصورة**: `.prodcell .thumb img { max-width: none }` يلغي تعارض `max-width:100%` (السطر 345) الذي كان يقصّ عرض الصورة في خلية ضيّقة. **مقصور على `.upd-table`** — القائمة الوحيدة الأخرى ذات صورة+اسم هي بطاقات `batchCardHTML` (‏`.mc-row`/`.mc-txt`، لا `.prodcell`) والمستكشف بلا صورة، فلم تُمَسّ. **`col.c-sku` يبقى** (يستعمله جدولا الزيادات والمستكشف). حارس `table-mobile` عُدِّل قياسه من `.prodcell > span` (صار الصورة بعد إعادة التسمية) إلى `.prodcell > .pc-txt` — **تقادم بنيويّ لا تخفيف** (مُثبَت: إزالة `min-width:175px` تُرسبه: اسم 22px · صفّ 333px).
- **الشريط العلوي شفّاف (لا أطراف سوداء):** `.topnav { background: var(--bg) }` المعتم كان يملأ الفراغ حول `.tn-bar` المتمركز (146px كل جانب @1440) **بأسود صمّاء تحجب الخلفية المحيطة**. الحلّ: `.topnav { background: transparent; backdrop-filter: blur(10px) }` — تجلس البطاقة الزجاجية على المحيط (الشبكة/الكرات تظهر حولها)، والضبابية الخفيفة تمنع تسرّب المحتوى المتمرّر تحت الشريط اللاصق. مقيس: تمرير الـ300 مع الضبابية = median/p95 8.3/8.6ms (بلا تدهور).
- **§٧ الخلفية المحيطة (`.bg-ambient`):** عنصر `fixed` **شقيق** (قبل `.app-shell`) بـ`z-index:-1` · `pointer-events:none` · ساكن. ثلاث كرات `radial-gradient` من التوكنات عبر `color-mix` (أزرق عميق `--info` · نحاسي `--gold` · زنجاري `--green` — اختبار الفوشيا يسري) ＋ شبكة 88px بـ`--hair` (`repeating-linear-gradient`) مع `mask` إشعاعي علوي في `::before`. **قرارات مقيسة:** (١) شقيق لا أب + z-index:-1 ⇒ لا سياق تكديس يحبس الشريط اللاصق/لوحة الهامبرغر (فخّ z-120-في-z-100)؛ (٢) `radial-gradient` ناعمة **لا `filter: blur(90px)`** — البلور فلتر يجبر إعادة تركيب مستمرة على تمرير الـ300 (مقيس: scroll_ms بقي ≤0.4ms، بلا تغيير)؛ (٣) حارس التباين بعدها 24×0 (الكرات ألفا منخفض فوق `--bg`).
- **احتواء `.mcard`:** البطاقة عنصر flex داخل `.mcards`، فتحتاج `min-width: 0` وإلا تنتفخ بأعرض محتوى (صفّ الأزرار nowrap) وتتجاوز الحاوية يساراً (RTL). وصفّ الإجراءات على الجوال `flex-wrap: wrap` فيلتفّ بدل التجاوز. حارس `tests/perf/mcard-containment.mjs` (viewport حقيقي 360/390 coarse) يرسب إن تجاوز أي عنصر داخل `.mcard` حدودها؛ **التجاوز احتوائي لا أفقي على مستوى الصفحة فلا يمسكه `overflow-guard`** — لذا حارس منفصل. self-check `--broken` (حقن 900px) ＋ مثبت رسوبه على نسخة ما قبل الإصلاح (+33.7px@360).

## خطة المرحلتين (جوهرية لفهم «الغائب»)
- **المرحلة 1 (الحالية):** ملف المستودع يمثّل **المستودع الرئيسي فقط**. أصناف كثيرة موجودة في زد وغائبة عن الملف لأنها متوفرة في **فروع أخرى** — لا يصح تصفير كمياتها.
- **المرحلة 2 (لاحقاً):** الملف سيشمل **إجمالي المستودع + الفروع** بنفس الأكواد. عندها يصبح تصفير الغائب آمناً (خيار «صفّر كميتها»).
- لذلك الافتراضي للأصناف الغائبة = «اتركها كما هي»، والتصفير خيار صريح مع تحذير.

## المدخلات
- **ملف المستودع**: يحوي أعمدة «رقم الصنف»، «الكمية»، أسعار («شامل»/«قبل الضريبة»)، «اسم».
- **ملف تصدير زد**: تصدير المنتجات الكامل (~76 عموداً)، أهمها: `sku`, `name_ar`, `name_en`, `quantity`, `price`, `barcode`, وعمود صور (يُكشف تلقائياً).

## هيكل الواجهة: SPA بشريط تنقّل علوي موحّد (RTL) + صفحات
الواجهة **صفحة واحدة (`index.html`) بأقسام تُظهر/تُخفى** (`.page` / `.page.active`, `display:none/block`) لا ملفات منفصلة.

**الشريط العلوي الموحّد** (`.topnav` > `.tn-bar`, `position:sticky; top:0`): بطاقة مستديرة واحدة تضم كل شيء بترتيب RTL — **الشعار يميناً · الروابط وسطاً · أدوات المستخدم يساراً**:
- **الشعار** `.side-brand` = عنصر `<a href="#home" aria-label="الرئيسية">` يستدعي `goPage('home')` — النصّ «ASJ / Inventory Sync» هو الرابط نفسه، **بلا أيقونة دائرية** (أُزيلت لمنع تكرار الشعار).
- **الروابط** `.side-nav` (`id="sidebar"`, `role` ضمني nav): **الرئيسية · المخزون · تحديث قاعدة البيانات · الخيارات · مستخدم جديد** (أيقونات SVG لا إيموجي). `goPage(id)` يبدّل `.active` ＋ `aria-current="page"`. الحالة النشطة = خطّ سفلي (`inset 0 -3px 0`).
- **الأدوات** `.tn-actions`: `#notifBtn` ＋ `.notif-panel` · شارة الاتصال `#dbBadgeMini` (مرآة لـ`setDbBadge`) · `.side-userrow` (`#sideUser`) بلا إطار فتقرأ كجزء من الشريط.
- **عمود واحد يحكم العرض**: توكنات `--shell-max` (1200px) و`--shell-pad` (26px) و`--topbar-h` (64px) في `:root`. `.tn-bar` عرضه `calc(--shell-max - --shell-pad*2)` و`.page-wrap` عرضه `--shell-max` بحشو `--shell-pad` ⇒ **حافة الشريط فوق حافة البطاقات تماماً** وارتفاعه ثابت في كل الصفحات. على ≤860px يصير `--shell-pad: 15px` فيتبعه الاثنان.
- **الاستجابة (ثلاث مراحل)**: ≥1241px أيقونة ＋ تسمية · 861–1240px أيقونات فقط (التسمية مخفية **بصرياً** بـ`clip` لا `display:none` حتى يبقى الاسم المتاح للزرّ) · ≤860px الروابط تنسدل تحت الشريط بزر `.hamburger` (`aria-expanded`) ＋ `#sidebarScrim` عبر `toggleSidebar`/`closeSidebar` · ≤560px يُخفى اسم العلامة وشارة الاتصال.
- **الصفحات ومحتواها** (نُقلت العناصر بنفس المعرّفات — **صفر تغيير منطق**، الحالة مشتركة عبر DOM دائم):
  - **🏠 الرئيسية (`#page-home`)** = سير العمل اليومي فقط: هيرو ← **شريط الجاهزية `#readyBar`** (3 بطاقات من القاعدة: 🏬 المستودع · 🌿 الفرع · 🏪 زد — عدد/عمر/رابط «تحديث ←»، `renderReadiness`) ← زر «طابق وأنشئ» (`runFromDB`: يجلب المخزن + زد من القاعدة ويوحّد ويطابق) ← `#result` كاملاً ← قسم ③ «استعلام عن منتج» (بحث المخزن من الجدولين مع تفصيل الكمية بالمصدر). **لا بطاقات رفع في الرئيسية.**
  - **المخزون (`#page-inventory`)** = لوحة معلومات للمراجعة. رأس الصفحة «مراجعة الجرد» ＋ «آخر تحديث منذ X» من `invLastUpdateMs()` = أحدث طابع بين `zidSyncedAt` و`invMeta.wh_synced_at` و`invMeta.branch_synced_at` (فارغ إن لم يوجد أي طابع — لا قيمة ثابتة). قسم «يحتاج انتباهك» = شبكة عمودين (عمود واحد ≤860px) من أربع بطاقات يولّدها **`statusCard({status, icon, count, countLabel, title, description, body, actionLabel, onAction})`** بصنف `.stc` — بادئة `.stc-` تفادياً لتصادم `.sc-` مع «تعارض التخفيض». مصادر الأرقام حيّة حصراً: danger أكواد متصادمة (`lastCollisions.length`) · amber بدون ربط (`lastUnmatchedRaw.length`) · info أب/خيار (`lastVariantGroups.length`) · success ملخّص الملفّين الكامل (`lastRunSummary` الموسّع: كميات/أسعار/إعادة نشر/صُفّر/إخفاء تلقائي/موزّع/بلا تغيير ＋ ملاحظة تقادم `.stc-stale` حيّة). الإجراءات: `invGoto('collisionWarn')`/`invGoto('variantInfo')` تمريرٌ داخل المخزون (اللوحتان صارتا فيه) و`invStartBatch()` ينقل للرئيسية. كل رقم ديناميكي داخل `<bdi>`. `renderInventoryPage()` يُستدعى من `goPage('inventory')`، وهو **محروس بـ`invVisible()`** فلا يرسم شيئاً والصفحة مخفيّة.
    الأقسام (بترقيم موحّد ١ ٢ ٣): **١ يحتاج انتباهك** (البطاقات الأربع ＋ لوحات تفاصيلها المنقولة من الرئيسية: `#collisionWarn` · `#newAlert` · `#variantInfo` داخل `.action-center.inv-details`) · **٢ المخزن الموحّد** (`#mergeResult` — منقول من صفحة التحديث) · **٣ نظرة عامة** (KPI: منتجات زد `zidDbMeta.product_count` · الروابط `manualMap` · قائمة الانتظار `waitingSet.size`) · ثم صفّان: **الاتصال والجلسة** (`#dbBadge` · `#mapCountBadge` · `#roleBadge` · زرّ الحفظ في الريبو `.gh-menu` ＋ الإصدار والبصمة في تذييل خفيف `.inv-meta-foot`) و**سجل النشاط** (`#activityFull` بنفس مصدره ومنطق فلترته وشريط تمريره وتلاشيه).
  - **🔄 تحديث قاعدة البيانات (`#page-db`)** = بطاقتا رفع «🏬 المستودع» و«🌿 الفرع» (المرحلة 4، كل جدول مستقل + سطر ميتاه) ＋ داشبورد الدمج (`#mergeResult`) ＋ بطاقة «متجر زد» (المرحلة 3).
  - **⚙ الخيارات (`#page-options`)** = قسم ② كاملاً (`.settings` ＋ `offsetsCard`) — يقرأ/يكتب نفس `opts` عالمياً فيسري على المطابقة في الرئيسية.
  - **👤 الحساب والنظام (`#page-account`)** = حالة الاتصال (`dbBadge` + `mapCountBadge`) ＋ عدّادات (`accProducts`/`accLinks`/`accWaiting` عبر `renderAccount`) ＋ الحفظ في الريبو (`gh-menu`) ＋ خروج ＋ النسخة/البصمة (`fingerprint`/`fpWarn`).
  - **➕ مستخدم جديد (`#page-newuser`)** = نموذج `doSignup` (بريد ＋ كلمة مرور ＋ تأكيد): `sb.auth.signUp` ثم **استعادة جلسة المدير** بـ`setSession` إن أنشأ Supabase جلسة للمستخدم الجديد (بلا service key إطلاقاً) — فجلسة المدير لا تنكسر.
- `launchMatchIfReady` يشغّل `run()` (المطابقة) متى توفّر المخزن ＋ زد **بلا انتقال تلقائي إلى الرئيسية** (دفعة ٢ — قرار المستخدم): المطابقة تُعاد **في الخلفية** فالنتيجة تبقى حيّة وجاهزة، والمستخدم يبقى في مكانه ليرفع بقيّة الملفات (أربع بطاقات). النجاح يظهر في البطاقة نفسها (✓ ＋ العدد/الوقت) ＋ توست «✓ حُدّثت المطابقة — جاهزة في الرئيسية» **حين لا يكون في الرئيسية**. المدخل كامل دائماً (‏`loadInventoryFromDB` يسبق `autoAdopt`) فلا تعود علّة «المدخل الناقص». التنقّلات المقصودة الأخرى (روابط الشريط · `invStartBatch` · `mqGoto`) لها `goPage('home')` **خاصّ بها** (مستقلّ عن `launchMatchIfReady`). حارس `tests/perf/inv-nav-stay.mjs` (G-NAV): بعد الرفع تبقى الصفحة ويُستدعى `run`؛ `--broken` يعيد `goPage('home')` ⇒ يرسب. النوافذ المنبثقة (`loginOverlay`/`offOverlay`/`toast`) خارج `.app-shell` (fixed). **ملاحظة RTL**: للتثبيت البصري يمين استخدم `right`/`inset-inline-start` لا `inset-inline-end` (الأخير = يسار في RTL).
- **تبديل الوضع:** أُزيل — التطبيق داكن فقط (حُذف `#themeBtn`/`applyTheme`/`toggleTheme`/`THEME_KEY`/`ICO.sun`/`ICO.moon`/سكربت إقلاع `zid-theme`/كل قواعد `:root[data-theme="light"]`). صنف `.theme-btn` باقٍ لأن `#ghBtn` يعيد استعماله.
- **سطر المستخدم ＋ الخروج:** `.side-userrow` (`#sideUser`) داخل `.tn-actions`: 👤 + `authSession.user.email` (مع قصّ `ellipsis`) + زر `.su-logout` (🚪, `sideLogout` = تأكيد خفيف ثم `doLogout`). `renderSideUser` يُظهره عند وجود جلسة (من `onAuthStateChange` والإقلاع) ويخفيه بلا جلسة. البريد يُخفى تحت 1240px والأيقونتان تبقيان. زر الخروج في صفحة الحساب (`#logoutBtn`) باقٍ كما هو (مكانان لا ضرر).
- **ترقيم الأقسام موحّد** عبر الصفحات: أرقام عربية-هندية بشارة واحدة (`.section-head .n` و`.stage-n` في قاعدة CSS واحدة: 28×28 · نصف قطر 9 · `--panel-2` · نص `--gold`). الرئيسية ١ الجاهزية · ٢ طابق وأنشئ · ٣ استعلام عن منتج. صفحة التحديث ١ المستودع والفرع · ٢ متجر زد.
- **حاوية الصفحات**: `<main class="page-wrap">` عنصر واحد يلفّ الصفحات الخمس ⇒ العرض والحشو موحّدان بحكم البنية. نموذج «مستخدم جديد» موسّط أفقياً (`.signup-card`, `max-width:480px; margin-inline:auto`).

## الخطوة ① «جهّز الملفات» — تدرّج مفتوح دائم (لا طي) بمرحلتين
الخطوة ① تعرض مرحلتين ظاهرتين دائماً:
- **المرحلة ١: المستودع والفروع** — بطاقتا «ملف المستودع» (`#whCard`, `onMergeWh`) و«ملف الفرع (اختياري)» (`#brCard`, `onMergeBranch`) → خط تدفق «⬇ دمج تلقائي ⬇» → شريط نتيجة `#mergeResult`.
- **المرحلة ٢: متجر زد** — بطاقة مزدوجة (`#stCard`, `onStore`) مستقلة: **وضع القاعدة** (لوحة `#zidDbPanel` الخضراء: «منتجات زد من القاعدة: N · آخر تحديث منذ X» ＋ زر «✓ استخدمها للمطابقة» `useZidFromDB`) ＋ **الرفع/التحديث** (label الرفع، عنوانه يتحوّل إلى «🔄 تحديث من ملف تصدير جديد» متى امتلأت القاعدة) ＋ شريط ملخّص `#zidSyncMsg`. تفاصيل المرحلة 3 في قسمها أدناه.
- **الدمج تلقائي بلا زر** (`autoAdopt`): فور رفع ملف المستودع (± فرع) يُبنى «المخزن الموحّد» (`mergeInventory`) ويُعتمد فوراً كـ`whRows` (مصدر المطابقة)، ويظهر **شريط إحصائيات (داشبورد `#mergeResult`)** من خلايا `.mr-cell` (رقم بارز فوق تسمية): `المخزن الموحّد ✓` (مميّزة خضراء) · `مشترك` · `مستودع فقط` (ذهبي) · `فرع فقط` (رمادي مزرق) [· `بلا سعر`]؛ بلا فرع → خليتان (`المخزن` ＋ `المصدر: ملف المستودع`). رابط التنزيل صار أيقونياً صغيراً `.mr-dl` في طرف الشريط. الفرع **اختياري فعلاً**: المستودع وحده = المخزن مباشرةً، وإضافة الفرع لاحقاً تعيد الدمج تلقائياً (`onMergeBranch → autoAdopt`).
- **الخطوة ② بمظهر إعدادات حديث** (`.settings`): كل خيار `.setrow` (تسمية يميناً · segmented control `.seg` من `.chip`s متلاصقة بحواف موحّدة والنشط `.on` بتعبئة ذهبية · خط فاصل بين الصفوف). حقلا التصفير مدمجان في سطر `.lowfields` واحد. التحذير الأحمر الطويل صار أيقونة `.set-info` (ⓘ) بـtooltip ＋ سطر مختصر `#absentWarn` يظهر عند اختيار «صفّر كميتها». **تجميل فقط** — `setOpt`/`data-group`/`data-v` بلا تغيير.
- بقي زر «⬇ تنزيل الملف الموحّد (اختياري)» (`downloadMerge`, يعمل مع المستودع وحده أو مع فرع). أُزيلت أزرار «دمج واعتماد» و`doMerge`/`onWarehouse`/`checkMergeReady` (صار الدمج تلقائياً). المنطق (`mergeInventory`/الإحصائيات/اتحاد الكميات) كما هو — التغيير في التدفق والعرض فقط.
- الفهم الصحيح: الملفات (المستودع + الفروع) **من نفس النظام** بأكواد وأسماء وأسعار موحّدة، وكل ملف يحوي أصنافه بكمياته. المطلوب **اتحاد كامل بجمع الكميات**.
- **قارئ مرن** `parseInvRows(aoa)`: يكتشف الأعمدة بالاسم إن وُجد صف رأس (رقم الصنف/كود · اسم · كمية · شامل · قبل)، أو بالموضع إن كان بلا رأس؛ ويقبل الملف **كاملاً (٥ أعمدة)** أو **مختصراً (٣: كود/اسم/كمية)** — المختصر يأخذ سعره من الملف الآخر.
- **الدالة النقية** `mergeInventory(whList, branchLists)` (تقبل **مصفوفة فروع** — قابلة للتوسّع، والواجهة الآن خانتان): تُوحّد بالكود بعد trim بلا تطبيع، **تجمع الكميات عبر كل الملفات**، والاسم/السعر من المستودع أولاً ثم يُملأ من الفرع إن غاب. تُرجع `{unified, noPrice, both, whOnly, brOnly, total}`. `downloadMerge()` طبقة فوقها.
- كل صنف **له سعر في أي ملف** يدخل الورقة الرئيسية (لا يُستبعد إلا من لا سعر له إطلاقاً). عمود **«مصدر الكمية»**: «مستودع ＋ فرع (مجموعة)» / «مستودع فقط» / «فرع فقط».
- الإخراج xlsx بورقتين: **«تقرير عن المخزون»** (الموحّد المُسعّر، بنفس أسماء أعمدة ملف المستودع + «مصدر الكمية» — جاهز للرفع كملف مستودع) · **«بلا سعر — للتسعير»** (أصناف بلا سعر في أي ملف). الملخص: «موحّد · مشترك · مستودع فقط · فرع فقط · بلا سعر»، وتحذير إن لم يوجد أي كود مشترك (ملفات غير متطابقة النظام).
- **المرحلة الثانية — التغذية المباشرة**: زر **«🔗 دمج واعتماد كملف المستودع»** (`doMerge`) يعتمد ناتج الدمج مباشرةً كـ`whRows` (يبني كائنات بنفس أسماء أعمدة ملف المستودع فتُعالَج في `run()` كأنّ الملف رُفع يدوياً — بلا تنزيل/إعادة رفع). خانة «ملف المستودع» تعرض «✓ من الدمج: مستودع ＋ فرع — X صنف». رفع ملف يدوي لاحقاً في خانة المستودع يلغي الاعتماد (`onWarehouse` يستبدل `whRows`). زر **«⬇ تنزيل الملف الموحّد (اختياري)»** (`downloadMerge`) باقٍ للفحص اليدوي فقط. كلاهما طبقة فوق `mergeInventory` بلا تعديل منطقها.

## شريط الدمج — مصدر واحد وحارس عرض
`lastMerge` هو مصدر الحقيقة الوحيد (يضبطه `autoAdopt`)؛ و`renderMergeBar()` يقرأ منه ويكتب في `#mergeResult` **الذي انتقل إلى صفحة المخزون**. `autoAdopt` — نقطة الحدث الوحيدة لرفع أي ملف (`onMergeWh`/`onMergeBranch`/`runFromDB`) — يحدّث الحالة دائماً ثم يستدعي الرسم **فقط إن كانت الصفحة معروضة** (`invVisible()`)؛ وإلا فالرسم يقع عند فتحها. **لا polling ولا حالة مكرّرة.** تحذير «لا كود مشترك» (`#mergeWarn`) يبقى في صفحة التحديث بجوار بطاقات الرفع.

## التقاط أرقام آخر مطابقة
`lastRunSummary` كائن يُملأ في نهاية `run()` بـ`{qtyCount, priceCount, distItemCount, distGroupCount, noChangeDropped, republishCount, zeroLowCount, autoUnpubCount}` — **التقاط حالة بحت** للأرقام نفسها المكتوبة في `#qtyN`/`#priceN` وسطر التأكيد (كانت محلية داخل `run` فتُفقد). تقرأه بطاقة «ملخّص الملفّين» في المخزون، وقد **وُسِّع ليحمل الملخّص الكامل** (إعادة نشر · صُفّر لانخفاض القيمة · إجمالي الإخفاء التلقائي) فتصير البطاقة النسخة الغنيّة في المخزون بينما يبقى السطر الأخضر ملاصقاً للأزرار في الرئيسية (قرار: أُثريت البطاقة ولم يُنقَل سطر مكرّر). التقادم في البطاقة يُقرأ حيّاً بـ`zidIsStale()` لا مُلتقَطاً. قبل أول مطابقة يكون `null` فتُعرض رسالة لا أرقام. **لا يعيد حساب شيء ولا يغيّر منطقاً.**

## رقم واحد لمفهوم واحد — `batchOkItem` مصدر «يحتاج ربط» لكل القرّاء
> **رقم واحد لمفهوم واحد. تعريفان لنفس المفهوم يولّدان تشخيصاً خاطئاً — رقمان واثقان مختلفان بلا ما يميّز الصحيح.**
> **وإصلاح عدّاد في شاشة لا يسري على نظيره في أخرى تلقائياً — المصدر الواحد يُطبَّق على كل القرّاء دفعةً؛ فتّش عن كل قارئ قبل الإصلاح لا شاشتين اثنتين.**

**الحادثة:** «يحتاج ربط» ظهر بأربعة أرقام في المخزون وحده: KPI 936 · «يحتاج انتباهك» 936 · لافتة 466 · وعنوان القائمة 736. الجذر: `#sUn` في الرئيسية أُصلح لـ`batchOkItem` (736) بينما بقي **ثلاثة قرّاء** على تعريف أقدم/مختلف. `batchOkItem(u)` (مصدر واحد، قبل `analyzeBatch`) = `!boundSet && !waitingSet && !orphanParent && !famIndex.isParent` — يستبعد «غير متوفر» المقرَّرة **والآباء غير القابلين للربط**. القرّاء الموحَّدون كلهم عليه الآن: الرئيسية `#sUn` · `updateUnmatchedCard` · `analyzeBatch.needCount` · عنوان القائمة/الشريحة (`needN`) · **KPI المخزون** (`renderInvKPIs`) · **«يحتاج انتباهك»** (`renderInventoryAttention`) · **لافتة `#newAlert`** (`updateNewCount`). الطول الخام `lastUnmatchedRaw.length` **لم يعد عدّاداً لهذا المفهوم في أي شاشة** (يبقى مصفوفةً للرسم فقط).

- **الآباء اليتامى (`orphanParent`) كانوا محسوبين مرّتين:** `cls.parent` في الدونات (توزيع) ＋ يُدفعون إلى `unmatched` (index.html:4915) فيَعُدّهم الطولُ الخام. `batchOkItem` يستبعدهم ⇒ توحيد القرّاء عليه **يُخرجهم من عدّاد «يحتاج ربط»** ويُبقيهم مرّة واحدة في الدونات (`cls.parent`) وفي القائمة كصفّ «أب بلا خيارات» — بلا حذف من `unmatched` (يبقون معروضين للمعالجة في زد).
- **الوحدات تُسمّى صراحةً** (لا تُوحَّد الأرقام): بطاقات «يحتاج انتباهك» وحدتها **مجموعة** («مجموعات متصادمة» = `lastCollisions.length` · «مجموعات أب/خيار» = `lastVariantGroups.length`)، والدونات وحدتها **منتج** («منتجات متصادمة» = `cls.collided` · «منتجات ضمن مجموعات الأب» = `cls.parent`). اسمان مختلفان لوحدتين مختلفتين، لا رقم موحَّد.
- **`relTime` تعيد «—» لا فراغاً** عند وقت غائب/غير قابل للتحليل (index.html:2506) — الفراغ يُقرأ عطل عرض؛ «—» يجعل أي صفّ ناقص الوقت مرئيّاً كنقص بيانات لا كخلل.
- **حارس `tests/perf/count-consistency.mjs` (G-COUNT، بأسنان):** على تجهيزة (2 يحتاج + 1 غير متوفر + 1 أب يتيم) يجب أن يكون KPI == «يحتاج انتباهك» == عنوان القائمة == الشريحة == اللافتة == **2** (batchOkItem)؛ `--broken` يعيد KPI إلى الطول الخام ⇒ 4 ≠ 2 ⇒ يرسب. أُثبِت رسوبه على الكود قبل التوحيد (KPI/انتباهك=4، اللافتة=3).

## المواضع النهائية للمنقولات
| المنقول | من | إلى |
|---|---|---|
| شريط الدمج `#mergeResult` | تحديث قاعدة البيانات | المخزون ← ٢ المخزن الموحّد |
| KPI (منتجات زد · الروابط · الانتظار) | الحساب والنظام | المخزون ← ٣ نظرة عامة |
| الاتصال والجلسة ＋ الحفظ في الريبو ＋ البصمة | الحساب والنظام | المخزون ← صفّ سفلي |
| سجل النشاط | الحساب والنظام | المخزون ← صفّ سفلي |
| تصادم الأكواد `#collisionWarn` | الرئيسية | المخزون ← ١ يحتاج انتباهك |
| بدون ربط `#newAlert` | الرئيسية | المخزون ← ١ يحتاج انتباهك (يستدعي `invStartBatch()`) |
| أب/خيار `#variantInfo` | الرئيسية | المخزون ← ١ يحتاج انتباهك |
| نشر مُنقَل إلى الأب `#parentPublishMsg` | الرئيسية | المخزون ← ١ يحتاج انتباهك (بعد الشبكة، في `.action-center.inv-details` بجوار `#collisionWarn`/`#variantInfo`؛ `renderParentPublish` يجده بالمعرّف — صفر تغيير منطق) |

**حُذف من الرئيسية:** بانر «⚠ بيانات زد قديمة» المستقلّ (`#zidStaleWarn`) — التحذير يبقى مدمجاً في **سطر الملخّص الأخضر** (`.vm-stale` على `#verifyMsg`) **وفي بطاقة «ملخّص الملفّين» بالمخزون** (`.stc-stale`، تُقرأ حيّةً عبر `zidIsStale()`). حُذف عنصره وكود ملئه في `renderZidFreshness` وقاعدة `.action-center .zid-stale-warn` (اليتيمة)؛ القاعدة الأساس `.zid-stale-warn` باقية لأن `#resyncWarn` (صفحة التحديث) يستعملها.

**سطر الملخّص الأخضر نُقل إلى المخزون:** فرع نجاح `#verifyMsg` (السطر الأخضر «✓ ملف الكميات…») يُكتب الآن في **`#invSummaryLine`** ضمن بطاقة «ملخّص سريع» بالمخزون (مع `.vm-stale` للتقادم)، وعند النجاح **يُخفى `#verifyMsg` في الرئيسية**. **فرع الفشل فقط** («✗ تم إيقاف التحميل — …») يبقى في `#verifyMsg` بالرئيسية — صمام أمان ملاصق لزرّي التنزيل المعطّلين (نقله يترك الزرّين بلا تفسير). بطاقة «ملخّص الملفّين» في «يحتاج انتباهك» **أُزيلت** (كان محتواها = السطر المنقول، فصارت مكرَّرة)؛ و`.stc-figs`/`.stc-fig`/`.stc-stale` حُذفت (يتيمة). `lastRunSummary` يبقى (بوابة KPI «بلا ربط»). كتلة `?debug` تُلحَق بالعنصر النشط (سطر المخزون عند النجاح، `#verifyMsg` عند الفشل). وبقي في `.action-center` بالرئيسية تنبيهان: `#saleConflictMsg` · `#reactivatedMsg`.

## صفحة «الحساب والنظام» — محذوفة
حُذفت بالكامل وانتقل محتواها إلى **المخزون**. المراجع الثمانية عولجت: زرّ التنقّل · رابط لوحة الجرس (النصّ والوجهة معاً ⇒ «عرض الكل في المخزون») · قسم `#page-account` · `PAGE_HASH` (حُذف) · `HASH_PAGE` (**`account: "inventory"`** — الروابط المحفوظة `#account` تفتح المخزون) · `goPage` · `renderAccount()` (دُمجت في `renderInventoryPage()` ووُحّدت استدعاءاتها الثلاثة) · `#logoutBtn` (حُذف — الخروج في قائمة المستخدم بالشريط العلوي).

## المطابقة
لكل صنف في زد يُبحث عن مطابقته بالمستودع بالترتيب: (1) ربط يدوي محفوظ `manualMap[sku]`، (2) تطابق الكود، (3) كود الأساس قبل النقطة. الأصناف بلا مطابقة تظهر في «يحتاج قرار» للربط اليدوي (يُحفظ ويُعاد استخدامه تلقائياً). المطابقة بالاسم مستخدمة فقط لاقتراحات الربط اليدوي (لا في الإخراج). كل مطابقة ناجحة تُضاف إلى `matchedHistory` (تاريخ المطابقة).

### الأصناف بلا مطابقة في الملف الحالي — ثلاث حالات
عند عدم وجود مطابقة في الملف المرفوع يُميَّز الصنف:
- **بانتظار التوفر ⏳ (تصفير مُدار — `waitingSet` / `waiting.json` ＋ Supabase `waiting_items`):** صنف سجّله المستخدم بزر «غير متوفر حالياً» — يُستثنى من تنبيه المنتجات الجديدة، و**يدخل «ملف الكميات» الرئيسي بكمية 0** (تُضاف صفوفه إلى `qtyRows` مباشرةً في فرع الانتظار — تماماً كالغائب-صفر، فلا حاجة لملف منفصل)، و**يُخفى في «ملف الأسعار» بـ`published=No`** (عبر الإخفاء الموحّد). `stripNoChangeQty` يُسقط تلقائياً ما هو 0 أصلاً في زد (بلا استيراد عبثي)، فيظهر فقط المنحرف (زد≠0). **(أُزيل زر «ملف تصفير الغائب» المنفصل — كان يُنزّل صنفاً واحداً فيبدو معطّلاً بينما الباقي 0 أصلاً؛ صار كل شيء ضمن الملفين الرئيسيين.)** المُصفَّر/المخفي **يظهر صفّاً خفيفاً «⚠ سيُصفَّر» (⏳＋زد≠0) أو «✓ مُدار» (⏳＋زد=0) في «بدون ربط» بزرّ تراجع مباشر** (`unifiedLightRow`) — مُحتسَب في الشريط ومُعالَج في الملفّين. **(جُرِّب إخفاؤه من القائمة في البند ٢ فأربك المستخدم «ضغطت ولا شيء حدث»، فأُعيد ظهوره — قرار المستخدم.)** ولمنع الفعل العبثي أصلاً: **زرّ «غير متوفر» في بطاقة «يحتاج قرار» يُعطَّل حين يكون no-op** (`waitNoopReason`: كمية زد=0 و[متغيّر ⇒ «النشر يُدار من الأب» · أو بسيط غير منشور ⇒ «مصفّر ومخفي أصلاً — لا تغيير مطلوب»]) بسبب دائم مرئي (لا tooltip، فالجوال بلا تحويم). **الشرط نفسه الذي يستعمله `run` (نفس `curXNum`/فحص published/famIndex) — مصدر واحد.** **خطاف الرفع `processWaitingOnUpload` (يُستدعى في `autoAdopt` قبل المطابقة) يمرّ على الانتظار عند كل بناء للمخزن:** الغائب مع **رفع ملف فعلي** (عَلَم `whWasUploaded` يُرفع في `onMergeWh`/`onMergeBranch`) ⇒ `missed_rounds++` (عبر `db.waiting.bumpMeta`؛ ＋ `last_seen_at`=الآن عند الوسم — أساس heuristic «الأصناف الميتة»؛ **لا يُعدّ عند تحميل القاعدة** فلا يتضخّم كل جلسة). **وفور ظهور كوده المُحَلّ في المخزن (ربط يدوي → كود ذاته → أساس، نفس `resolveWhCode` — فلا يفوتنا المربوطون يدوياً؛ يُكتشَف دائماً — حالة لا رفع) يُزال من الانتظار (محلياً ＋ القاعدة `bulkRemove`) ويُوسَم في `reappearedSet`؛ فيوجّهه `run` إلى «يحتاج قرار» بوسم «↩ توفّر» بلا مطابقة تلقائية** (الدفعة ج البند ٣ — الاعتراض `reappearedSet.has(skuN) && !mapped && !ignoredSet.has(skuN)`: **غير المربوط ⇒ «يحتاج قرار» بوسم «↩ توفّر»؛ المربوط يدوياً يستأنف رابطه فعلاً (يدخل «تم تحديثه» والملفّين) لكن بوسم «↩ توفّر — راجع» ظاهر في صفّه** (البند ١ — لأن الكود قد يكون أُعيد استعماله لصنف آخر بعد الغياب؛ «شيلها» متاح)؛ والمتجاهَل يبقى متجاهَلاً)، مع إشعار «↩ توفّر: X صنف» (`renderReactivated`, عنصر `#reactivatedMsg`؛ يُملأ من الخطاف عبر `lastReactivated`). حياد: `reappearedSet` فارغ في الحالة العادية ⇒ صفر تغيير في المطابقة. الوسم كتابة مزدوجة (`markWaiting`/`unmarkWaiting`): المحلي فوراً ＋ القاعدة بأفضل جهد؛ و`loadWaitingFromDB` يجلبها عند الدخول (اتحاد مع المحلي) فتتزامن بين الأجهزة. الفرق عن «✕ تجاهل» حادّ: ⏳ = تصفير مستمر ＋ مراقبة عودة · ✕ = خارج كل الملفات والمطابقات نهائياً.
- **غائب عن الملف 📭 (`lastAbsent`):** له تاريخ مطابقة (`manualMap[sku]` أو `matchedHistory`) لكنه غير موجود في الملف المرفوع — يُصنّف «غائب» لا «بدون ربط». **يُصدَّر بكمية 0 ويُخفى (`published=No`) دائماً** — قاعدة ثابتة بلا خيار منذ الدفعة ٥. **لم يعد له بطاقة مستقلة في صف النتيجة** — يُرى الآن ضمن **📚 المستكشف الشامل** بفلتر «غائب 📭» (شارة `expBadges`). المنطق (⏳/الغائب/العودة/`opts.absent`) **كما هو تماماً** — التغيير عرضي فقط.
- **بدون ربط / يحتاج قرار:** جديد فعلاً بلا تاريخ ولا انتظار.
- **(محذوف) ✕ تجاهل:** أُزيل بالكامل في **الدفعة ١ من تبسيط التدفّق** — راجع القسم المخصّص أدناه.

### تصادم التطبيع + نمط «أب/خيار» في زد
أكواد زد خام مختلفة قد تتطابق بعد `normCode` (إزالة الأصفار البادئة)، مثل `060175` و`60175`. تُصنّف كل مجموعة تصادم قبل الحلقة:
- **مجموعة «أب/خيار» (تُعالَج تلقائياً):** الشكل المؤكد من لوحة زد هو أن المنتج الأب كوده بصفر بادئ إضافي (`060175`) وخياراته بدونه (`60175`, `60178`...). تتأهّل المجموعة إذا كان هناك كود واحد فريد بأكثر الأصفار البادئة، **و** (صف زد له `has_variants` = Yes  **أو**  تشابه اسمه مع الخيارات ‏`nameSimTokens ≥ 0.7`). عندها: الأب (`parentRaws`) يُستبعد من ملفي الكميات والأسعار ولا يُحسب في أي عدّاد، والخيارات تُعاد لها المطابقة التلقائية بالكود. تُعرض هذه المجموعات في لوحة رمادية (`renderVariantInfo`, عنصر `#variantInfo`) بشارة «منتج أب — خياراته هي التي تتحدث»، ولا تظهر في التحذير الأحمر.
- **تصادم صعب (ربط يدوي إلزامي):** المجموعات ذات الأسماء المختلفة جوهرياً (مثل مجلس تركي/سوري، سيف ذهبي/فضي) وبلا أب `has_variants` تبقى في `collidedNorm` + التحذير الأحمر (`renderCollisionWarn`)، ومطابقتها التلقائية معطّلة حتى تُربط يدوياً.

### تصادم التطبيع + نمط «أب/خيار» في زد
أكواد زد خام مختلفة قد تتطابق بعد `normCode` (إزالة الأصفار البادئة)، مثل `060175` و`60175`. تُصنّف كل مجموعة تصادم قبل الحلقة:
- **مجموعة «أب/خيار» (تُعالَج تلقائياً):** الشكل المؤكد من لوحة زد هو أن المنتج الأب كوده بصفر بادئ إضافي (`060175`) وخياراته بدونه (`60175`, `60178`...). تتأهّل المجموعة إذا كان هناك كود واحد فريد بأكثر الأصفار البادئة، **و** (صف زد له `has_variants` = Yes  **أو**  تشابه اسمه مع الخيارات ‏`nameSimTokens ≥ 0.7`). عندها: الأب (`parentRaws`) يُستبعد من ملفي الكميات والأسعار ولا يُحسب في أي عدّاد، والخيارات تُعاد لها المطابقة التلقائية بالكود. تُعرض هذه المجموعات في لوحة رمادية (`renderVariantInfo`, عنصر `#variantInfo`) بشارة «منتج أب — خياراته هي التي تتحدث»، ولا تظهر في التحذير الأحمر.
- **تصادم صعب (ربط يدوي إلزامي):** المجموعات ذات الأسماء المختلفة جوهرياً (مثل مجلس تركي/سوري، سيف ذهبي/فضي) وبلا أب `has_variants` تبقى في `collidedNorm` + التحذير الأحمر (`renderCollisionWarn`)، ومطابقتها التلقائية معطّلة حتى تُربط يدوياً.

## القائمة الموحّدة «بدون ربط» (الدفعة ب-٢) — قائمة واحدة، لا تبويبات ولا وضعان
فلتر «بدون ربط» يعرض **قائمة واحدة متصلة** لكل الأصناف (`renderUnifiedList(title, wrap)` — الأساس بطاقة `batchCardHTML`، **دائماً على التحليل**، بلا زر «تحليل الكل» ولا تبويبات). حُذف الوضع العادي القديم (`renderUnmatchedCard`/`cardHtml`/`buildItems`) وتبويبات الثقة الثلاث والتبويبات الخمس (`openBatchAnalysis`/`renderBatchTab`/`renderGreen/Yellow/Red`/`setUnmatchedSub`… — عنقود ~44 دالّة/متغيّراً حُذف بالتحقّق المزدوج). الترتيب: **«يحتاج قرار» أولاً** (بطاقات كاملة، ترتيب الثقة green→yellow→red، «↩ توفّر» ثم «سبق ربطه» أولاً) ثم **«📭 غائب»** (قسم مستقلّ `.uni-sec`) ثم **المُقرَّرة صفوفاً خفيفة** (`unifiedLightRow`): «غير متوفر» (الدفعة ٢ وحّدت التسميتين القديمتين «سيُصفَّر»/«مُدار») و«✕ متجاهَل» بتراجع مباشر (`undoWaitDecision`/`undoIgnoreDecision`). **عدّادات الشريط تُحسَب من `batchData` الخام دائماً (`needN`/`absentN` قبل `btFilter`)، و`btFilter` يُطبَّق على البطاقات المرسومة فقط** (عطل قديم: كان يُحسَب من المُصفّى فيختلف عن البطاقة العلوية عند تفعيل «سبق ربطه فقط»). كل إجراء يمرّ عبر `applyDecision(action)` الموحّد (الدفعة ج البند ٤: قاعدة أولاً → `run(true)` يعكسه في الملفّين فوراً). السبب الجذري للتكدس: الاسم مختلف كلياً بين زد والمستودع بينما **المقاس والسعر مشتركان**.
- **فصل «غائب» عن «يحتاج قرار» (حاسم للمرحلة 1)**: `analyzeBatch` يصنّف كل صنف `isAbsent` (من `lastAbsent`) ويعيد `needCount`/`absentCount` منفصلين. «يحتاج قرار» = **الجديد فقط** (`unm` بعد `okItem` — بلا ربط ولا تصنيف)؛ «غائب» = `lastAbsent` (أصناف لها كود سابق وغائبة عن الملف الحالي — فروع المرحلة 1، غيابها متوقّع لا قرار مطلوب). كلاهما **يبقى في القائمة وقابلاً للإجراء** — الفصل في العدّاد/التسمية/الترتيب لا الوصول. **خطأ مواصفة سابق صُحّح**: كان الغائب يُطوى ضمن «يحتاج قرار» فينتفخ العدّاد (609 = 37 جديد + 572 غائب).
- **«سبق ربطه» (`wasLinked`) = `isAbsent && !!manualMap[sku]`** — غائب رغم **كود يدوي محفوظ** (رابط يدوي ضائع). **ليس `matchedHistory.has`** (تراكمي لا يُمسح فيتشبّع ويصير صادقاً للجميع، فيساوي «يحتاج قرار» زوراً). الآن lostCount مقيّد بعدد الروابط اليدوية الغائبة (صغير ومعقول — إثبات ميكانيكي في `tests/b2-analyze-counts.mjs`: مع `matchedHistory` مُشبَّع، القديم=15 والجديد=3<needCount).
- **الرسم التدريجي (mixed + lazy + progressive)** — للجوال: أول `UNI_CHUNK=40` بطاقة فوراً ثم الباقي عبر `requestAnimationFrame` (`scheduleUnifiedChunk`). البحث النصّي يعمل **فوق الـ300 كاملة** لا المرسوم فقط: `filterUnmatched` يستدعي `flushUnified` (يُدخِل كل المتبقّي) قبل التصفية. الصور `loading="lazy"` أصلاً في `thumb`. **مقيس** (`tests/perf/run-bench.mjs`): baseline 300 كاملة ≈ 8343 عقدة/110ms؛ mixed ≈ 3906/68ms — العقد هي المهيمن، والتمرير ≤0.5ms.
- **شريط الحالة اللاصق المعتم** (`unifiedBar`): عدّادات الحالة ＋ ثقة «يحتاج قرار» (نقاط SVG `DOT_*` لا إيموجي) ＋ فلتر «🔗 سبق ربطه» (`toggleLostOnly`/`btFilter`) ＋ «✓ اعتماد المؤكد» (green) و«✓ اعتماد كل المحدد» (`acceptUnified`) ＋ «↩ تراجع» (`undoLastBatch`). خلفيته `linear-gradient(panel) فوق bg` = **معتمة** (لا يشفّ المحتوى تحته).
- **الحراس الأربعة** (CI، وكلٌّ يُثبت رسوبه على الكود المعطوب): تجاوز أفقي ＋ لاصق معتم @360/390 عبر `puppeteer-core` (viewport حقيقي — `tests/perf/overflow-guard.mjs`) · الرسم التدريجي لا يكسر البحث (`tests/perf/progressive-search.mjs`) · إيموجي=SVG ＋ لا أرقام قبل المطابقة (`tests/b2-invariants.mjs`).
- **محرك نقي** (قابل للاختبار): `extractSize(name)` (يوحّد `* × x`، يرتّب تصاعدياً، ثنائي/ثلاثي ⇒ «6x120» أو null) · `normalizeName` (يزيل المقاس والتشكيل وأل التعريف والرموز، يوحّد الهمزات/التاء المربوطة) · `nameSimilarity` (Dice على ثنائيات الحروف، 0-1) · `priceMatch` (فرق ≤5% أو ≤2 ريال) · `nameRoot` (أول 3 كلمات مطبَّعة — للعائلة وaliases).
- **الترشيح والتصنيف** (`analyzeBatch`): لكل صنف زد يُرشَّح من **أكواد المستودع غير المربوطة/غير المطابَقة**، بفهرسة بالمقاس والكلمات. الدرجة (للترتيب/السبب): مقاس +50 · سعر +30 · اسم≥0.5 حتى +20 · alias +40.
  - **⚠ تشديد T1 (حاسم — بعد حادثة الـ22 رابطاً خاطئاً في «اعتماد كل المحدد»):** التصنيف صار: **🟢 أخضر = تطابق الاسم التامّ لمرشّح وحيد فقط** (`classifyBatch(r)`: `r.exactUnique` أولاً، قبل أرضية `<40`) — عبر `normNameStrict` (يُبقي **الأرقام والمقاسات** بخلاف `normalizeName`) ＋ `whStrictCount===1` (الاسم المكرّر ⇒ أصفر). **لا سعر/تشابه/alias يرفع لأخضر إطلاقاً** (أُسقط مسار `score≥80`). · **🟡 أصفر = ما عداه** (مقاس/سعر/تشابه) · **🔴 <40**. الترتيب: اسم تامّ ← غير متعارض (`numConflict`: أرقام مميِّزة منفصلة 162≠164) ← درجة. السبب المرئيّ (`reasonText`): «اسم مطابق تماماً» / «مقاس✓» / «سعر✓ · اسم52٪ · ⚠ موديل/حجم مختلف».
  - **الصندوق الافتراضيّ للأخضر فقط** (`chkCls==="bt-g"`) — «اعتماد كل المحدد» لا يبتلع الأصفر/الأحمر بنقرة. **التعلّم (`aliases`) من الأخضر (`auto-confirmed`) فقط** — لا من `auto-approved`/يدوي. **الدرس:** «نظام يتعلّم من اعتماداته يضاعف أخطاءه: خطأ واحد يرقّي أخطاء لاحقة إلى مؤكّد؛ التعلّم يقتصر على أعلى الثقة.» **التصدير كوديّ بحت** (`resolveWhCode`: كود/`baseOf` فقط، لا اسم) — التسرّب كان في **طبقة الاقتراح**، والاعتماد الدفعيّ يحوّل اقتراحاً ضعيفاً إلى رابط كوديّ ثابت.
  - **زرّ «إلغاء ربط المحدّد» (`bulkUnlink`، دفعيّ):** يُلغي الروابط اليدوية للأصناف المحدّدة (`testSelected` التي لها `manualMap`) دفعةً — تشغيل `run(true)` **واحد** في النهاية. `dbDelMapping` لكلٍّ (‏`unlinkedSet.add` ⇒ تعود «يحتاج ربط» **بلا تصفير**، لا `DELETE` مباشر الذي يجعلها «غائبة»⇒صفر). تأكيد بالعدد. `testSelected`/`expSelected` **مجموعتان مرجعيّتان** تصمدان عبر البحث/الفلترة (الصناديق تُبنى منهما عند كل رسم).
  - **المستكشف — شريحة «رابط محفوظ» ＋ إلغاء دفعيّ (حاسم للأصناف «الغائبة»):** «مربوط» في المستكشف يشمل **المطابَق بالكود** (مئات)، فلا يعزل الروابط المحفوظة. شريحة **«رابط محفوظ (N)»** (‏`f.link.has("saved")`، عدّ حيّ) تعرض **ما له صفّ في `mappings` حصراً** (‏`r.saved = !!manualMap[raw]`، لا `linked`). ＋ عمود تحديد (للمحفوظة فقط) ＋ «تحديد المحفوظ المعروض» ＋ «إلغاء ربط المحدّد» (`bulkUnlinkExplorer`، نفس `dbDelMapping`/`unlinkedSet`). **لماذا المستكشف لا «تم تحديثه»:** الصنف المربوط بكود **غائب** (مثل «مقلاة ارت لين» qty 0) لا يظهر في «تم تحديثه» (حالته «غائب») — والمستكشف وحده يعرضه ويتيح إلغاءه. **المصير بعد الإلغاء** ([index.html:4415](index.html#L4415)): `hasHistory = (manualMap||matchedHistory) && !unlinkedSet` ⇒ بعد `dbDelMapping` (يملأ `unlinkedSet`) = false ⇒ «يحتاج ربط» **لا «غائب»** ⇒ لا يُصدَّر بصفر. **القيد:** `unlinkedSet` للجلسة — الإلغاء ＋ إعادة الربط ＋ التنزيل في جلسة واحدة. حارس `tests/perf/explorer-unlink.mjs` (G-EXPUNLINK، بأسنان): «رابط محفوظ» يعزل mappings فقط (لا المطابَق بالكود)؛ `--broken` يجعل `saved=linked` ⇒ يرسب. لم تُمَسّ بقيّة فلاتر المستكشف ولا «تصدير المعروض».
  - حارس `tests/perf/match-tighten.mjs` (G-TIGHTEN، بأسنان): BP162/BP164 بنفس السعر ＋ alias (درجة 88) ⇒ **أصفر لا أخضر** · الاسم التامّ ⇒ أخضر · الصندوق/التعلّم للأخضر فقط؛ `--broken` يعيد `score≥80` ⇒ BP يصير أخضر ⇒ يرسب. ＋ عدّاد `?debug` «قياس تشديد المطابقة» (أخضر حاليّ · هبوط · صعود ＋ ٥ أمثلة) — نافع لقياس أي تغيير معيار.
- **بطاقة «يحتاج قرار»** (`batchCardHTML`, صنف `.mcard.batch-card` بصورة `thumb` و`data-search`/`data-status="need"`, **عمودان متقابلان** ZID PRODUCT ↔ WAREHOUSE ＋ إجراءات؛ النسبة/الأسباب `title` tooltip). البحث الهجين لكل بطاقة **عائم على مستوى `body`** (`.bt-results-float`, `position:fixed`, تُموضَع بـ`getBoundingClientRect`) — إغلاق بفحص المستطيل (`ptInDrop`) لا `blur`، تنقّل بالأسهم ＋ `Enter` (`comboKey`)، اختيار يثبّت المرشّح (`comboSearch`/`comboPick`) بلا حفظ فوري. سجل `batchRowByUid` (uid⇒صف يصمد أمام الفلترة). إجراءا الصفّ ⏳/✕ (`batchExclude`، القاعدة أولاً) يُحوّلانه إلى صفّ خفيف مُقرَّر. الاعتماد للمحدد فقط عبر `acceptUnified('green'|'all')`.
- **الاعتماد والتراجع** (`batchApply`/`undoLastBatch`): يكتب `mappings` في القاعدة دفعةً (`dbSetMappings`، المفتاح = SKU الخام)، يسجّل التراجع، يتعلّم زوج aliases (`db.aliases.bulkUpsert`). **الوسم يُشتقّ من `mappings.source`** (لا `linkTags` — أُسقط). بعد الاعتماد يُعاد التحليل (`batchData=analyzeBatch()`) ويُعاد رسم القائمة.
- **قواعد السلامة**: كود مستودع واحد لا يُعتمد لأكثر من صنف زد (إلا متغيّرات نفس الأساس `baseOf` — توزيع)؛ عند التنازع الأعلى درجة يفوز والآخر يهبط 🟡. الآباء (`has_variants=Yes`/`orphanParent`) خارج الترشيح. لا مساس بالروابط اليدوية القائمة.
- **التعلّم** (`aliases.json`): مجموعة «جذر زد|||جذر مستودع»؛ كل اعتماد يضيف زوجاً فترتفع 🟢 كل دورة. تُضاف `aliases.json` و`link-tags.json` إلى منظومة `☁ الحفظ في الريبو` وexportConfig وloadConfig (ملف ＋ localStorage).
- **وسم «🔗 سبق ربطه» (الروابط اليدوية الضائعة)**: تُدرَج الأصناف **الغائبة** (`lastAbsent` = في `matchedHistory` ولا تُطابَق بالكود الآن — مثبت أنها رُبطت سابقاً) داخل التحليل الدفعي مع الجديدة (`analyzeBatch` يجمع `lastUnmatchedRaw` ＋ `lastAbsent`)، وتُوسَم `wasLinked = matchedHistory.has(skuN)` بشارة **«🔗 سبق ربطه»** وتُرتَّب **أولاً** في كل تبويب (وداخل عائلات 🟡). شريط اللوحة يعرض عدّادها (`lostCount`) وزرّ فلترة **«🔗 المفقودة فقط»** (`batchLostOnly`/`toggleLostOnly`/`btFilter`/`curYellowFams`). هذه أولى بإعادة الربط لأن هدفها المستودعي مفقود من كل مصدر (mapping.json/localStorage/القاعدة) — إعادة ربطها تكتب في Supabase مباشرةً (مصدر دائم).

## آلية الإخراج: ملفّان منفصلان (مؤكدة بالتجربة مع استيراد زد)

### 1) ملف الكميات — `zid_quantities_update.xlsx`
- **٤ أعمدة حرفياً وبالترتيب**: `sku | name_ar | name_en | <ZID_QTY_COL>`
- `ZID_QTY_COL` ثابت (const) أعلى السكربت = `"عبدالله صلاح الجهني للتجارة - كميات"` — **غيّره إذا تغيّر اسم المستودع في زد**.
- صف لكل صنف مطابق تغيّرت كميته؛ العمود الرابع = الكمية الحقيقية من المستودع (عدد صحيح ≥ 0).
- **القاعدة الحاسمة من زد — لا منتج أب في ملف الكميات إطلاقاً**: أي صف قيمته `has_variants = Yes` في تصدير زد **منتج أب** (شكل الكود لا يدل على شيء — قد يكون عادياً بخيارات داخلية مثل `80223`/`80153`). يُجمَع في `hasVarYesSet` ويُضاف إلى `parentRaws` مبكراً، فيُستبعَد من التوزيع والملفين. **كميته تُوزَّع على خياراته المكوّدة** (صفوف زد التي تُطابِق نفس كود المستودع) بالمنطق الحالي. **الأب بلا خيارات مكوّدة** (لا صنف يتلقّى كميته) يُدفَع إلى «يحتاج قرار» بشارة «🚫 أب بلا خيارات مكوّدة — عالجه في زد» (`orphanParent`). دالة `stripHasVar(rows)` **فحص نهائي إلزامي** يُطبَّق على **كل** مصفوفة صفوف كميات قبل الكتابة (الرئيسي `run` · التجربة `downloadSelected`) فتُحذف أي بقايا. تقرير `?debug`: «has_variants=Yes مُستبعَدة: N · منها كانت ستدخل الملف: M — أكواد …».
- **«غير المحدود» (Infinite) المطابِق يُستبدل بالكمية الحقيقية** (يحل مشكلة الأصناف غير المحدودة؛ عدّاد `infConverted` في سطر التأكيد). **غير المحدود بلا مطابقة دُمج في «بدون ربط»** (ب-١ من الدفعة ج): أُزيلت بطاقته وتصنيفه (`lastInfinite`/`infiniteList`)، ويسقط الصنف في `unmatched` (فرع `else`) بـ`finalQtyByRaw=∞` بلا أي دفع إلى ملفي الكميات/الأسعار — يُعامَل كبقية «بدون ربط».
- `name_ar`/`name_en` تُنسخ من تصدير زد حرفياً.
- **مسار الاستيراد في زد**: استيراد ← تحديث كميات المنتجات الحالية فقط.
- **توزيع كمية الكود على المتغيرات** (`opts.split`, افتراضياً «قسّم بالتساوي»): إذا طابَق كودَ مستودع واحد أكثرُ من متغيّر زد (غير الآباء المستبعدين)، تُقسَّم كمية الكود على المتغيرات (تمريرة أولى تبني `whMembers` ثم `shareByRaw`) — قسمة صحيحة والباقي للمتغيّر الأول (7 على 3 = 3+2+2). الوضع البديل «الكمية الكاملة لكل متغير». **الحصة الموزّعة تصبح `whQtyReal`** فتتكامل مع قاعدة تصفير القيمة المنخفضة. شارة 🔀 في جدول المحدّثة بـtooltip «كمية الكود (X) موزّعة على Y متغيّراً». عدّاد حيّ في سطر التأكيد: «موزّع الكمية: X متغيّر في Y كود».
- **دالة موحّدة `resolveWhCode(rawSku, skuN)`** تحدد كود المستودع (ربط يدوي → تطابق كود → `baseOf`) وتُستخدم في **تمريرة التوزيع وحلقة المطابقة معاً** — يمنع أي اختلاف صامت بين «من يُحسب له نصيب» و«من يأخذ الكمية».
- **استبعاد الأب بنمط الكود المنقّط** (يُعالَج داخل تمريرة التوزيع): صنف زد بلا نقطة كوده = كود المستودع الأساس وله متغيّرات منقّطة تشترك بنفس الكود (مثل «80151» أباً و«80151.1..4» خياراته) ⇒ يُضاف إلى `parentRaws` فيُستبعد من المخرجات، و**الكمية تُوزَّع على الخيارات المنقّطة فقط** (88 على 4 = 22 لكلٍّ · أو 17 على 4 = 5+4+4+4). هذا مكمّل لاستبعاد الأب بنمط التصادم (الصفر البادئ). الآباء لا يدخلون القسمة إطلاقاً.
- **قاعدة «تصفير الكميات المنخفضة القيمة»** (`opts.lowzero`, افتراضياً مفعّلة): أي صنف مطابَق **سعره النهائي (شامل زيادة المتغير) أقل من `opts.lowPrice` (افتراضي 400)** وكميته الفعلية `≤ opts.lowQty` (افتراضي 3) ⇒ **تُصدَّر كميته 0** (بدل كمية المستودع). **أساس السعر في الحكم يتبع `opts.price`** (شامل/قبل الضريبة) فيطابق السعر المُصدَّر فعلاً؛ وفي وضع «لا تحدّث» يرجع للشامل ثم قبل الضريبة (`valBase`). سعره يتحدّث طبيعياً في ملف الأسعار، ولا يدخل «إعادة النشر» (كميته 0). حقلان رقميان قابلان للتعديل في الخيارات (يُحفظان في `opts`/localStorage). المقارنة بالكمية تكون على **الكمية الفعلية للصنف** (لو أضيف لاحقاً توزيع «قسّم بالتساوي» فستُقارن الحصة تلقائياً لأنها تصبح `whQtyReal`). شارة «📉 صُفّر (قيمة منخفضة)» في جدول المحدّثة + «· صُفّر لانخفاض القيمة: X صنف» في سطر التأكيد. زر «شيلها» يستثنيها للجلسة كالمعتاد.

### 2) ملف الأسعار — `zid_prices_update.xlsx`
- **نفس بنية تصدير زد كاملة** (كل الأعمدة بترتيبها وتسمياتها)، يُعدَّل `price` و`published` و(عند اللزوم) `sale_price` فقط.
- **تعارض سعر التخفيض (`sale_price`) ⇒ استبعاد كلّي، لا تفريغ**: زد يشترط `sale_price < price`، **ويتحقق من السعر الجديد مقابل التخفيض المخزَّن لديه** (ثبت بالتجربة: تفريغ الخلية في الملف لا يزيل الخصم المخزَّن مع «تجاهل الحقول الفارغة»، فيبقى الرفض). لذا **أي صنف تخفيضه المخزَّن (`iSale = H.indexOf("sale_price")`) ≥ سعره النهائي يُستبعَد كليّاً من ملف الأسعار** (لا يُدرَج أصلاً)، ويُجمَع في `saleConflicts` ثم يظهر في لوحة برتقالية **«⚠ تعارض تخفيض — عالجه يدوياً في زد»** (`renderSaleConflicts`, عنصر `#saleConflictMsg`) بالـSKU والسعرين (الجديد ＋ التخفيض المخزَّن). المستخدم يعدّل/يحذف الخصم في لوحة زد ثم يحدّث السعر. `verifyPrices` يتحقق دفاعياً ويرفض أي صف `sale_price ≥ price` تسرّب.
- **حماية إكسل (التفريغ/القصّ الوحيد المتبقّي)**: أي خلية في صف الأسعار تتجاوز **32767 حرفاً** (حد إكسل الصلب) تُقصّ إلى 32767 لمنع تلف الملف؛ `verifyPrices` يسمح بهذا الاختلاف حصراً (`ov.length > 32767 && nv === ov.slice(0,32767)`).
- **السعر النهائي = تقريب سعر المستودع ＋ زيادة المتغير** (price offset، الافتراضي 0). قاعدة تجاهل فرق < 1 ريال تُطبَّق على الناتج النهائي مقابل سعر زد الحالي.
- **إدارة النشر تلقائياً (`published`) — يُتحكَّم من الأب لا المتغيّر**: في زد النشر خاصية المنتج **الأب**؛ وضع `published=Yes` على صف متغيّر بلا أثر ويسبب تكرار «سيُعاد نشره» كل دورة. لذا:
  - **ربط المتغيّر بأبيه**: يُكتشَف عمود «مرجع الأب» تلقائياً (`parentCol`) = العمود الذي تشير قيمه إلى `sku` صنفٍ `has_variants=Yes`، فيُبنى `parentOf[variantSku] = parentSku` (مثال: 421629 ← 0423247). لا يعتمد على شكل الكود.
  - **فحص النشر على الأب**: `wantsPublish` يفحص حالة نشر **الأب** (`effPubRow`) للمتغيّرات، وحالة الصنف نفسه للبسيط. فحين يُنشَر الأب يتوقف التكرار.
  - **البسيط** (`simpleRepublish`): `published=Yes` على صفّه مباشرة (No→Yes فقط). **المتغيّر لا يحمل `published` إطلاقاً** — يُفرَّغ العمود على صفّه (بلا أثر ومضلل).
  - **صف الأب**: لكل أب نُقل إليه النشر يُضاف صف في ملف الأسعار بـ`sku` الأب و`published=Yes` **فقط** (بقية الأعمدة ومنها السعر فارغة؛ الأصل المحاذي published=No). يظهر في لوحة خضراء **«🔆 نشر مُنقَل إلى الأب»** (`renderParentPublish`, `#parentPublishMsg`, `lastParentPublish` — **العنصر في صفحة المخزون ← ١ يحتاج انتباهك**، والرسم في `run()` بالمعرّف). **تنبيه**: الآباء مرفوضون في ملف الكميات فقط؛ إن رفض زد صف الأب في «تحديث المنتجات» يُفعَّل نشرهم يدوياً من القائمة.
  - **الإخفاء التلقائي الموحّد عبر كل أبواب التصفير (`zeroDoor` + تمريرة موحّدة)**: القاعدة = «أي صنف بسيط منشور صارت كميته النهائية `finalQtyByRaw[raw] === 0` — مهما كان باب تصفيره — يُخفى (`published=No`) بلا أي تغيير سعر». التصفير له ثلاثة أبواب: (1) قاعدة القيمة المنخفضة (2) ⏳ اليدوي (3) الغائب بخيار «صفّر». **البابان 1 وحالة المطابَق-صفر يُعالَجان في حلقة المطابقة (`autoUnpubSimple`)؛ والبابان 2/3 يصفّران بلا مطابقة (`continue`) فكانا يبقيان الصنف منشوراً — سُدَّت الفجوة بتمريرة موحّدة بعد الحلقة** تقرأ `zeroDoor` (يُوسَم فيه raw عند التصفير عبر ⏳/غائب-صفر) وتُصدِر صف `published=No` للبسطاء المنشورين (تتخطّى: المُعالَج مسبقاً في `autoUnpub`، اليدوي 🚫، الآباء/المتغيّرات، غير المنشور، وتعارض التخفيض `sale≥price`). **المتغيّرات لا تُعالَج هنا** — `finalQtyByRaw` يغذّي **إجماع إخفاء الأب** بـ0 أصلاً فيُخفى الأب متى كانت كل أبنائه 0 (والابن الحيّ ولو واحد يحميه). **التماثل في العودة**: عودة الكود للمخزن بكمية ⇒ مطابقة تلقائية + إزالة ⏳ + `wantsPublish` (newQty>0 و published=No) ⇒ إعادة نشر — نفس المسار للأبواب الثلاثة ذهاباً وإياباً. سطر التأكيد يفكّك المصدر: «إخفاء تلقائي: N — بسيط X (رخص · مطابَق٠ · ⏳ · غائب) · آباء P»، و`?debug` يعرض كل صنف بباب تصفيره `sku(src)`.
  - `verifyPrices` يسمح في `published` بـ **No→Yes أو التفريغ «»**، و**Yes→No فقط للإخفاء الآلي** (`allowUnpub` = `autoUnpub` — يشمل البسطاء عبر كل الأبواب ＋ الآباء بالإجماع، فيتّسع الصمام لهم تلقائياً بلا استثناء عام؛ أي Yes→No لصنف خارجه يبقى مرفوضاً). **(أُزيل مسار «🚫 إلغاء النشر» اليدوي `unpublishSet` — الدفعة ج البند ١؛ فلم يعد الاتحاد `[...unpublishSet, ...autoUnpub]` بل `autoUnpub` وحده.)**
- **عمود `quantity` يبقى بقيمه الأصلية دون أي لمس** (الكميات مسؤولية الملف الأول).
- لتقليل الحجم: يشمل فقط صفوف المنتجات التي تغيّر سعرها فعلاً **أو** لزمها إعادة نشر + صف الرأس الكامل.
- **مسار الاستيراد في زد**: استيراد ← تحديث المنتجات الحالية ← حدّد حقلَي (السعر) و(منشور) ← تجاهل الحقول الفارغة.

## صمامات الأمان (تمنع التحميل عند الفشل)
- **ملف الكميات** (`verifyQuantities`): الرأس = ٤ أعمدة بالتسميات الحرفية بالترتيب، `sku` غير فارغ، الكميات أعداد صحيحة ≥ 0.
- **ملف الأسعار** (`verifyPrices`): أعمدة مطابقة للأصل بالترتيب والتسميات، ولا يتغيّر سوى عمودَي `price` و`published` (فيبقى `sku`/`barcode`/`quantity` وبقية الأعمدة كما هي). و`published` يُسمح فيه بـ **No→Yes حصرياً** — أي اتجاه آخر يوقف التحميل.

## قسم ③ «🔍 استعلام عن منتج» (بحثان مستقلان — بدل «ربط يدوي» القديم)
- استُبدل قسم «③ ربط يدوي» بالكامل (لا بقايا واجهة: أُزيلت `whSearch`/`mSku`/`mCode`/`pills`/`searchWarehouse`/`pickWh`). **منطق الربط باقٍ** (`addMapping`/`dbSetMapping`)، والربط اليدوي الآن **حصراً داخل لوحة التحليل الذكي** (البحث الهجين `comboSearch`/`comboPick`) — زر «🔍 ربط يدوي» في بطاقات «يحتاج قرار» صار `startManualLink` يفتح لوحة التحليل ويركّز على صف الصنف. عدّاد الروابط انتقل لشريط الترويسة (`#mapCountBadge`، ولا يزال `#mapCount` مصدره).
- **عمودان RTL مستقلان** (`.query-row`، يتكدّسان تحت 900px): بحث حيّ بـdebounce (`qDebounce`, 180ms)، **يعرض جميع النتائج** (بلا حدّ) مع عدّاد «N نتيجة» وتمرير داخلي (`.q-results`)، وتطبيع بـ`normText`/`normCode`.
- **🏬 بحث في المخزن** (`runWhQuery`): يبحث في `lastMerge` بالكود/الاسم/الباركود، لكن **تفصيل الكمية لكل فرع** يُقرأ من `sourceBreakdown(code)` لا من `lastMerge` (الذي يجمع كل الفروع في `brQty` واحد). كل بطاقة: الكود · الاسم · **تفصيل المصادر** · السعر · الباركود.
  - **`sourceBreakdown(code)` (دالّة موحّدة، عرض محض):** تقرأ `mergeWh` (اسم «المستودع») ＋ **`mergeBranches` مباشرةً** (كل فرع بقائمته)، وتُرجع `{ total, sources:[{name, qty, price}] }` بترتيب المستودع ثم الفروع (أقدميّة). الأسماء من `invBranches[].name` (عمود `branches.name`). **قابلة لإعادة الاستعمال في بطاقة المطابقة لاحقاً** — لا تُكرَّر. اعتمادها على `bid` على كل صفّ فرع (يُوسَم في `loadInventoryFromDB`؛ `mergeInventory` يتجاهله ⇒ صفر أثر على المخرج، G-THROUGH أخضر) — لا على ترتيب الفهرس (‏`.filter` يُسقط الفروع الفارغة).
  - **العرض:** المصادر بكمية 0 تُخفى (يقصر السطر)؛ لا مصدر بكمية ⇒ «غائب عن كل المصادر» صراحةً (لا سطر فارغ)؛ السعر مع كل مصدر إن **اختلف** بينها (يكشف أخطاء الإدخال، `.q-src-px`) ومرّة واحدة إن **توحّد**.
  - حارس `tests/perf/query-sources.mjs` (G-SRC، بأسنان): 6+11 ⇒ إجمالي 17 وتفصيل «العزيزية 6 (100) · الخضرة 11 (120)» والمستودع 0 مخفيّ · الموحّد ثابت 17 · الغائب رسالة؛ `--broken` يجمع الفروع في مصدر واحد (يتجاهل `bid`) ⇒ يختفي التفصيل ⇒ يرسب.
- **🏪 بحث في متجر زد** (`runZdQuery` + فهرس مُخزَّن `buildZidIndex`، مُبطَّل عند تغيّر `stData`): يبحث بالـSKU/الاسم/الباركود. كل بطاقة: SKU · الاسم · شارة النشر (✅/🚫 من `published`، **وللمتغيّر تُعرض حالة نشر أبيه** عبر اكتشاف عمود مرجع الأب كما في `run`) · السعر · الكمية · الباركود · شارة «أب» إن كان `has_variants=Yes`. **رسالة إن لم يُرفع تصدير زد بعد.**

## الواجهة
- **داشبورد النتيجة (`#result`) بأربع طبقات هرمية** (إعادة تنظيم بصري بحت — صفر تغيير منطق، نفس المعرّفات/onclick): ① **شريط الحالة** `.result-status` (نقطة `#rsDot` طازج/قديم ＋ `#zidAgeHead` ＋ `#rsInvAge` عمر المخزن) · ② **بطاقات KPI** `.stat` — **أربع بأدوار محددة** (شريط لون علوي `::before` حسب `data-filter`، أيقونة `.stat-ic`، رقم بارز، تسمية `.stat-lbl`، hover يرفع، `.active` توهج ذهبي): (1) **✅ تم تحديثه** (أخضر) · (2) **🔶 بدون ربط** (كهرماني، `.stat-wide` عريضة، سطر تفكيك `#sUnSub`: «يحتاج قرارك X · متجاهَل Y · ⏳ Z · مُدار W» من `updateUnmatchedCard`) · (3) **📭 غائب عن الملف** (بلا `data-filter` — ليست فلتراً؛ لكنها **بطاقة نشطة قابلة للإبراز** عبر `data-card="absent"`: `selectAbsent()` يفتح القائمة الموحّدة (فلتر unmatched) ويُمرّر إلى قسم «غائب» `#uniAbsentSec` ويُبرزه، ويجعل البطاقة نشطة. الإبراز مفصول عن الفلتر: `renderDetailBody` يضبط `.active` بـ`data-card === activeCard` لا `data-filter`، فبطاقة واحدة نشطة فقط دائماً. `<button>` بدور أصيل + `aria-label` وجهة + `aria-pressed`) · (4) **📚 إجمالي الأسطر — مستكشف**. `currentFilter` القيم الآن: `unmatched`(افتراضي)/`updated`/`explorer` (أُزيل `infinite` مع تصنيفه). أُزيلت بطاقتا «جديد» و«غير محدود» ودُمجتا في «بدون ربط». `#sNew`/`#sAbsentLbl` مخفيان دفاعياً؛ `#sInf` حُذف. · ③ **مركز التنبيهات** `.action-center` (بالرئيسية بقي تنبيهان بعد نقل `collisionWarn`/`newAlert`/`variantInfo`/`parentPublishMsg` إلى المخزون وحذف بانر `zidStaleWarn`: 🟠 `saleConflictMsg` ← 🟢 `reactivatedMsg`، بحاشية بادئة ملوّنة موحّدة) · ④ **الملفات** (`#verifyMsg` شريط ملخّص أخضر فوق بطاقتَي التنزيل `.dl-card` بأيقونة+اسم+عدد+مسار الرفع). `renderZidFreshness` يملأ الطبقة ①.
- **قائمة «🔶 بدون ربط» الموحّدة (`renderUnifiedList`, `currentFilter="unmatched"`):** قائمة واحدة (لا تبويبات) — راجع قسم «القائمة الموحّدة» أعلاه. العدّاد `#sUn` = كل أصناف زد بلا ربط. الحالة داخل كل صفّ: «يحتاج قرار» بطاقة كاملة · و«⚠ سيُصفَّر»/«✓ مُدار»/«✕ متجاهَل» صفوف خفيفة بتراجع مباشر. كل صفّ (كامل وخفيف) يحمل **شارة نشر زد** (`pubBadge`: «منشور» `--green` / «غير منشور» `--muted`، أيقونة SVG، ومصدرها عمود `published` نفسه؛ و«← سيُخفى» إن كان في `lastAutoUnpub`). عند عودة الكود للمخزن يُزال من الانتظار (خطاف الرفع) ويظهر في «يحتاج قرار» بوسم «↩ توفّر» بلا مطابقة تلقائية (البند ٣).
- بطاقات النتائج الأخرى: «✅ تم تحديثه» (تغيّرت كميته أو سعره، ويشمل تحويلات غير المحدود المطابِق)، و«📚 إجمالي الأسطر» صارت **مستكشف منتجات زد الشامل**. (بطاقة «غير محدود بلا مطابقة» أُزيلت — دُمجت في «بدون ربط».)
- **📚 المستكشف الشامل (`renderExplorer`/`buildExplorerRows`/`explorerCompute`, `currentFilter="explorer"`):** جدول كل منتجات زد من `stData` بشريط فلاتر **متعدد التحديد يتقاطع (AND بين الأبعاد، OR داخل البُعد)**: الربط (مربوط/بدون ربط) · النشر (منشور/غير منشور — للمتغيّر نشر أبيه من `famIndex`) · النوع (أب/متغيّر/بسيط) · الكمية (صفر/>0/∞) · الحالة الإدارية (⏳/✓ مُدار/🚫/متجاهَل/غائب 📭). بحث نصي حيّ فوقها (`onExpSearch` يحدّث الجدول فقط فيبقى تركيز الحقل)، عدّاد «يعرض X من N» (`#expCount`)، عمود حالة بالشارات (`expBadges`)، سقف عرض 500 صف (`#expCapNote`)، وزر **«⬇ تصدير المعروض»** (`exportExplorer`: إكسل بالصفوف المفلترة كاملةً — أداة جرد فورية). الفلاتر في `expFilters` (Sets)، البحث `expSearch`/`expSearchRaw`. الربط يُشتق من `manualMap` ＋ أكواد `lastMerge`؛ الغائب من `lastAbsent`.
- خيار «الأصناف الغائبة عن الملف» (قسم الخيارات، `opts.absent`): «اتركها كما هي في زد» (افتراضي) / «صفّر كميتها في زد» (مع تحذير `#absentWarn` — لا يُستخدم إلا في المرحلة 2). تغييره يعيد بناء الملفين فوراً.
- زر ثالث «⏳ غير متوفر حالياً» على بطاقات «يحتاج قرار» (`waitRow`): يسجّل الصنف في `waitingSet`/`waiting.json` ثم **يستدعي `run(true)` فوراً** فتُعاد المعالجة لحظياً: يُصفَّر بكمية 0 في «ملف الكميات» **و**يُخفى في «ملف الأسعار» (`published=No` عبر الإخفاء الموحّد) في نفس اللحظة — بلا انتظار ضغط «طابق وأنشئ». (قبلُ كان الوسم يحدّث العدّادات فقط فتبقى ملفات التنزيل قديمة — سُدَّت الفجوة.) `unwaitRow` كذلك يستدعي `run(true)` (يخرج من التصفير/الإخفاء، ويُعاد نشره إن عاد بكمية عبر المطابقة). **ملاحظة**: الأداة تولّد الملفات؛ التغيير الفعلي في المتجر يحدث عند **استيراد الملفين في زد** (التصفير للكمية ＋ الأسعار للنشر). (⏳ داخل لوحة التحليل الذكي `batchExclude` لا يعيد المعالجة فوراً — يبقى في اللوحة وتُعاد المعالجة عند التشغيل التالي.)
- **لا يوجد تصدير تصفير منفصل — المسار الفعلي داخل الملفين الرئيسيين**: الصنف الموسوم ⏳ «غير متوفر حالياً» يدخل **«ملف الكميات»** بكمية 0 (تُضاف صفوفه إلى `qtyRows` في فرع الانتظار) ويُخفى في **«ملف الأسعار»** بـ`published=No` عبر الإخفاء الموحّد. و`stripNoChangeQty` يُسقط ما هو 0 أصلاً في زد فلا يُستورَد شيء بلا فائدة. الغائب 📭 يحكمه `opts.absent` (`keep` افتراضياً · `zero` يضيفه بكمية 0 في نفس ملف الكميات). أي رمز قديم لتصدير تصفير مستقل (`zeroCandidates`/`zeroTableHTML`/`downloadZeroAbsent`/`zeroSelected` وتوابعها) **حُذف بالكامل** — لا تُعِد إحياءه إلا بزرّ ومسار واجهة صريحين.
- **(أُزيل عمود «🚫 إلغاء النشر» اليدوي — الدفعة ج البند ١):** كان زرّه في `cardHtml` المحذوف فبقي `toggleUnpublish` بلا مُنادٍ و`unpublishSet` فارغاً دائماً (مسار ميت). دُمج في «غير متوفر» (⏳ waiting) الذي يوفّر نفس الأثر عبر الإخفاء الموحّد: **كمية 0 ＋ `published=No` في الملفّين**. حُذفت `unpublishSet`/`toggleUnpublish` وتمريرتها في `run` و`lastUnpublish` وشارة/فلتر المستكشف وCSS؛ و`verifyPrices` صار يمرّر `autoUnpub` وحده.
- زرّا تحميل منفصلان مع عدّاد لكل ملف وسطر تعليمات الاستيراد أسفله؛ سطر التأكيد الأخضر يفصّل عددَي الكميات والأسعار **وعدد إعادة النشر** (`... · إعادة نشر: X صنف`).
- شارة «🔆 سيُعاد نشره» (`.pub-tag`) في جدول «تم تحديثه» على الأصناف التي ستتحوّل `published` من No إلى Yes.
- زر «🧪 جرّب» بجانب كل صنف محدّث (`downloadOne`): ينزّل ملف اختبار من صف واحد لتجربة الاستيراد في زد — ملفاً لكل نوع تغيّر (الكميات و/أو الأسعار)، فإن تغيّر الاثنان نزل ملفان.
- زر «⬇ نزّل المحدّد للتجربة (كميات ＋ أسعار)» (`downloadSelected`) في جدول «تم تحديثه»: ينزّل للأصناف المحددة ملفَّين معاً — `test_quantities.xlsx` (نموذج الكميات) و`test_prices.xlsx` (بنية الأسعار الكاملة بالأسعار النهائية شاملة الزيادات وتعديلات `published`). يمرّان بصمامَي `verifyQuantities`/`verifyPrices`. إن لم يكن في المحدد تغيّر سعر/نشر: يُنزَّل ملف الكميات فقط مع toast تنبيهي (`showToast`). التحديد `testSelected` in-memory للجلسة.
- زر «⚙ زيادة» على صفوف المتغيّرات (sku فيه نقطة) في جدول المحدّثة (`openOffset`): يفتح لوحة لإدخال زيادة المتغير عن سعر المستودع، مع زر «احسب من سعر زد الحالي» (يقترح سعر زد − سعر المستودع). عند وجود زيادة تظهر شارة بنفسجية «＋X إضافة» وتفصيل «أساس ＋ زيادة = نهائي».
- زر «✕ شيلها» (`excludeUpdated`) على كل صنف محدّث — **استبعاد مؤقت للجلسة فقط** (في الذاكرة، لا يُحفظ في localStorage ولا الريبو):
  - الصنف **يبقى في قائمة «تم تحديثه» في مكانه** بمظهر باهت (`row-excluded`) مع شارة «⏸ مستبعد من هذه الجلسة» وزر «↩ استعادة» (`restoreExcluded`) — **تصنيفه لا يتغيّر** وعدّاد بطاقة «تم تحديثه» يبقى كما هو.
  - يُستثنى فقط من **ملفي التنزيل** (الكميات والأسعار) وتنقص أرقام أزرارهما وسطر التأكيد الأخضر لحظياً.
  - يُلغى تحديد صندوق «نزّل المحدّد للتجربة» فوراً إن كان محدَّداً، ويُخفى صندوق التحديد لصفّه.
  - عند وجود مستبعدين يظهر شريط فوق الجدول: «⏸ مستبعد من هذه الجلسة: X — استعادة الكل» (`restoreAllExcluded`).
  - **يُصفَّر تلقائياً** عند إعادة تحميل الصفحة أو أي مطابقة جديدة (`run` بدون `reprocess`). الاستبعادات تُخزَّن في `excludedSet` (متغير جلسة in-memory).
  - الربط اليدوي لصنف مستبعَد يُلغي استبعاده تلقائياً (قرار صريح).

## فلتر جدول «تم تحديثه» — شرائح تغيير (عرض محض)
شرائح فوق الجدول (`updatedTableHTML`، `.upd-filter`/`.upd-chip`): **الكل · الكمية · السعر · النشر · الاثنان**، بعدد حيّ في كلٍّ. الحالة `updChgFilter` (all/qty/price/pub/both).
- **الدلالة (أعلام مستقلّة، التداخل مقصود — لا مجموع):** الكمية = `qtyChanged` · **السعر = `priceChanged` فقط** (تغيّر السعر) · **النشر = `pubChanged`** (‏`republish` No→Yes أو `autoUnpub` Yes→No — تغيّر حالة النشر) · الاثنان = `qtyChanged && priceChanged` (كمية∩سعر، كما اتُّفق — أكثر ما يُراجَع). **فُصل النشر عن السعر** (كان `priceChanged||republish` فيخلط تغيّر السعر بتغيّر النشر: «السعر 400» بينما ملف الأسعار 394، الستّة نشرٌ فقط). صنف بالاثنين يظهر في الشريحتين. **لا شريحة لتقاطعات أخرى** (كمية∩نشر…) — أربعة أنواع تكفي.
- **بيانات مخزَّنة لا محسوبة:** `qtyChanged`/`priceChanged`/`pubChanged` تُخزَّن في صفّ `updatedList` وقت `run()` — الفلتر **عرض محض** لا يمسّ `qtyRows`/`priceRows` ولا أي عدّاد (G-THROUGH يبقى أخضر).
- **العرض دالّةُ الشريحة ＋ البحث معاً:** `filterUnmatched` يقرأ `data-q`/`data-p`/`data-pub` (أعلام مستقلّة) و`data-search`، ويعرض الصفّ عند تحقّق **الاثنين** — لا يلغي أحدهما الآخر. `setUpdFilter(v)` عرضٌ محض.
- **الصفر يُعطَّل لا يُخفى** (`disabled`). **يعود «الكل»** عند كل مطابقة جديدة (‏`!reprocess`).
- **إبراز الخلية المتغيّرة:** `.chg-hi` على خلية الكمية عند `qtyChanged` وخلية سعر المستودع عند `priceChanged || pubChanged` (سعرٌ أو نشر).
- **«تحتاج مراجعة»/«كاملة» ليست شرائح:** `reviewReasons` (اسم مختلف · فرق سعري >50% · كمية=0) **طبقة تنبيه متعامدة** على تغيّر الكمية/السعر، تبقى تعليقاً في العنوان لا شريحة (جعلها شريحة يكسر الشمول/الاستبعاد).
- حارس `tests/perf/upd-filter.mjs` (G-FILT، بأسنان): العدّادات الخمسة بالقيمة · **«السعر» تعرض تغيّر السعر فقط لا النشر** (صفّ نشر-فقط لا يظهر في «السعر») · عرض محض (‏`qtyRows`/`priceRows` ثابتان) · فلتر×بحث يتقاطعان · الصفر معطّل؛ `--broken` يعيد خلط النشر بالسعر (`priceChanged||pubChanged`) ⇒ «السعر»=4≠2 ⇒ يرسب.

## زيادة سعر المتغيرات — `price-offsets.json`
- لمعالجة المتغيرات ذات الإضافات المدفوعة غير الموجودة في المستودع (مثل كرسي ＋ جلسة).
- البنية: `{ "skuN": قيمة_الزيادة }` (المفاتيح مُطبَّعة بـ`normCode`)، مثال: `{ "80151.1": 180 }`.
- يُحمّل بـ`fetch` عند الفتح ويُدمج مع localStorage (`OFF_KEY`)، ويُصدَّر ويُحفظ في الريبو ضمن منظومة `mapping.json`/`ignored.json`.
- المتغيرات بلا زيادة: الزيادة = 0 ⇒ لا تغيير في السلوك (ألوان/أجزاء عادية).

## قاعدة البيانات السحابية Supabase — المرحلة 1: الروابط (mappings) هي المصدر الأساسي
تُخزَّن **الروابط فقط** في Supabase هذه المرحلة؛ بقية ملفات JSON (`aliases`/`waiting`/`price-offsets`/`matched-history`) تبقى كما هي بلا تغيير، و`mapping.json` ＋ زر ☁ الحفظ في الريبو يبقيان **نسخة احتياطية** (القاعدة هي المصدر الأساسي الجديد).
- **العميل**: `supabase-js v2` عبر CDN (jsdelivr). `SUPA_URL` ＋ `SUPA_KEY` (مفتاح **publishable** الجديد `sb_publishable_…`، آمن للنشر مع RLS — **لا secret key إطلاقاً**). `initSupabase()` يُنشئ العميل ويستمع `onAuthStateChange`.
- **المصادقة**: بريد/كلمة مرور عبر Supabase Auth (`signInWithPassword`)، شاشة `#loginOverlay` (سطر تعريفي «أداة مزامنة المخزون — ASJ») تظهر عند غياب الجلسة، الجلسة تُحفظ تلقائياً (`persistSession`)، زر «🚪 خروج» بالترويسة. مؤشر `#dbBadge`: 🟢 متصل بالقاعدة (عدد الروابط) / 🔴 غير متصل. (أُزيل زر «متابعة بلا اتصال».)
- **نسيت كلمة المرور** (`forgotPassword`/`setNewPassword`): رابط «نسيت كلمة المرور؟» أسفل زر الدخول يُرسل `resetPasswordForEmail(email, {redirectTo: صفحة الأداة})`. العودة من الرابط تُطلق حدث `PASSWORD_RECOVERY` في `onAuthStateChange` ⇒ يُعرض نموذج `#recoverForm` («تعيين كلمة مرور جديدة») بدل `#loginForm`، وحفظه يستدعي `updateUser({password})` ثم يدخل الأداة. **يتطلّب إعداداً يدوياً مرة واحدة في Supabase → Authentication → URL Configuration**: إضافة رابط GitHub Pages إلى **Site URL** و**Redirect URLs**، وإلا فشل توجيه رابط البريد.
- **جدول `public.mappings`** (SQL في `supabase_phase1.sql`، يُنفَّذ يدوياً مرة): `id` (identity PK) · `zid_sku` (text unique) · `warehouse_code` (text) · `source` (manual/auto-confirmed/auto-approved/imported) · `note` · `created_by` (uuid=auth.uid()) · `created_at`/`updated_at`. ＋ فهرس على `warehouse_code` ＋ trigger لـ`updated_at` ＋ **RLS** (كل العمليات للدور `authenticated` فقط، لا شيء لـ`anon`) ＋ grants صريحة.
- **الوحدة `db.mappings`**: `getAll()` · `upsert(sku,code,source)` · `bulkUpsert(rows)` · `remove(sku)` · `bulkRemove(skus)`. الأغلفة `dbSetMapping`/`dbSetMappings`/`dbDelMapping`: **القاعدة أولاً، وعند النجاح فقط** يُحدَّث المحلي (`manualMap` ＋ localStorage). `requireOnline()` يمنع أي كتابة في وضع عدم الاتصال (منع تشعّب البيانات).
- **جدول `public.waiting_items`** (SQL في `supabase_waiting.sql`، يُنفَّذ يدوياً مرة): `zid_sku` (text unique) · `name` · `marked_at` · `created_by` · `created_at`/`updated_at` ＋ trigger ＋ نفس RLS/grants كـmappings. الوحدة `db.waiting`: `getAll()`/`upsert(sku,name)`/`remove(sku)`/`bulkRemove(skus)`. أغلفة **الكتابة المزدوجة** `markWaiting`/`unmarkWaiting` (المحلي فوراً ＋ القاعدة بأفضل جهد — لا تعطّل عند فشل الاتصال، فـ`waiting.json` احتياطي بالتوازي في المرحلة الانتقالية)، و`loadWaitingFromDB` (اتحاد مع المحلي عند الدخول). التعافي التلقائي في `run` يحذف العائدين من القاعدة (`db.waiting.bulkRemove`).
- **كل مواضع كتابة الروابط تمر عبر الوحدة**: الربط اليدوي (`addMapping` → manual) · اعتماد اقتراح (`bindRow` → auto-approved) · ربط المؤكدة (`bindAll90` → auto-confirmed, bulk) · الربط الدفعي 🟢/🟡 (`batchApply` → **bulkUpsert واحد**) · الحذف (`delMap`) · التراجع الدفعي (`undoLastBatch` → bulkRemove ＋ bulkUpsert).
- **الإقلاع**: بعد `loadConfig` (المحلي) يُستدعى `initSupabase`؛ عند الجلسة `loadMappingsFromDB()` يجلب الروابط ويجعلها مصدر الحقيقة (يستبدل `manualMap`). **الترحيل**: إن كانت القاعدة فارغة والمحلي فيه روابط ⇒ شريط `#migrateBar` ＋ زر `doMigration()` (`bulkUpsert` للجميع بـ`source='imported'`). **عدم الاتصال**: فشل الوصول ⇒ `dbOnline=false`، قراءة محلية، منع الكتابة، شارة 🔴.
- **خريطة المراحل القادمة**: (2) نقل بقية ملفات JSON للقاعدة (`price-offsets`/`matched-history`/`aliases` — و`waiting` **تمّ**؛ و`ignored` **حُذف**) · **(3) منتجات زد في القاعدة — تمّ (القسم التالي)** · تاريخ المطابقة الكامل والتحليلات · إدارة موظفين متعددين (التصميم يراعيها عبر `created_by`/RLS).

## قاعدة البيانات السحابية Supabase — المرحلة 3: منتجات زد (مرآة لزد)
بدل رفع تصدير زد كل دورة، تُخزَّن منتجاته في القاعدة وتُحدَّث عند الحاجة. المطابقة تقرأ من القاعدة، وملف المخزن يبقى المدخل المتكرر الوحيد. القاعدة **مرآة**: زد يتغيّر خارجنا بالمبيعات، لذا التقادم حرج.
- **الجدولان** (SQL في `supabase_zid_products.sql`، يُنفَّذ يدوياً مرة، نفس نمط RLS/grants/triggers السابق):
  - `public.zid_products`: `sku` (text unique) · `barcode`/`name_ar`/`name_en` · `price`/`sale_price`/`quantity` (numeric) · `published`/`has_variants`/`parent_sku` (text) · **`raw` jsonb** (الصف الكامل من التصدير — الأعمدة الـ76) · **`row_order` int** (ترتيب الصف في الملف الأصلي) · `imported_at` ＋ `created_by/at/updated_at` ＋ فهارس على `barcode` و`parent_sku`. الأعمدة المفهرسة «لحاجة مستقبلية»؛ **إعادة بناء `stData` تعتمد على `raw` ＋ `row_order` فقط** (أمينة لترتيب الملف ⇒ نتائج مطابقة مطابقة تماماً).
  - `public.zid_sync_meta` (صف واحد `id=1`): **`header` jsonb** (رأس الأعمدة بالترتيب — يُخزَّن مرة، لإعادة بناء الملف على أي جهاز) · `product_count` · `synced_at`.
- **الوحدة `db.zidProducts`**: `getAllSkus()`/`getAll()` (**كلاهما paginated بـ`.range()` — حدّ سوپابيس 1000 صف/طلب؛ `getAll` يرتّب بـ`row_order`**) · `bulkUpsert(rows,onProgress)` (**دفعات 500 صف** — 2300 صف ليست 2300 طلباً) · `removeSkus(skus)` (دفعات 200 عبر `.in`) · `getMeta()`/`setMeta(header,count)`.
- **التدفق المزدوج في `onStore`/`useZidFromDB`** (يوحّدان المصدرين في `stData` واحد قبل المطابقة فيعمل كل المنطق — الآباء/التحليل/⏳🚫/الصمامات — بلا تغيير أياً كان المصدر):
  - **رفع ملف** (`onStore`): يبني `stData` ثم — إن متصلاً — `syncZidToDB()` = **مزامنة كاملة** (الملف الجديد هو الحقيقة): `bulkUpsert` للكل (الجديد يُدرَج فيلتقط منتجات زد المضافة حديثاً، والموجود يُحدَّث بكل حقوله ＋ `raw` ＋ `imported_at`) ＋ **حذف الأصناف الغائبة عن الملف الجديد** (`removeSkus`) ＋ ملخّص «جديد X · محدَّث Y · حُذف Z» في `#zidSyncMsg` ＋ تحديث الميتا. عند عدم الاتصال ⇒ **fallback**: الملف مباشرةً بلا مزامنة (توافق تام مع ما قبل المرحلة 3).
  - **من القاعدة** (`useZidFromDB`): يجلب الميتا (الرأس) ＋ كل المنتجات، ويعيد بناء `stData = [header, ...raw بترتيب row_order]` بلا رفع ملف.
  - **`buildZidDbRows()`**: يستخرج الأعمدة المهمة ＋ يكشف عمود مرجع الأب best-effort (`parent_sku`) ＋ يخزّن `raw`/`row_order`. **صفوف بلا `sku` تُتجاوز** (قيد unique) — لا تؤثر على المطابقة (`run` يتجاوزها أصلاً)، لكن قد يختلف عدّاد «إجمالي الأسطر» بمقدارها بين وضعَي القاعدة/الملف.
- **ضمانات التقادم** (`zidSource`/`zidSyncedAt`/`ZID_STALE_MS=24س`، `renderZidFreshness`): طابع «بيانات زد: منذ X (القاعدة/الملف)» بارز في رأس النتيجة `#zidAgeHead` دائماً ＋ في لوحة القاعدة (`#zidDbAge`، شارة `.stale` صفراء عند التجاوز). عند **>24 ساعة**: التحذير في **سطر الملخّص الأخضر المنقول** (`.vm-stale` على `#invSummaryLine` بالمخزون). (أُزيل البانر الأصفر المستقلّ `#zidStaleWarn` وبطاقة «ملخّص الملفّين» — كانا مكرّرين.) الملف الطازج (`onStore`) = `zidSyncedAt=now` ⇒ غير قديم؛ القاعدة تأخذ `synced_at` من الميتا.
- **الإقلاع/الأجهزة**: `loadZidMeta()` (يُستدعى في `loadMappingsFromDB` بعد `loadWaitingFromDB`) يجلب الميتا فقط (خفيف) ويعرض اللوحة؛ جلب كل المنتجات كسول (عند «استخدمها للمطابقة»). المطابقة **تنطلق تلقائياً متى توفّر المصدران** (`launchMatchIfReady` في `onStore`/`useZidFromDB`/`autoAdopt`). أي جهاز بنفس الحساب يرى نفس بيانات زد فوراً من القاعدة.

## سجل النشاط Supabase — الأساس المستقبلي للإشعارات
- **الجدول `public.activity_log`** (SQL في `supabase_activity_log.sql`، يُنفَّذ يدوياً مرة، نفس نمط RLS/grants): `event_type` text (`link_added`/`link_removed`/`product_synced`/`sync_full`/`waiting_added`/`waiting_removed`/`unpublish_added`/`file_generated`) · `zid_sku` · **`details` jsonb** (المرشح/الأعداد/المصدر…) · `created_by` · `created_at` ＋ فهرسان على `created_at desc` و`event_type`. سجل مشترك (كل مصادَق يقرأ الكل).
- **الوحدة `db.activity`**: `insert(event_type,zid_sku,details)` · `getRecent(limit=50)` · `getByType(type,limit)`. الغلاف **`logActivity(event_type,zid_sku,details)` غير حاجب (fire-and-forget)**: يتجاهل بلا اتصال ولا يعطّل العملية عند الفشل (`.catch`).
- **مواضع الكتابة الموسومة**: `dbSetMapping`/`dbSetMappings` (link_added، مع `count` للدفعي) · `dbDelMapping` (link_removed) · `markWaiting`/`unmarkWaiting` · `syncZidToDB` (sync_full بأعداد جديد/محدَّث/محذوف في `details`) · نقر تنزيل الملفات في `wireDl` (file_generated بنوع كميات/أسعار). أي write مستقبلي يُتبع بـ`logActivity`.
- **العرض**: زر 🔔 في الترويسة (`.notif-menu`) يفتح لوحة «آخر النشاطات» (أحدث 50، `loadActivityInto("activityList")`): أيقونة الحدث (`ACT_META`) ＋ وصف عربي (`actDescribe`) ＋ SKU ＋ وقت نسبي (`relTime`). صفحة الحساب فيها قسم موسّع `#activityFull` مع فلتر بالنوع `#actFilter` (`renderActivityFull`، أحدث 200) يُحدَّث عند فتح الصفحة (`goPage('account')`).
- **لا إشعارات بريدية بعد**: الجدول مُهيّأ لتقرأ منه Edge Function لاحقاً وترسل ملخّصاً.

## قاعدة البيانات السحابية Supabase — المرحلة 4: المخزن بجدولين (مستودع/فرع)
بدل رفع ملفَّي المخزن كل دورة، يُخزَّن كلٌّ في جدوله ويُحدَّث مستقلاً. المطابقة **توحّدهما في الذاكرة** (نفس `mergeInventory` حرفياً) فلا يتغيّر منطقها.
- **الجداول** (SQL في `supabase_inventory.sql`، يُنفَّذ **بعد `supabase_user_roles.sql`** لاعتماده على `get_my_role()`): `warehouse_items` و`branch_items` (نفس البنية: `code` unique · `name`/`barcode` · `qty`/`price_incl`/`price_excl` numeric · `row_order` · `imported_at` ＋ فهرس barcode) · `inventory_sync_meta` (صف واحد id=1: `wh_*` و`branch_*` منفصلة — يُحدَّث كلٌّ على حدة). RLS: القراءة للموثّقين، الكتابة لـowner+admin.
- **الوحدة `db.inventory`** (تأخذ اسم الجدول): `getAll`/`getAllCodes` (paginated) · `bulkUpsert` (دفعات 500، onConflict `code`) · `removeCodes` (دفعات 200) · `getMeta`/`setMeta(patch)` (upsert جزئي على id=1 فلا يمسّ حقول الجدول الآخر).
- **التجميع الحاسم**: ملفات الفرع بها أكواد مكرّرة (3028 سطر → 2165 كود)، و`code` unique. لذا `aggregateInv(list, mode)` يجمع الكمية بالكود قبل التخزين — **مستودع=آخر غير فارغ للنص، فرع=أول غير فارغ** (يطابق سلوك `mergeInventory` داخل الملف) فإعادة الدمج من القاعدة تعطي **نفس النتائج تماماً** (مُختبَر). `mergeInventory` أُضيف له `whQty`/`brQty` (تفصيل بالمصدر للاستعلام — لا يمسّ المطابقة التي تستخدم `qty`).
- **التدفق**: `onMergeWh`/`onMergeBranch` (في صفحة التحديث): يقرأ ← `aggregateInv` ← `syncInventoryToDB(kind,...)` (upsert + حذف الغائب + `setMeta` + سجل `warehouse_sync_full`/`branch_sync_full`) ← `renderInvCards`/`renderReadiness` ← `autoAdopt`. الرئيسية `runFromDB`: إن لم يكن `mergeWh` محمّلاً و`invDbAvailable()` ⇒ `loadInventoryFromDB` (يعيد بناء `mergeWh`/`mergeBranches` من الجدولين) ثم `autoAdopt` (يبني `whRows`/`lastMerge`/الداشبورد ثم `launchMatchIfReady` ⇒ يعتمد زد ويطابق).
- **الاستعلام**: `runWhQuery` يحمّل الجدولين كسولاً (`invLoading`) ويعرض الكمية مفصّلة: «مستودع: X» / «فرع: Y» / «إجمالي» حسب `srcKind`. **fallback**: الرفع المباشر يعمل offline (بلا مزامنة) عبر نفس `mergeWh`/`mergeBranches`.
## الفروع المتعدّدة — الدفعة ١أ  ⚠️ SQL يدوي (`supabase_branches.sql`)
كل فرع فعليّ **كيان مستقلّ** (`uuid` + اسم للعرض + ملفه)، لا ملفٌّ واحد مدموج. حلّت محلّ الدفعة الصفرية القديمة (خانة فرع واحدة كانت تحذف أكواد الفرع الآخر).
- **المخطّط** (`supabase_branches.sql`، يُنفَّذ **بعد** `supabase_inventory.sql`، مرّة، في معاملة `begin/commit`): جدول `public.branches` (`id` uuid PK · `name` · `item_count` · `file_name` · `header` jsonb · `synced_at` ＋ created_by/at/updated_at ＋ RLS/grants بنمط الجداول السابقة). و`branch_items`: عمود `branch_id` (not null، ترحيل الحالي إلى فرع افتراضي «العزيزية») · **المفتاح الفريد من `code` إلى `(branch_id, code)`** (نفس الكود في عدّة فروع) · **FK `on delete cascade`** (حذف فرع يمسح صفوفه — لا يتامى). نُفِّذ حيّاً: 3296 صفّاً محفوظة، المفتاح المركّب قائم، القديم (`branch_items_code_key`) مُسقَط.
- **الوحدة `db.branches`**: `getAll` (مرتّبة `created_at` تصاعدياً — أقدميّة) · `create(id,name)` · `rename` · `remove(id)` (يُفعّل الـcascade) · `setMeta(id,patch)`. و`db.inventory` **حُجِّمت بـ`branch_id`**: `getAll(table)` يجلب `branch_id` للفروع · `getAllCodes(table, branchId)` و`removeCodes(table, codes, branchId)` يُرشِّحان بـ`.eq("branch_id",…)` · `bulkUpsert` onConflict = `"branch_id,code"` للفروع.
- **العزل (شرط قبول لا تفاوض فيه):** `syncInventoryToDB(kind, aggList, header, fileName, branchId)` — رفع فرع B يحذف الناقص **داخل branch_id الخاصّ به فقط** (`getAllCodes`/`removeCodes` محصوران) ولا يمسّ صفّاً من فرع A. الحذف عاد للفرع لكن **محصوراً** (لا الدفعة الصفرية «لا حذف»). سجلّ الفرع يُحدَّث (`db.branches.setMeta`) ＋ **مرآة إجماليّة** في `invMeta.branch_*` (مجموع `item_count` كل الفروع) ليبقى العرض القديم (`renderInvCards`/`renderReadiness` وقرّاء `branch_count` الخمسة) عاملاً حتى تحويلهم في ١ب.
- **الاتحاد الحقيقي:** `loadInventoryFromDB` يجمع صفوف الفروع بـ`branch_id` إلى **قوائم منفصلة** (بترتيب `invBranches` = أقدميّة) ⇒ `mergeInventory(mergeWh, mergeBranches)` **يجمع الكميات** عبر المصفوفة (لا آخر-غالب)، والسعر = المستودع ثم **أقدم فرع**. `onMergeBranch` بعد المزامنة يعيد `loadInventoryFromDB` (كل الفروع). offline ⇒ `mergeBranches=[agg]` (الفرع المرفوع محليّاً فقط).
- **الواجهة (دفعة ٢ — بطاقات ديناميكية):** صفّ `.inv-cards` (‏`flex-wrap`، لفّ الأسطر لا تمرير أفقي) = بطاقة `#whCard` (المستودع) ＋ **بطاقة رفع لكلّ فرع** يبنيها `renderBranchCards()` من `invBranches` (‏`<label class="upload branch-card" data-branch="ID">` فيها الاسم · رفع مباشر · عددها/عمرها/ملفها من `invBranches[i]` · سطر تقدّم مزامنة `#brSync-ID` معرّفه مركّب) ＋ بطاقة `.add-branch-card` «＋ فرع جديد» (`addBranch`). **الرفع مباشر من بطاقة الفرع** — `onMergeBranch` يقرأ `branchId` من `inp.closest(".branch-card").dataset.branch` (لا قائمة منسدلة)، والحالة/المزامنة تُكتب على عناصر البطاقة (‏`card.querySelector`) لا معرّفات عامّة. **البطاقات ديناميكية**: فرع يُضاف في القاعدة يظهر تلقائياً عند `renderBranchCards` (‏`loadInventoryMeta`/`goPage('db')`/`addBranch`). **لا زرّ حذف فرع من الواجهة** (قرار المستخدم). سطر تفكيك في شريط الدمج (`renderMergeBar`): «أصناف الفروع Σ = فرع₁ n₁ ＋ فرع₂ n₂» شرائح `.mr-brchip` ملتفّة. حُذفت `#brSelect`/`#brStatus`/`#brCard`/`#brMetaLine`/`#brSyncMsg`/`renderBranchSelect`/`.br-col`/`.br-picker`.
- **الحراس (كلٌّ أُثبت رسوبه على المعطوب):** **G1 عزل** (`tests/perf/inv-branch-isolation.mjs`): رفع فرع B (30→10) لا يمسّ فرع A (50→50)؛ `--broken` يزيل تحجيم `getAllCodes`/`removeCodes` ⇒ A يُمحى. **G-THROUGH نفاذ** (`tests/perf/inv-branch-through.mjs`، «أهمّ حارس»): المخزن الموحّد نفسه ⇒ المخرج نفسه — كمية مشتركة مجموعة (6+4=10) · سعر أقدم فرع (100 لا 200) · **فرع واحد (10) ≡ فرعان (6+4)**؛ `--broken` يعكس ترتيب الفروع ⇒ السعر من الأحدث. **G5 مخطّط** (`tests/inv-branch-schema.mjs`، ساكن): يقفل FK cascade · مفتاح مركّب · not null · معاملة واحدة · حذف الفرع بالمعرّف؛ `--broken` يزيل `on delete cascade`.
- **الدفعة ١ب (تمّت) — بوّابة الانكماش لكلّ فرع ＋ تدقيق قرّاء `branch_count`:** `invShrinkCheck(kind, newCount, branchId)` صار يقارن الفرع بمرجع **الفرع المختار نفسه** (`item_count` من `invBranches.find(id===branchId)`) لا بالإجمالي `invMeta.branch_count` — وإلا فرفعُ فرعٍ صغير (الخضرة 50) يُقارَن بمجموع الفروع (1050) فيبدو انكماشاً هائلاً ⇒ **إنذار كاذب يُلغي رفعاً سليماً**. فرع بلا مرجع (`item_count=0`، جديد) ⇒ لا بوّابة. `onMergeBranch` يمرّر `branchId`. **تدقيق القرّاء الخمسة (تحويلٌ دلاليّ لا ميكانيكيّ):** (١) `invShrinkCheck` ⇒ **per-branch** (الإصلاح) · (٢) `invMergeIncomplete` و(٣) `showInvIncomplete` ⇒ **يبقيان إجماليَّين** (‏`branch_count>0` = «هل في القاعدة أيّ مخزون فرع غائب عن الموحّد؟» — الإجماليّ هو الصحيح دلالياً؛ per-branch يكسره) · (٤) `renderInvCards` و(٥) `renderReadiness` ⇒ **صارا واعيَين بالعدد** (يُظهران «الفروع (K)» حين K>1 بدل «الفرع» المفردة المضلّلة). حارس `tests/perf/inv-branch-gate.mjs` (G-GATE، بأسنان): الصغير السليم يمرّ · الانكماش الحقيقي يوقف · الجديد بلا بوّابة؛ `--broken` يعيد الأساس الإجماليّ ⇒ إنذار كاذب ⇒ يرسب. (`valves.mjs` حُدِّثت تجهيزته لتمرّر `branchId` — كانت تختبر الأساس القديم.)
- **الدفعة ٢ (تمّت جزئياً):** بطاقات فروع كاملة **نُفّذت** (أعلاه). **حذف فرع من الواجهة: مرفوض صراحةً بقرار المستخدم — لا يُضاف** (‏`db.branches.remove` تبقى للاستعمال البرمجي فقط، بلا زرّ). **الفهم:** الفروع **طبقة تحت الموحّد فقط** — `run()`/`resolveWhCode` يقرآن `whRows`/`lastMerge` (المبنيّين عبر `mergeInventory` في `autoAdopt`)، لا `mergeBranches` مباشرةً ⇒ صفر تغيير في تدفّق المطابقة/الرباعية/الملفّين.

### سجلّ الرفعة واقعة لا تحليل — `recordUpload`/`recordMovements` (إصلاح «آخر رفعة» العالقة)
**العَرَض:** كتلة «آخر رفعة» تُظهر موقعاً عالقاً على رفعة قديمة رغم إعادة رفعه اليوم — إن كان الملف مطابقاً لِما في القاعدة (لا كمية تغيّرت). **الجذر:** كانت `recordMovements` تُدرج صفّ `sales_uploads` **بعد** `if (!movs.length) return;` — فرفعةٌ بلا حركات لا تكتب طابعاً، و«آخر رفعة» (تقرأ `sales_uploads.captured_at`) و`uploadedInPeriod` (تقرأ `ups` كذلك) يبقيان قديمين. **«هل رُفع اليوم؟» سؤالٌ عن الفعل، والكود يجيب بالنتيجة.**
- **الفصل:** `analyzeMovements(existMap, rows, location, captured_at, prevSyncedAt)` حسابٌ بحت (‏`computeMovements` ＋ حوار الشذوذ ＋ التوست) يُرجع `{movs, note, suspect, skip}` بلا كتابة · **`recordUpload(...)` = واقعة** (يُدرج صفّ `sales_uploads` **دائماً** عند نجاح الرفع، `await`؛ **يُعيد `false` عند الفشل — 🚫 لا throw ولا رسالة فشل عامّة** لئلّا يوحي بضياع البيانات المحفوظة فعلاً) · **`recordMovements(uploadId, movs)` = تحليل** (يُدرج `sales_movements` إن وُجدت، fire-and-forget ＋ `console.warn`). موضع الاستدعاء في `syncInventoryToDB`: `uploadOk = await recordUpload(...)` **بلا شرط** ثم `if (uploadOk && !anz.skip) recordMovements(...)` (المطابق ⇒ `movs=[]` ⇒ رأس بلا حركات · فشل الرأس ⇒ لا حركات يتيمة). النتيجة تحمل `{identical, uploadOk, prevSyncedAt}`. **الرأس بلا حركات آمن** (baselineIds من الحركات ⇒ لا يُصنّفه تأسيساً · uploadedInPeriod يعدّه مرفوعاً — المطلوب · التجميع من الحركات ⇒ لا أثر).
- **تنبيهات ملخّص الرفع `syncExtras(r, locName)`** (تُضاف لرسالة النجاح في المستودعات الثلاثة، `.zid-sync-warn` أمبريّ هادئ 🚫 لا يمنع الرفع): **بند ١ «ملف مطابق»** حين `r.identical` — «⚠ <الموقع>: هذا الملف مطابق تماماً للرفعة السابقة (<العمر من `prevSyncedAt`>) — لم تتغيّر أي كمية. تأكّد أنك صدّرت ملفاً جديداً من الكاشير» (الدليل: العزيزية رُفعت بصفر حركة والأداة سكتت ⇒ ضاعت ٩ ساعات). **بند ٢ فصل النجاح عن فشل التسجيل** حين `r.uploadOk===false` — «حُفظت بيانات <الموقع> في القاعدة ✓ · لكن تعذّر تسجيل الرفعة في السجلّ ⚠ — «آخر رفعة» قد تظهر قديمة. أعد الرفع» (🚫 لا إيحاء بفشل حفظ البيانات — الأصناف كُتبت قبل تسجيل الرفعة).
- حارس `tests/perf/upload-record.mjs` (G-UPLOAD-RECORD، بأسنان): رفعة مطابقة ⇒ `sales_uploads=1`/`sales_movements=0` · الرأس غير مشروط · فشل الرأس ⇒ `false` بلا throw/توست عامّ · تنبيه «ملف مطابق» باسم＋عمر · فشل التسجيل يفصل «حُفظت البيانات»؛ `--broken` يقيّد الرأس بـ`movs.length` ⇒ يرسب.

### كشف «لم تُرفع اليوم» — `invStaleToday` **تنبيه إعلاميّ لا قفل** (تراجع مقصود عن قفل 60a483d بأمر بدر) ＋ مصدر موحّد
🚨 **المصدر موحّد الآن على `sales_uploads`** (نفس كتلة «آخر رفعة») — `invStaleToday(latest)` يأخذ خريطة `salesUploadsLatest(ups)` (‏`location→ms` من `sales_uploads`) بدل `invMeta.wh_synced_at`/`invBranches.synced_at` (تفادي مصدرين متناقضين). `renderStaleNote()` (async، fire-and-forget من `run`) يجلب `ups` ويعرض `#invStaleNote`. الحراج مستثنى (لا يُذكر في `chk`).
`invMergeIncomplete()` (الأصليّ) يبقى كما هو: يوقف المطابقة والتنزيل فقط حين **لا فرع مُحمَّل إطلاقاً** في الجلسة (موحّد مبتور). **أمّا `invStaleToday(latest)`** فيكشف — إعلاميّاً فقط — المواقعَ التي لم تُرفع اليوم من خريطة **`salesUploadsLatest(ups)`** (‏`location→ms` من `sales_uploads` — نفس مصدر كتلة «آخر رفعة»)، بتوقيت الرياض (`riyadhDay`): المستودع ＋ فروع زد. **🚫 فرعا الحراج (`SALES_EXTRA_LOCS`) خارج الحساب.** 🚫 **لا يقفل شيئاً:** شرط `wireDl` هو القديم (‏`ok && count>0 && dbOnline && !invMergeIncomplete() && !histIncomplete`) بلا `staleLocs`؛ أزرار التجربة بلا حجب. `renderStaleNote()` (async من `run`) يعرض `invStaleMsg` (تنبيه أمبريّ هادئ) في **`#invStaleNote`**: «⚠ لم تُرفع اليوم: <الأسماء> (آخر رفعة: <التاريخ>) — الملفان قد يكونان ناقصَين لهذه المواقع.» — 🚫 لا «أوقفتُ التنزيل». **السبب:** القفل عطّل تدفّق بدر اليوميّ بلا مبرّر؛ الكشف يبقى تنبيهاً لا مانعاً. حارس `tests/perf/inv-complete-today.mjs` (G-INV-COMPLETE، بأسنان): المصدر `sales_uploads` موحّد مع الكتلة · الكشف يبقى (الأسماء/التاريخ · الحراج مستثنى) · **التنزيل غير مقفول** · الرسالة إعلاميّة لا مانعة؛ `--broken` يُعيد القفل ⇒ يرسب. (خريطة فارغة ⇒ لا كشف — المشغّل السلوكيّ غير متأثّر.)

### admin: كتلة «بلا رفعة» من `sales_uploads` وحده (دفعة ٣ متمّمة)
`fillUploadStatus` لـadmin يستدعي `renderAdminMissingToday(ups)` — يعرض في `#salesMissBanner` **أسماء المواقع (المستودع ＋ كل ما يرفعه admin: فروع زد ＋ الحراج) التي لم تُرفع اليوم**، من `db.sales.uploads()` **وحده**. 🚫 صفر استعلام `sales_movements`/`sales_stock` (صفر صفّ مبيعات في استجابة admin) · 🚫 لا كتلة «المفقود» (‏`#salesBanner`/`#salesDisBox` تبقى مخفيّة). حارس `tests/perf/admin-noupload.mjs` (G-ADMIN-NOUPLOAD، بأسنان): الأسماء تظهر · صفر استعلام مبيعات · المفقود مخفيّ · المرفوع اليوم لا يظهر؛ `--broken` يُعيد إخفاء «بلا رفعة» ⇒ يرسب.

### المساعد الذكيّ: القيمة «بسعر البيع شامل الضريبة» (تمييز عن التكلفة)
بعد أن صارت البطاقة تعرض «قيمة المخزون (تكلفة)»، وحدة أرقام القيمة في المساعد (‏`mM`) صارت **«ر.س بسعر البيع شامل الضريبة»**، و`answerNote` لقيمة المخزون يذكر **«بسعر البيع شامل الضريبة (لا سعر التكلفة)»** — فلا يبدو رقما المساعد والشاشة خللاً وهما وحدتان مختلفتان بقصد. 🚫 `cost_price` لا يُرسَل إلى Gemini (تعديل نصّ فقط؛ الدالّة تختار أعمدة صريحة بلا التكلفة). أُعيد بناء `ai-assistant.bundle.ts`. (G-AI-METRIC-UNITS حُدّثت مرساته.)

### سلامة المدخل — «مطابقة طازجة على مدخل ناقص» (علّة سابقة لـ١أ، أُصلحت)
> **مطابقة طازجة على مدخل ناقص تنتج أرقاماً واثقة وخاطئة — لا مؤشّر يميّزها عن الصحيحة. سلامة المدخل تُفحص، لا تُفترض.**

**العَرَض:** رفع «ملف المستودع» في جلسة **لم تُرفَع فيها الفروع** أظهر «تم تحديثه ٥٥٢ · غائب ١١٥٤ · كميات ٨٢٤» بثقة تامّة؛ وتحديث الصفحة ثم «طابق وأنشئ» أعادها إلى «٠/٦١٠/٠» الصحيحة. **الجذر:** `onMergeWh` → `autoAdopt` → `mergeInventory(mergeWh, mergeBranches)` و`mergeBranches` **فارغة** لأنّ `loadInventoryFromDB` لا يُستدعى إلا في `onMergeBranch` أو `runFromDB` **بشرط `!mergeWh`** — فرفع المستودع وحده يبني الموحّد بلا فروع (العزيزية ٣٢٩٦ صفّاً خارجه) ⇒ كمياتٌ أقلّ من زد ⇒ فروق مصطنعة. **المطابقة تُعاد فعلاً** (‏`launchMatchIfReady`→`run`) لكن **على مدخل مبتور** — فالخلل ليس «لم تُعَد» بل «أُعيدت على ناقص».
- **علّة سابقة لـ١أ لا منها:** `git diff HEAD~1 HEAD` يُظهر أنّ `onMergeWh`/`autoAdopt` لم يتغيّرا في ١أ؛ التغيير الوحيد `+await loadInventoryFromDB()` في `onMergeBranch`. فالثغرة قديمة، و١أ **حسّنت رفع الفرع** (صار يعيد التحميل من القاعدة) فبقيت اللامماثلة في رفع المستودع.
- **الإصلاح (السبب لا العَرَض):** `onMergeWh` بعد المزامنة الناجحة يستدعي `await loadInventoryFromDB()` — نفس ما يفعله `onMergeBranch` — فيصير الموحّد = المستودع ＋ **كل** فروع القاعدة، ثم `run` يطابق على مدخل كامل.
- **الحارس البنيوي `invMergeIncomplete()`** (رخيص، مستقلّ عن الاتصال): `mergeWh && invMeta.branch_count>0 && mergeBranches.length===0`. يُفحَص **في أوّل `run()` قبل أي معالجة** ⇒ `showInvIncomplete()` يوقف المطابقة، يُظهر بانر «الموحّد ناقص» في `#verifyMsg`، ويعطّل زرّي التنزيل — **لا أرقام صامتة**. ＋ دفاع عميق في `wireDl` (‏`&& !invMergeIncomplete()`). يغطّي **الوضع دون اتصال** أيضاً (رفع مستودع بلا تحميل فروع ⇒ توقّف لا أرقام خاطئة)؛ الحدّ الوحيد: جلسة دون اتصال من البداية (‏`invMeta` فارغ) لا يمكن معرفتها — لكنها بلا معنى (الدخول يتطلّب اتصالاً).
- **الحارس `tests/perf/inv-stale-merge.mjs` (G-STALE، بأسنان):** ① رفع المستودع يحمّل الفروع (X=10 مجموعاً لا 6) · ② الموحّد المبتور يوقف `run` (بانر ＋ تنزيل معطّل ＋ `lastRunSummary` يبقى null). `--broken` يزيل إعادة التحميل ويُعطّل `invMergeIncomplete` ⇒ يرسب بخمسة فحوص.
- **المنع فعليّ لا تحذيريّ (مقيس):** ملفّا التنزيل الرئيسيان `#dlQty`/`#dlPrice` عنصرا `<a class="dl-card" download>` — والموحّد المبتور يمنعهما **بثلاث طبقات**: (١) `run` يُجهَض قبل بناء `qtyRows`/`priceRows` فلا ملفّ أصلاً · (٢) `showInvIncomplete`＋`wireDl` يزيلان `href` ويضيفان `.disabled` · (٣) CSS `.dl-card.disabled { pointer-events: none }` فالنقر لا يُسجَّل. أزرار **التجربة** (`downloadSelected`/`downloadOne`) تقرأ `lastQtyRows`/`lastPriceRows` من **آخر تشغيل مكتمل** (‏`run` يُجهَض قبل تحديثها على المبتور) فلا تُصدِر بيانات مبتورة، لكنها غير محروسة بـ`invMergeIncomplete` صراحةً (بند مفتوح بسيط إن لزم التشديد).
- **برهان التزامن الأسرع (درس):** **«تم تحديثه = 0» مع مدخل كامل = برهانُ تزامنِ المتجر مع المخزون** (كل صنف مطابَق كميته=كمية المخزون الصحيح) — أسرع فحص سلامة لدينا، وبه ثبت أنّ لا ضرر تاريخيّاً وقع (لو أفسد تنزيلٌ سابق المتجر لظهر الفرق). **لكنه لا يصحّ إلا بعد التأكّد من اكتمال المدخل — صفرٌ على مدخل مبتور لا يعني شيئاً** (بل يعني غالباً أنّ الفروع لم تُحمَّل). فالصفر برهانٌ فقط حين `!invMergeIncomplete()`.

## أدوار المستخدمين Supabase (user_roles) — الأمان الحقيقي في RLS
- **الجدول `public.user_roles`** (SQL في `supabase_user_roles.sql`، يُنفَّذ **بعد** كل الملفات السابقة، مرة واحدة): `user_id` uuid PK · `role` text check(`owner`/`admin`/`viewer`) · `created_at`. يُدرج المالك يدوياً في SQL (`mr.badraljohani@gmail.com` = owner، `on conflict do nothing`). **⚠ لا تُعِد تشغيل الملفات القديمة بعده** وإلا استعادت سياسات الكتابة المفتوحة.
- **الدالة `get_my_role()`** (`security definer`, `stable`): تقرأ دور `auth.uid()` متجاوزةً RLS فتمنع الـrecursion. تُستدعى في السياسات وعبر `sb.rpc('get_my_role')`.
- **المصفوفة (مطبَّقة في RLS على مستوى القاعدة، لا الواجهة فقط)**: **owner** = كل شيء وحصرياً **كتابة `mappings`** (insert/update/delete تتطلب `get_my_role()='owner'`) · **admin** = بقية الكتابة (waiting/zid_products/zid_sync_meta/activity_log تتطلب `in ('owner','admin')`) لكن لا روابط · **viewer** = قراءة فقط. القراءة (select) تبقى مفتوحة لكل موثّق في كل الجداول.
- **الواجهة تجمّل فقط** (`myRole`, `loadMyRole` عبر `db.roles.mine`، `applyRoleUI`): `requireOwner()` في `dbSetMapping/dbSetMappings/dbDelMapping` يمنع الربط لغير المالك بتوست «🔒 الربط صلاحية المالك» (RLS هو الحاجز الفعلي؛ `myRole=null` غير محمّل ⇒ نترك القاعدة تقرّر). `body.nonowner` يعتّم `#bindAllBtn`، وشارة الدور `#roleBadge` في صفحة الحساب.
- **صفحة «مستخدم جديد» للمالك فقط**: `applyRoleUI` يخفي رابط القائمة لغير المالك و`goPage('newuser')` يرفض الدخول. النموذج فيه segmented (admin افتراضي / viewer — بلا owner)؛ `doSignup` بعد `signUp` ＋ استعادة جلسة المالك يستدعي `db.roles.setRole(newId, role)` (ينجح لأن المنفّذ owner). حقول النموذج بنمط شاشة الدخول (`.signup-card` يشارك CSS `.login-box input`، بريد `dir=ltr`، عيون إظهار كلمة المرور).

## دور «marketing» وعزل «شاشة عرض المبيعات» — درسان في فحص الدور والحارس
- **الدور الرابع `marketing`** (SQL في `supabase_marketing_role.sql`): يقرأ `sales_movements`/`sales_uploads`/العرض `sales_stock` فقط، ولا يصل أي جدول آخر. **owner+marketing** يقرآن المبيعات · **admin يكتبها ولا يقرؤها** (INSERT owner+admin، وrecordMovements يُدرِج بلا `.select()` فلا يحتاج قراءة) · UPDATE/DELETE owner. عزل marketing عن كل جدول آخر بسياسات **restrictive** إضافية (`get_my_role() is distinct from 'marketing'`) — تُدمَج AND فتمنع marketing وحده **بلا لمس أيّ سياسة admin قائمة**. `sales_stock` عرضٌ `security_invoker=false` (يقرأ الأساس بصلاحية مالكه متجاوزاً RLS) مع `where get_my_role() in ('owner','marketing')` (يعمل بدور المُستدعي) ⇒ admin يرى 0 صفّ ولا يُتجاوَز الترشيح. الواجهة: `canSeeSales()` (owner+marketing) يحكم رؤية الرابط/الصفحة، و`goPage` يحجب admin/viewer عن `sales` ويقصر marketing عليها.
- 🚨 **درس «استثنِ المسموح لا تعدّد الممنوع»:** فحص الدور بقائمة أسماء ممنوعة يتسرّب عند إضافة دور جديد. `linkForbidden` كان `myRole==='admin'||myRole==='viewer'` ⇒ marketing يمرّ الحاجز البصريّ (RLS أنقذه). الصحيح: `myRole != null && myRole !== 'owner'` (استثناء المسموح). نظيره `doSignup` (`viewer?viewer:admin` ⇒ marketing يصير admin) صار قائمة مسموح صريحة `['admin','viewer','marketing']`. **عند إضافة أي دور، فتّش كل تعداد أسماء (`=== 'admin'` / `in (...)`) لا `isOwner` وحده.**
- 🚨 **درس «حارس يُعلن ما لا يغطّيه أصدق من حارس يُوهم بالشمول»:** `G-ROLE-SALES` يغطّي **الواجهة** (رؤية/تنقّل لكل دور) و**بنية SQL** (نصّ السياسات)، لكنّ **منع admin فعلياً في القاعدة** (RLS) يحتاج Supabase حيّاً — لا يُختبَر في المتصفّح بـsb مُزيّف. الحارس **يُعلن هذا الحدّ صراحةً**، ويُثبَّت المنع بالاستعلام اليدويّ (بحساب admin: `select count(*) from sales_movements` ⇒ 0). حارسٌ يدّعي تغطية RLS وهو يفحص سلسلة SQL نصّياً = وهم شمول.

## القرارات في القاعدة حصراً — الدفعة (أ)  ⚠️ SQL يدوي
نُقلت **كل قرارات المستخدم** إلى Supabase مصدراً وحيداً، وأُزيل تخزينها المحلي نهائياً. **لا تُستعمل الأداة بلا اتصال.**
- **الجداول** (`supabase_decisions.sql`، يُنفَّذ **يدوياً بعد** `supabase_user_roles.sql`، مرة واحدة — لا يفترضه الكود): `public.ignored_items` (**مهجور** — راجع «حذف تجاهل» أدناه) · `public.aliases` (أزواج «جذر\|\|\|جذر») · ＋ `ALTER waiting_items` بأعمدة `kind`('wait' الآن، 'unpublish' لاحقاً في ج) · `missed_rounds` · `last_seen_at`. RLS بنمط `waiting`: قراءة كل موثّق · كتابة owner+admin.
- **الوحدات**: `db.aliases{getAll,add,bulkUpsert}` · توسيع `db.waiting`. (`db.ignored` **حُذفت** في دفعة تبسيط التدفّق ١.)
- **كل كتابة القاعدة أولاً** (`markWaiting`/`unmarkWaiting`/`dbAddIgnored`/`dbDelIgnored`/`dbSetMapping*`): `requireOnline()` ثم `await db…`؛ **الذاكرة تُحدَّث بعد نجاح القاعدة فقط** (فشل ⇒ `dbFail` يُظهر توست ولا يترك أثراً — لا تشعّب صامت). `batchApply` يتعلّم `aliases` بـ`db.aliases.bulkUpsert` (بأفضل جهد، لا يُفشل الاعتماد).
- **التحميل من القاعدة** (بعد الدخول في `loadMappingsFromDB`): `loadIgnoredFromDB`/`loadAliasesFromDB`/`loadWaitingFromDB` **تستبدل الذاكرة إن كانت القاعدة غير فارغة** (وإلا تُبقي المحلي كبذرة ترحيل).
- **`link-tags.json` أُسقط** نهائياً — الوسم يُشتقّ من `mappings.source`.
- **الترحيل لكل جدول على حدة** (`migPending`/`doMigration(type)`): لقطات إقلاع `bootLocalMap`/`bootLocalWaiting`/`bootLocalAliases` (من `mapping/waiting/aliases.json` ＋ localStorage القديم، **قراءة بذرة فقط**). شريط `#migrateBar` يعرض زرّاً لكل نوع قاعدته فارغة ومحلّيه غير فارغ؛ الفشل الجزئي لا يمسّ بقية الأنواع ولا يمسح المصدر.
- **حجب عدم الاتصال** (`setOfflineGate`/`blockedOffline`، بانر `#offlineBar`): `!dbOnline` ⇒ بانر أحمر ظاهر ＋ `body.db-offline` يعتّم ويعطّل `#runBtn` وأزرار التنزيل، و`runFromDB`/`downloadSelected`/`downloadOne`/`downloadMerge` تُحجب بـ`blockedOffline()`، و`wireDl` لا يفعّل التنزيل إلا `dbOnline`. `setDbBadge` يستدعي `setOfflineGate` فيغطّي كل مسارات الحالة.
- **حارس انحدار** (`tests/batch-a-invariants.mjs` ＋ `.github/workflows/tests.yml`): فحص ثابت يرسب إن عاد أي `localStorage.setItem` للقرارات، أو كُسر ترتيب «القاعدة قبل الذاكرة»، أو سقط حجب التنزيل، أو تأثّرت مسارات `run` الجوهرية. **مقياس أداء 300 صفّ (headless) يأتي في الدفعة (ب)** (يقيس رسم قائمتها).
- **خارج النطاق (يبقى محلياً):** `matchedHistory` (الدفعة د لاحقاً) و`priceOffsets` — عبر `saveMatchedHistory`/`saveOffsets`، ويُصدَّران في `exportConfig`/`saveToRepo` فقط.

## الحفظ والإعدادات (localStorage + الريبو)
- بعد الدفعة (أ): القرارات (روابط/انتظار/aliases) **ليست في localStorage ولا الريبو** — مصدرها القاعدة. يبقى في الريبو (`exportConfig`/`saveToRepo`) فقط: زيادات الأسعار `price-offsets.json` وتاريخ المطابقة `matched-history.json` (`matched-history` يتراكم تلقائياً في كل مطابقة ناجحة).
- **الاستبعاد (`excludedSet`) ليس ضمن هذه المنظومة**: مؤقت للجلسة فقط (in-memory)، لا localStorage ولا ملف في الريبو — راجع زر «✕ شيلها» في قسم الواجهة.
- **بصمة الجلسة** (`renderFingerprint`، بعد الدفعة أ): الروابط/الانتظار تُعرض كـ«(قاعدة)»؛ و`matchedHistory` وحده يُظهر «محلي غير مصدَّر» وينبّه للحفظ في الريبو.
- رمز GitHub (fine-grained, Contents R/W على `zid-sync`) يُخزَّن في localStorage لكل جهاز — **لا يُوضع في الكود إطلاقاً** (الموقع عام).
- خيارات (`zid-options-v1`). (لا تخزين ثيم — داكن فقط.)

## نبضة قلب Supabase — منع الإيقاف بالخمول (GitHub Actions)
مشروع Supabase المجاني يُوقَف بعد **7 أيام خمول** (لا نشاط على القاعدة). لتفادي ذلك بلا خوادم وبلا تكلفة:
- **الملف** `.github/workflows/supabase-keepalive.yml`: workflow مجدول `schedule: cron "0 3 */3 * *"` (كل 3 أيام 03:00 UTC) ＋ `workflow_dispatch` (تشغيل يدوي).
- **الخطوة**: `curl` خفيف على REST endpoint (`/rest/v1/zid_sync_meta?select=id&limit=1`) — **أي وصول نشاط يصفّر عدّاد الخمول حتى لو رفض RLS القراءة**. يقرأ كود الاستجابة بـ`-o /dev/null -w "%{http_code}"` (بلا `-f`): **200/401/403 = نجاح النبضة** (المشروع حيّ واستقبل الطلب)؛ **000 (فشل شبكة) أو 5xx = فشل الخطوة**.
- **المفتاح من secrets حصراً**: `SUPABASE_PUBLISHABLE_KEY` في Settings → Secrets and variables → Actions (يُضاف يدوياً مرة واحدة). **لا يُوضع نصاً في الـworkflow** (نظافة، رغم أنه publishable).
- **الإشعار عند الفشل**: تلقائي — GitHub يرسل بريداً لصاحب الريبو ويُظهر التشغيل الفاشل في تبويب Actions (لا حاجة لتعقيد إضافي).

## عند حذف صفحة
ابحث عن **مقابض عناصرها** (المتغيّرات التي تحمل `getElementById`) لا عن اسم الصفحة وحده. مثال واقعي: بعد حذف `#logoutBtn` بقيت ثلاث إشارات للمتغيّر `lb` في `onAuthStateChange` و`setNewPassword` — لم يلتقطها البحث عن «account» ولا عن «logoutBtn»، وكانت ترمي `lb is not defined` **عند كل تغيّر جلسة**. كشفها اختبار «لا أخطاء JS» بعد الحذف. وبالمثل: حذف مولّد ترميز يُيتّم كل دالّة كان `onclick` يستدعيها من داخله (`zeroTableHTML` ⇒ `toggleZero`/`toggleUnpublish`) — افحص اليتامى بعد كل حذف.

## حفظ موضع التمرير — عودة التبويب وإعادة بناء القوائم
**العَرَض:** المستخدم يقرأ في منتصف جدول، ينتقل لتبويب متصفح آخر، وعند العودة يجد نفسه أعلى الصفحة.

**السبب الجذري (مقيس):** مكتبة Supabase تسجّل `visibilitychange` بنفسها (`_onVisibilityChanged` ⇐ `_recoverAndRefresh`) وتُطلق **`SIGNED_IN`** عند عودة التبويب — لا حدث `TOKEN_REFRESHED` كما قد يُظنّ. معالجنا في `onAuthStateChange` يستدعي عندها `loadMappingsFromDB()`، وهي تُنهي بـ`routeFromHash()` ⇒ `goPage(id, true)` ⇒ `window.scrollTo({top:0})` **بلا شرط**. القياس: `scrollY` 1042 ⇒ 2 على الرئيسية، و683 ⇒ 0 على المخزون.

**الإصلاح:** `goPage` يحتفظ بـ`changed` (هل تغيّرت `.page.active` فعلاً؟) ويمرّر للأعلى **فقط** عند `changed || !fromHash` — أي عند تنقّل حقيقي أو نقر صريح من المستخدم. `routeFromHash` يمرّر `fromHash=true`، فعودة التبويب على نفس الصفحة لا تمرّر. **تحديث البيانات عند العودة يبقى كما هو** — المطلوب كان تحديثاً بلا قفزة، لا تعطيل التحديث.

**القاعدة الوقائية العامة** (`scrollMark(el?)` ＋ `keepScroll(fn)` بجوار `goPage`): أي دالّة تعيد بناء جدول/قائمة كبيرة تلتقط `scrollTop` للحاويات (`SCROLL_KEEP_SEL` = `.twrap, .notif-list, .q-results, .wh-results, .col-list, .vg-list, .sc-list`) ＋ موضع النافذة قبل `innerHTML` وتعيده بعده. آمن دائماً لأن المتصفح يقصّ القيمة إن قصُر المحتوى فعلاً. مُطبَّقة على: `renderDetail` (غلاف رفيع فوق `renderDetailBody` — كل النداءات الخارجية بلا تغيير؛ يشمل القائمة الموحّدة والرسم التدريجي) و`loadActivityInto` (النمط الأخطر: **عنصر نائب** «جارٍ التحميل…» يقلّص القائمة أثناء انتظار الشبكة فيُقصّ الموضع إلى 0 ثم لا يعود).

**التمرير المقصود لم يُمَسّ:** `run()` · `invGoto` · `startManualLink` — كلها `scrollIntoView` تقع **بعد** الرسم فتغلب الاستعادة.

**فخّ في اختبار العنصر النائب:** محاكاة `db.activity.getRecent` بدالّة `async` فورية تعود في microtask ⇒ لا يقع تخطيط بين العنصر النائب والنتيجة ⇒ لا يُقصّ الموضع ⇒ **الاختبار ينجح على الكود المعطوب**. لا بدّ من تأخير حقيقي (`setTimeout`) يحاكي زمن الشبكة. تحقّق دائماً بتشغيل الاختبار على النسخة قبل الإصلاح (`git show HEAD:index.html`) — الفحص الذي لا يرسب قبل الإصلاح لا يثبت شيئاً.

**فخّ ثانٍ — Chrome headless يثبّت innerWidth عند 500 (الدفعة ب):** حارس «لا تجاوز أفقي عند 360/390» عبر `--headless=new --dump-dom` **يبدو ناجحاً وهو أعمى**: Chrome headless يفرض حدّاً أدنى للنافذة 500×500، فـ`--window-size=360` يُعطي `innerWidth=500` (مؤكَّد: `data-w="500x0"` عند 360 و390 معاً)، وقواعد `@media` للـviewport لا تُطبَّق. فبطاقة تتجاوز فعلاً عند 360 تمرّ لأنها تُقاس عند 500. البروكسي بحاوية ثابتة `width:360px` أسوأ (نتيجته تختلف باختلاف عرض النافذة، وبلا `@media`). **الحلّ الأمين:** `puppeteer-core` يقود Chrome النظامي ويضبط العرض الحقيقي بـ`page.setViewport({width:360,isMobile:true})` (CDP `setDeviceMetricsOverride`) فتُطبَّق `@media` ويصير `vw=360` فعلاً — راجع `tests/perf/overflow-guard.mjs`. **تحقّق ذاتي إلزامي** (`--broken`): يحقن بطاقة بعنصر `900px` ويؤكّد أن الحارس يرسب (التقط 841px تجاوزاً)، وإلا فالحارس لا يثبت شيئاً. القاعدة نفسها: أثبت رسوب الحارس على المعطوب قبل الوثوق بنجاحه على السليم.

## إعادة بناء innerHTML تمسح الحالة الحيّة — قِس الظاهرة لا البنية
إعادة بناء `innerHTML` تمسح **كل حالة حيّة لا يحملها الـDOM**: موضع التمرير (عولج بـ`keepScroll`) وتقدّم حركة CSS (عولج بخمول `renderMarquee`). فحص الـDOM يُظهر العناصر سليمة بينما السلوك مكسور — فالمقياس يجب أن يكون **الظاهرة نفسها لا بنيتها**. عطل الشريط المتحرّك مرّ على **ستّ نسخ من الحارس** (الطول → التنوّع → البنية → الخمول) قبل أن يُقاس ما يراه المستخدم فعلاً: **العبور عبر الـviewport**. والجذر النهائي لم يكن أياً مما سبق بل **تموضع RTL** (المحتوى خارج الرؤية رغم سلامة DOM والحركة — راجع §٦). الدرس المتكرّر: كل نسخة قاست بديلاً عن السلوك؛ الحسم جاء دائماً من **قياس صفحة المستخدم الحيّة** (عدّاد العابرين) ثم إعادة إنتاج الظاهرة نفسها في المِعمَل.
- **العطل الملموس (الشريط المتحرّك):** كل `renderMarquee` كان يعيد `track.innerHTML` ⇒ عنصر مسار جديد ⇒ حركة CSS تُستأنف من `translateX(0)`. المُعيدون الحيّون (Supabase `SIGNED_IN` عند عودة التبويب ⇒ `loadMappingsFromDB` · `ResizeObserver` · `run(true)` من قرار) كانوا يصفّرونها باستمرار، فلا تتجاوز أول عنصرين ⇒ «صنفان يعبران» بينما DOM فيه 30. القياسات «بعد الاستقرار» كانت تكذب (الحالة تُشفى قبل فتح console)؛ الحسم كان من **صفحة المستخدم الحيّة** (عدّاد العابرين عبر viewport = 2) ثم إعادة إنتاجه بـ«إعادة رسم كل 800ms ⇒ 0 يعبر».
- **الإصلاح — خمول `renderMarquee` (`mqLastKey`):** المفتاح = `mode` (ساكن/منزلق) + تسلسل SKUs المرسومة. إن لم يتغيّر ولا يزال `track` مرسوماً ⇒ **لا يُلمَس `innerHTML`** فتستمرّ الحركة. التسمية بالعدد الحيّ (774) **خارج المفتاح** — تُحدَّث دائماً (عنصر `.mq-label` منفصل عن `.mq-track`، فتحديثها لا يمسّ الحركة). الرسم المؤقّت (فرع `wCont≤1`) يضبط `mqLastKey=null` ليُعاد البناء المقيس عند إطلاق الـRO؛ والبناء المقيس والساكن يسجّلان المفتاح. تغيّر البيانات (قرار يُسقط صنفاً) يغيّر SKUs ⇒ إعادة بناء (لا بيات).
- **الحارس `tests/perf/marquee-idle.mjs`:** يقيس الظاهرة — بعد إعادة الرسم **بنفس البيانات** عنصر `.mq-item` الأول **هو نفسه لم يُستبدل** (وسم `data-probe` يبقى) ⇒ الحركة لم تُصفَّر؛ وعند **تغيّر** البيانات يُعاد البناء (الوسم يزول) ⇒ لا بيات؛ والتسمية تُحدَّث دائماً (تغيّر `needCount` وحده ⇒ التسمية جديدة والمسار محفوظ). أسنانه على `git show HEAD:index.html` (النسخة قبل الإصلاح تعيد الكتابة دائماً ⇒ الوسم يزول ⇒ يرسب).
- **مسارات `innerHTML` أخرى على عناصر ذات حالة حيّة (مكشوفة، خارج نطاق الإصلاح الحالي):** `#unBody`/بطاقات القائمة الموحّدة (`renderUnifiedList`) تحمل **حقل بحث combo قيد الكتابة والقائمة `.bt-results-float` المفتوحة**، و`#unSearch` (يُعاد بناؤه في `unifiedBar`) يحمل تركيز/مؤشّر البحث — إعادة رسم خلفية أثناء الكتابة تُتلفها (الموضع محفوظ بـ`keepScroll`، لا التركيز/المنسدلة). `#expSearch` محميّ (يحدّث الجدول فقط). الجداول/النشاط/الرسم الدائري بلا حالة حيّة أو محميّة بـ`keepScroll`.

## الاختبارات — الضمان البنيوي لا يكفي
الضمان البنيوي يثبت أن الكود لم يُحذف، لا أن السلوك صحيح.
مثال: `processWaitingOnUpload` كان يكشف العودة بالـSKU الخام فيفوت كل مربوط يدوياً (كوده المخزني مختلف بحكم التعريف). البنية سليمة والميزة معطّلة.
لكل ادّعاء سلوكي: اختبار سلوكي على متصفّح حقيقي، ويُشغَّل على النسخة قبل الإصلاح للتأكّد من رسوبه.

**الدفاع العميق والاختبار السلوكي:** استبعاد الأب (`has_variants=Yes`) من `qtyRows` محميّ **بطبقتين**: `continue` في حلقة `run` ＋ `stripHasVar` النهائية قبل الكتابة. **كسر إحداهما لا يُسرّب** (الطبقة الأخرى تمسك)، فالاختبار السلوكي يبقى أخضر **صحيحاً** لأنّ السلوك لم يتغيّر. لذا مشهد الأسنان في المشغّل السلوكي (`parentQtyCfg`) يكسر **الطبقتين معاً** ليتغيّر السلوك فعلاً (`PV` يدخل `qtyRows`). **درس عام:** حين يرسب حارس بنيوي ويبقى السلوكي أخضر، افحص أولاً إن كان السلوك محميّاً بطبقات قبل أن تتّهم السلوكي بالتساهل — أخضره قد يكون صواباً لا تهاوناً.

- **حارس التباين** (`tests/perf/contrast-guard.mjs`): يقيس WCAG 4.5:1 (3:1 للكبير) على الصفحة الحقيقية — لون كل نصّ مقابل خلفيته الفعّالة (تركيب rgba فوق `--bg` ＋ متوسّط محطّات التدرّج) عبر مشاهد (home/inventory/options/login) × عرضين (360/1200). **داكن فقط** (لا بُعد ثيم بعد حذف الفاتح). تحقّق ذاتي `--broken` يحقن نصّاً منخفض التباين ويؤكّد الرسوب. (كان موصوفاً في CLAUDE.md ولم يكن موجوداً — بُني في دفعة الثيم ٠.)
- **المشغّل السلوكي** (`tests/behavioral/run-behavior.mjs`): يشغّل `run()`/`processWaitingOnUpload`/`applyDecision` **الحقيقية** في Chrome عبر `puppeteer-core` على بيانات وهمية (Supabase/XLSX وهميان، الشبكة الخارجية محجوبة فلا نلمس القاعدة، والصفحة تُخدَم على `http://localhost` فـ`localStorage`/`saveMatchedHistory` تعمل). يثبت: ج-١ حياد (صفوف الملفّين متطابقة `1a036f4`↔`8bb0486`) · ٤ فورية `applyDecision` · ٥ إجماع الأب · صمام التخفيض · ٣+١ العودة · **٥أ+٥ب الثوابت الهشّة بالقيمة**: `shareByRaw` (7 على 3 = **3+2+2 ومجموعها 7** — لا مخزون وهميّ/مبخّر) · `offset` (100+180=**280** — لا بيع أرخص) · `lowValZero` (سعر 300<400/كمية 2≤3 ⇒ **qty=0**، وسعر 500 ⇒ qty=2 — سياسة تصفير الرخيص شبه الفارغ) · إعادة النشر (بسيط `No`+مخزون ⇒ **`published=Yes`** — وإلا بقي مخفيّاً رغم المخزون) · تحويل غير المحدود (زد «infinite» مطابَقة ⇒ **qty=9 عدداً** لا «infinite» — لا بيع زائد). **الأسنان بطفرات مقيسة** لكلٍّ: `base+rem`→`base` · `whBase+offset`→`whBase` · `<400`→`<0` · `="Yes"`→`="No"` · `isInf?orig:whQtyReal`. يتطلّب CI `fetch-depth: 0`.

## تبسيط تدفّق الأعمال — الدفعة ١: حذف «تجاهل» نهائياً
التدفّق النهائي المعتمد: **مؤكد · يحتاج ربط · غير متوفر · غائب عن المخزن** — لا خامسة. و«تجاهل» أُزيل من الواجهة والمنطق والقاعدة.

**الاكتشاف الذي حكم المخاطرة (مقيس قبل الحذف):** `ignoredSet` كان يظهر في `run()` **مرّتين فقط** — حارس العودة (`reappearedSet`) وعدّاد الرسم الدائري (`cls.ignored++`). **ولا واحدة تمسّ الإخراج.** الصنف المتجاهَل كان يسلك مسار أي صنف بلا مطابقة (`unmatched` ⇒ خارج الملفّين)، وأثره الوحيد إخفاؤه من قائمة القرار عبر `okItem`. ⇒ **الحذف صفر أثر على ملفَي التصدير**؛ الأصناف التسعة عادت إلى «يحتاج ربط».

> ⚠ **تصحيح لوثيقة سابقة:** كان مكتوباً هنا أن «✕ = خارج كل الملفات والمطابقات نهائياً». **الكود لم يفعل ذلك قط**: لو كان للمتجاهَل كود مطابِق في المخزن لدخل الملفّين طبيعياً. الوصف كان نيّةً لا سلوكاً.

**ما حُذف (٦٧ تعديلاً):** CSS (`#ignoredCard`/`#ignoredBody`/`.cat-ign`/`button.mini.ignore`/`#unBody tr.ignored`) · البطاقة `<details id="ignoredCard">` · خيارا فلتر السجل · `IGN_KEY`/`ignoredSet`/`lastIgnored`/`bootLocalIgnored`/`baseIgnSet`/`dbIgnoredCount` · `db.ignored` ＋ `dbAddIgnored`/`dbDelIgnored`/`loadIgnoredFromDB` · فرع `ignored` في `migPending`/`doMigration` · قراءة `ignored.json` في `loadConfig` · زرّ «✕ تجاهل» في `batchCardHTML` · `undoIgnoreDecision`/`renderIgnored`/`unignore` · شارة/فلتر/تسمية المستكشف · سطر البصمة · شريحة `PIE_SLICES`.

**ما بقي عمداً:** `ignored_added`/`ignored_removed` في `ACT_META` و`actDescribe` — **حتى تبقى أحداث السجل التاريخية مفهومة**. لا تُحذف. وجدول `public.ignored_items` **يبقى في القاعدة بلا `drop`** (بيانات تاريخية)، والكود لا يقرأ منه ولا يكتب إليه.

**سلامة المجموع في الرسم الدائري:** حُذفت شريحة «متجاهلة» و`cls.ignored`، وصار السطر `if (collided) cls.collided++; else cls.unmatched++;` — فالمتجاهَل يُحسب «يحتاج ربط» وهو **وصفه الصحيح**. المجموع يبقى مضبوطاً **بحكم البنية** (كل مسار في الحلقة يزيد عدّاداً واحداً بالضبط) بلا فئة «أخرى» مبهمة.

**أسنان الحراس:** تأكيد `IG1` في المشغّل السلوكي كان يثبت أن المتجاهَل **لا** يظهر كعائد — **قُلِب** ليثبت أنه صار يسلك سلوك `W1` بالضبط (عاد غير مربوط ⇒ «يحتاج ربط» بوسم `reappeared`). فالحارس يرصد أي بقايا لاستثناء التجاهل بدل أن يصمت.

### ⚠ فخّ CRLF في الحراس الساكنة
خمسة حراس تقطع السكربت بـ`html.lastIndexOf("
<script>
")`. النسخة العاملة على ويندوز **CRLF** فتعطي `-1` ⇒ **رسوب زائف** لا علاقة له بالكود. تمرّ في CI لأن checkout بـLF. للتشغيل محلياً: انسخ `index.html` مع `.replace(/
/g,"
")` إلى مجلد مؤقّت وشغّل الحراس عليه. الحراس المتصفّحية تحتاج `CHROME_PATH`.

## تبسيط تدفّق الأعمال — الدفعة ٢: توحيد التسميات
**الحالات الظاهرة للمستخدم أربع لا غير:** مؤكد · **يحتاج ربط** · **غير متوفر** · **غائب عن المخزن**.

| قديم | جديد | لماذا |
|---|---|---|
| «بدون ربط» | **«يحتاج ربط»** | وصف بالفعل المطلوب لا بالنقص |
| «⚠ سيُصفَّر» ＋ «✓ مُدار» | **«غير متوفر»** واحدة | كلاهما `waitingSet` — الفارق كمية زد الحالية فقط، أي **تقسيمة عرض لا حالة عمل**. العدّاد صار `p + m`. |
| «سبق ربطه» زرّاً في شريط التنفيذ | **شارة في الصفّ ＋ فلتر** | معلومة مساعدة لا إجراء |

**النتيجة ليست حالة** (البند ٧): الصفّ الخفيف يعرض ثلاث شارات بترتيب سببي — `غير متوفر` (الحالة) ← `pubBadge` (نشر زد الحالي) ← `.cat-out` **«التصدير: 0 · غير منشور»** (النتيجة). **الوسم «التصدير:» ضروري**: بدونه تتجاور «منشور» و«غير منشور» فتُقرأ تناقضاً — كُشف باللقطة لا بالحارس.

**تراتب الأزرار** في `unifiedBar`: صفّ `.uni-filter` مستقلّ (الكل · سبق ربطه N) ثم `.uni-actions`: **Primary** «اعتماد العناصر المحددة» (`btn-run`) · **Secondary** «اعتماد المطابقات المؤكدة» · **Utility** «التراجع عن آخر عملية».

**كل الأيقونات SVG** (`hourglass`/`eyeOff`/`check`/`spark`) — `emoji-guard` يرسب على أي إيموجي في عنصر واجهة دائم.

**العرض على الجوال مقيس لا مفترض:** «غير متوفر» أطول من «مُدار» وأُضيفت شارة ثالثة. القياس @360/390 بـ`isMobile`: صفر تجاوز صفحة وصفر تجاوز داخل الصفّ (282×175 و312×156).

## حدّ الحراس — ما لا تمسكه آلة
**الحراس تفحص البنية والقياس لا المعنى.** لا حارس يقرأ شارتين متجاورتين ويسأل: هل تتناقضان؟

**مثال واقعي (الدفعة ٢):** «التصدير: 0 · غير منشور» بجوار «منشور» في الصفّ نفسه — تناقض صريح يقرأه المستخدم في لحظة. مرّ على **عشرين حارساً** (بنية · تباين · تجاوز · احتواء · إيموجي · سلوك) وكشفته **اللقطة وحدها**.

**القاعدة:** بعد أي تغيير في نصوص الواجهة، **اقرأ الصفّ كاملاً كما يقرأه المستخدم — لا كما يفحصه الحارس.** الحارس يجيب «هل العنصر موجود ومقاسه سليم؟»؛ والمستخدم يسأل «هل ما أقرأه متّسق؟». السؤال الثاني لا يُؤتمت — لقطة وقراءة بشرية.

**ما يُشتقّ منها:** كل شارة تُضاف بجوار شارة قائمة تحتاج **وسماً يفصل دورها** (حالة · نتيجة · سياق)، وإلا قُرئت الشارتان كأنّهما من نوع واحد فيتولّد التناقض.

## تبسيط تدفّق الأعمال — الدفعة ٣: إعادة تصميم «يحتاج ربط»
**الشريط بعدّادين لا ثمانية.** كان: `يحتاج قرار N · 🟢g · 🟡y · 🔴r · غائب A · سيُصفَّر p · مُدار m · متجاهَل i` (ثمانية أرقام في سطر). صار: رأس وصفي `.uni-head` ＋ شريحتان `.uni-chip` — **يحتاج ربط N** · **غير متوفر M**. لا رقم فُقد:
- **الثقة** انتقلت من الشريط إلى **بطاقة الصنف** (`matchLine`/`CONF_LBL`): «مطابقة مؤكدة» · «مطابقة محتملة» · «بلا مرشّح موثوق» ＋ الأسباب بأرقامها (`مقاس✓ · سعر✓ · اسم ٦١٪`).
- **الغائب** انتقل إلى **ترويسة قسمه** — وبالعدّ **الخام** (`absentN`) لا المُصفّى (`absCards.length`)، فلا يتغيّر بتفعيل فلتر «سبق ربطه». (نفس عطل ٢ القديم، كان كامناً في الترويسة.)

**النسبة ظاهرة لا tooltip:** كانت `title` على البطاقة — **والجوال بلا تحويم** (نفس علّة `waitNoopReason`). صارت صفّ `.mc-match` مرئياً، و`comboPick` يحدّثه بدل الـ`title`.

**البطاقة بزرّين:** «اعتماد الربط» (checkbox — الآلية الجماعية تعتمد عليه) ＋ «غير متوفر». الزرّ الثالث «تجاهل» زال في الدفعة ١.

### ثلاثة تكرارات دلالية كشفتها القراءة البشرية — لا حارس
كلها مرّت على **عشرين حارساً** ورصدتها قراءة النصّ المرسوم صفّاً صفّاً:
1. **«لا مرشّح — ابحث بالأسفل» ＋ شارة «بلا مرشّح موثوق»** تحتها ⇒ الجملة مرّتين. العلاج: `matchLine` تُعيد `""` حين `!c` — عمود المستودع يقولها فعلاً.
2. **ترويسة «غير متوفر — تُصدَّر بكمية 0 وغير منشورة» ＋ شارة «التصدير: 0 · غير منشور» في كل صفّ** ⇒ القاعدة معلنة مرّتين. العلاج: الترويسة صارت عدّاداً فقط `غير متوفر (N)`؛ الشارة تحمل النتيجة.
3. **صفّ `.mc-match` الفارغ** يشغل `margin-top` فيترك فراغاً ميتاً ⇒ `.mc-match:empty { display: none }`.

**والقاعدة المستخلصة:** كل شارة/ترويسة تُضاف تسأل أولاً — *هل يقول هذا النصّ ما يقوله عنصر مجاور؟* الحارس يفحص الوجود والمقاس؛ التكرار الدلالي لا يمسكه إلا القارئ.

## المرساة الجوفاء — حارس يمرّ بالمصادفة
**المرساة النصّية تفحص وجود كلمة، لا سلوكاً.** أي كلمة قد ترد في `title` أو `aria-label` أو تعليق فتنجح المرساة بلا أن تحرس شيئاً.

**حارس يمرّ بالمصادفة أخطر من حارس غائب:** الغائب معلوم، والجوف يعطي طمأنينة كاذبة.

**القاعدة:** اربط المرساة بثابت **بنيوي** (اسم دالّة · بنية حساب · معرّف عنصر) لا بنصّ معروض.

### الوجه المقابل — المرساة مُفرِطة التخصيص ترسب زوراً
> **مرساة مثبّتة على تفصيل صياغيّ (قوس، مسافة، ترتيب حدود) ترسب زوراً عند أي إضافة مشروعة. المرساة تحرس المعنى لا الصياغة.**

**الحادثة (سلامة المدخل):** ثابت `batch-a` «التنزيل يشترط `dbOnline`» كان `/…&& dbOnline\s*\)/` — مثبّتاً على **القوس الختامي**. حين أضفتُ حدّاً مشروعاً `&& !invMergeIncomplete()` في `wireDl` (شرط أمان ثانٍ)، لم يعد القوس يلي `dbOnline` ⇒ **رسبت المرساة مع أنّ الثابت قائم** (بل شُدّد: `dbOnline` لا يزال حدّاً لازماً). **رسوب زائف على كود سليم — عكس المرساة الجوفاء تماماً.**
- **التمييز الحاسم قبل تعديل أي مرساة راسبة:** أهو ثابت انكسر فعلاً، أم ثابت تقادم، أم **مرساة سليمة الثابت مُفرِطة التخصيص**؟ الحسم بالطفرة: إن كان الثابت المعنويّ ما زال محقّقاً في الكود الجديد (‏`dbOnline` conjunct لازم) فالمرساة هي المُخطئة لا الكود.
- **الإصلاح يبقي الأسنان:** لا تُرخِ المرساة حتى تُثبت أنّها ما زالت ترسب على الطفرة الحقيقية. رُخّيت `dbOnline\s*\)` → `dbOnline\b` (تؤكّد الحدّ اللازم، تقبل حدوداً بعده)، وأُثبت بالطفرة أنّ حذف `&& dbOnline` يُرسبها. ثم **شُدّدت** إلى `dbOnline\s*&&\s*!invMergeIncomplete\(\)` فصارت تحرس **شرطَي الأمان معاً** (الطفرة على أيٍّ منهما تُرسبها) — أقوى لا أضعف.

### كيف تُكتشف: الطفرة، لا النظر
لا يُحكم على مرساة بالقراءة. **عطّل التنفيذ الحقيقي وشغّل الحارس**: إن نجح ⇒ جوفاء.

**وتحقّق أولاً أن الطفرة طُبِّقت فعلاً.** في تدقيق ٥٦ مرساة أعطت خمسُ طفراتٍ نتيجة «جوفاء» ثم تبيّن أن **الطفرة نفسها معيبة**: `replace` بلا `/g` أصاب أول تطابق خارج الدالّة · وتغيير `needCount` إلى `XneedCount` يُبقي `includes("needCount")` صادقاً بالسلسلة الجزئية · و`fnBody` يقتطع الدالّة عند أول قوس داخل نصّ. **طفرة معيبة تُدين مرساة سليمة** — وهي الوجه الآخر لقاعدة «الاختبار الذي لا يرسب قبل الإصلاح لا يثبت شيئاً».

### نتيجة التدقيق (٥٦ مرساة في `b2-invariants`)
**جوفاوان مؤكَّدتان بالطفرة:**
1. `script.includes("↩ توفّر")` — النصّ يرد في **تعليقين** (2769 · 4086) وفي `renderReactivated`. حذفُ الشارة من `lostTag` كلياً **لا يُرسِب الحارس**. العلاج: اربطها بـ`fnSrc("lostTag")`.
2. `uni.includes("isAbsent") && uni.includes("uni-sec")` بعنوان «renderUnifiedList **يرتّب**: يحتاج قرار ثم غائب» — تفحص **الوجود** وتدّعي **الترتيب**. قلبُ الترتيب (`rows = [...absCards]`) **لا يُرسِبها**. العلاج: افحص موضع `needCards` قبل `absCards` في بناء `rows`.

**والباقي متين** — ٥٤ مرساة مربوطة باسم دالّة أو بنية حساب أو معرّف عنصر، وكلها أرسبت طفراتها.

**نمط متكرّر يستحقّ الحذر (ليس جوفاً بذاته):** `includes("منشور")` يصدق داخل «غير منشور» — الفحص الأوّل مُستَغرَق في الثاني فلا يضيف شيئاً.

## تبسيط تدفّق الأعمال — الدفعة ٤: صمّاما الحماية
يسبقان تغيير سلوك «الغائب» (الدفعة ٥) عمداً: بعده يصير أي خلل في ملف المستودع تصفيراً وإخفاءً لمئات المنتجات دفعةً واحدة، وخيار `keep` كان يحمي من ذلك ضمنياً.

### ① بوّابة الرفع — `invShrinkCheck(kind, newCount)` · `INV_SHRINK_PCT = 15`
تقارن عدد أكواد الملف الجديد بـ`invMeta.wh_count`/`branch_count` (كلٌّ بمرجعه المستقلّ). النقص فوق ١٥٪ ⇒ `confirm` بالأرقام الثلاثة (السابقة · الجديدة · النقص ＋ النسبة) ＋ العاقبة ＋ ما يفعله المستخدم.

**موضعها حاسم:** في `onMergeWh`/`onMergeBranch` **قبل إسناد `mergeWh`/`mergeBranches`** — لا داخل `syncInventoryToDB`. وضعُها في المزامنة يمنع الكتابة في القاعدة **لكنّ الملف الناقص يُعتمد محلياً فتجري المطابقة عليه**. الإلغاء الآن يترك المخزن السابق كما هو تماماً — وهذا ما يفحصه الحارس بأسنانه.

**بلا مرجع سابق ⇒ لا بوّابة** (أول رفعة، أو قاعدة فارغة): لا مقارنة ⇒ لا حكم.

### ② صمّام التنزيل — `zeroRatio(qtyRows, zidTotal)` · `zeroGateOk` · `ZERO_GATE_PCT = 60`
**المقام إجمالي منتجات زد لا حجم الملف.** حجم ملف الكميات يتقلّص كلما قلّت التغييرات، فنسبةٌ منه تقفز بلا معنى؛ أمّا إجمالي زد فمستقرّ ومعروف، فالنسبة **قابلة للمقارنة بين التشغيلات**.

**العتبة مشتقّة من قياس لا من رأي** (على بيانات المستخدم: 2294 منتج زد · 603 غائب · 20 انتظار غير مصفَّر · 226 انتظار مصفَّر أصلاً يُسقطه `stripNoChangeQty`):

| الحالة | مصفَّر | النسبة |
|---|---|---|
| اليوم (`absent = keep`) | ~20 | **0.9%** |
| بعد الدفعة ٥ (`always zero`) | ~623 | **27.2%** |
| العتبة | — | **60%** (أكثر من ضعف المتوقَّع) |

**والنسبة تُعرض في سطر الملخّص كل تشغيل** (`مصفَّر: N من M (P%)`) فيعرف المستخدم نطاقه الطبيعي وتُعايَر العتبة على واقعه لا على تقدير.

**⚠ فخّ التدوير عند الحدّ:** `Math.round(15.05) = 15` فيمرّ تجاوزٌ فعليّ. **المقارنة على القيمة الدقيقة والتدوير للعرض فقط** — في الصمّامين معاً. كشفه حارس الحدّ (2000 ⇒ 1699)، لا القراءة.

### الحارس `tests/valves.mjs`
سلوكي في متصفّح حقيقي (يستبدل `window.confirm`): العتبة الدقيقة (على الحدّ يمرّ · فوقه يوقف) · الفرع بمرجعه المستقلّ · بلا مرجع لا بوّابة · **الإلغاء لا يعتمد الملف محلياً** · المقام = إجمالي زد · الصمّام يمنع التنزيل فعلاً بـ`preventDefault` · وعلى ملف الكميات وحده. و`--broken` يكسر نداء البوّابة في `onMergeWh` ⇒ يرسب فحص «الإلغاء لا يعتمد».

## تبسيط تدفّق الأعمال — الدفعة ٥: «غائب عن المخزن» ⇒ صفر ＋ غير منشور دائماً
**القاعدة النهائية:** كل صنف **معروف أو مربوط سابقاً** (`manualMap[sku]` أو `matchedHistory`) وغير موجود في **المخزن الموحّد** (مستودع ＋ فرع) ⇒ `qty = 0` ＋ `published = No`. **حُذف `opts.absent` بقيمتيه** (`keep`/`zero`) ومعه صفّ الخيار و`#absentWarn`، وحلّ محلّه سطر إخباري: القاعدة تُشرح لا تُختار.

**التنفيذ ثلاثة أسطر في `run()`** (فرع `hasHistory`): `qtyRows.push([...,0])` ＋ `finalQtyByRaw = 0` ＋ `zeroDoor[rawKey] = "absent"` — كلها **بلا شرط**. و`zeroDoor` يغذّي **الإخفاء الموحّد** فيُخفى البسيط المنشور، ويُخفى الأب **بالإجماع** لا مباشرةً. و`stripNoChangeQty` يُسقط ما هو 0 أصلاً في زد ⇒ لا استيراد عبثي.

### تحوّل سلوكي غير مقصود لكنه صحيح — حذف رابط يدوي
`matchedHistory` **تتراكم أثناء التشغيل**. فبعد `delMap` يصير الصنف «معروفاً سابقاً وغائباً عن المخزن» ⇒ **يُصدَّر بـ0 ويُخفى** بدل أن يختفي من الملفّين كما كان. مطابق للقاعدة تماماً، لكنّه **غيّر معنى «احذف الرابط»**: لم تعد تراجعاً محايداً بل قراراً بالإخفاء. مشهد `delmapCfg` في المشغّل السلوكي أُعيد تأكيده على النتيجة الجديدة (كمية 0 ＋ `published=No`) — ما زال يثبت فوريّة `run(true)` لكن بالنتيجة الصحيحة.

### القياس (على توزيع المستخدم: 2294 منتج · 603 غائب · 20 انتظار غير مصفَّر)
المجهول الوحيد: كم من الـ603 كميتهم في زد **صفر أصلاً** فيُسقطهم `stripNoChangeQty`.

| مصفَّر أصلاً من الغائبين | يُسقطهم strip | صفوف الملف | منها بصفر | النسبة | الصمّام |
|---|---|---|---|---|---|
| 0% | 0 | 923 | **623** | **27.2%** | يمرّ |
| 25% | 151 | 772 | 472 | 20.6% | يمرّ |
| 50% | 302 | 621 | 321 | 14.0% | يمرّ |
| 75% | 452 | 471 | 171 | 7.5% | يمرّ |

**الحدّ الأعلى 623 صنفاً (27.2%)** — أقلّ من نصف عتبة الصمّام (60%)، فلا يشلّ التشغيل العادي. والرقم الحقيقي يظهر في سطر الملخّص بعد أول تشغيل.

### الحراس (المنطقة كانت بلا حارس إطلاقاً)
- **`absentCfg` سلوكية جديدة** — تمرّر `absent: "keep"` **عمداً**: نجاحُها يثبت أن التصفير صار قاعدة لا خياراً. تغطّي: الضابط المطابَق · الغائب بتاريخ · الغائب المربوط يدوياً · **المصفَّر أصلاً يُسقطه strip** · **المتغيّر الغائب يُصفَّر بلا `published` على صفّه**. مُثبتة الأسنان: خمسة فحوصات ترسب على `079e39e`.
- **خمس مراسٍ بنيوية في `b3-run-regression`** — أُثبت رسوب ثلاث منها على النسخة قبل التغيير.
- **⚠ فخّ في الـfixture:** `AB0` الضابط كانت كميته في زد تساوي المخزن ⇒ `stripNoChangeQty` يُسقطه فيبدو أن التغيير كسر المسار العادي. **كلّ ضابط في تجهيزة الكميات يحتاج فرقاً فعلياً** وإلا سقط بصمت.

## فلتر «سبق ربطه» — كان يعمل ويبدو معطّلاً
**بلاغ المستخدم:** «الفلتر لا يعمل». **القياس نفى العطل الميكانيكي**: النقر يخفّض البطاقات من ٩ إلى ٣ ويعيدها. العطل كان **إدراكياً بالكامل** — وهو عطل حقيقي لا وهم:
1. **عدّادا الشريط لا يتغيّران** (كانا `يحتاج ربط 170 · غير متوفر 243` قبل النقر وبعده) — لا شيء يخبر المستخدم أن شيئاً حدث. العدّادات من الخام **قصداً** (عطل ٢)، فالحلّ ليس تغييرها بل **إضافة مؤشّر للمعروض**.
2. **كل «سبق ربطه» غائب بحكم التعريف** (`wasLinked = isAbsent && manualMap`) ⇒ التفعيل **يُفرِغ أعلى القائمة** وتبقى النتائج في قسم «غائب» أسفل الصفحة خارج الشاشة. المستخدم يرى فراغاً.
3. **«غائب» لم يكن في الشريط أصلاً** — نُقل في الدفعة ٣ إلى ترويسة قسمه، فبقي عدداً غير مرئي قبل التمرير.

**العلاج (ثلاثة، كلٌّ يعالج سبباً):**
- **رقم دقيق بجوار كل فلتر:** `الكل ${needN + absentN}` · `سبق ربطه ${lost}` — الكمّيات بدقّة كما طلب المستخدم.
- **`.uni-shown`: «يعرض X من Y»** عند التفعيل، و`shownN = needCards.length + absCards.length` (**المرسوم فعلاً** لا الخام). وترويسة قسم «غائب» تصير «(٣ من ٤)» عند الفلترة.
- **`toggleLostOnly` يمرّر لأول نتيجة** بعد `flushUnified()` (الرسم التدريجي قد لا يكون أنهى الدفعة الأولى).
- ＋ أُعيدت **شريحة «غائب عن المخزن»** إلى الشريط: ثلاث شرائح لا اثنتان. (تراجع مقصود عن اختزال الدفعة ٣ — العدد ٦٠٣ أهمّ من أن يُخفى خلف تمرير.)

**الدرس:** «يعمل» ≠ «يبدو أنه يعمل». الحراس تُثبت الأولى وحدها. أي فلتر لا يغيّر رقماً مرئياً ولا يُظهر نتيجته في الشاشة الأولى **سيُبلَّغ عنه كعطل** ولو كان منطقه سليماً تماماً.

## ثلاثة أفعال لا يُخلط بينها
خلطُها هو ما جعل حذف الرابط يُخفي منتجاً من المتجر.

| الفعل | ما هو | من يقرّره | الأثر على التصدير |
|---|---|---|---|
| **إلغاء الربط** | **تصحيح** لخطأ ربط | المستخدم، صراحةً | يخرج من الملفّين ويعود إلى «يحتاج ربط». **لا تصفير ولا إخفاء** |
| **غير متوفر** | **قرار إخفاء** واعٍ | المستخدم، صراحةً | `qty = 0` ＋ `published = No` |
| **غائب عن المخزن** | **استنتاج تلقائي** | النظام (كوده ليس في المخزن الموحّد) | `qty = 0` ＋ `published = No` |

**الفارق بين الأخيرين في المصدر لا الأثر:** «غير متوفر» قرارٌ تتحمّله، و«غائب» استنتاجٌ يحتمل الخطأ — ولذلك يحرسه صمّاما الدفعة ٤.

**الفارق بين الأول والأخير حاسم:** كلاهما يترك الصنف بلا كود مخزني، لكنّ **النيّة معاكسة**. «ألغيتُ رابطاً خاطئاً» ≠ «لم يعد لديّ هذا المنتج».

### الآلية: `unlinkedSet` — التمييز في الفعل لا في البيانات
`matchedHistory` **تراكمي ولا يُمسح**: الصنف الذي أُلغي رابطه يبقى فيه، فيبدو للنظام «معروفاً سابقاً وغائباً» ⇒ يُصفَّر ويُخفى. **لا توجد بصمة في البيانات تفرّق بين الحالتين** — الفارق حدثٌ لا حالة، و`dbDelMapping` وحدها تعرفه.

```
dbDelMapping(sku)  ->  unlinkedSet.add(normCode(sku))                 // درع الجلسة
                   ->  matchedHistory.delete(skuN) + saveMatchedHistory()   // الدوام عبر إعادة التحميل ★
                   ->  delete bootLocalMap[raw]                       // يمنع migrateBar من إعادة العرض
                   ->  boundSet.delete(String(sku))
run()              ->  hasHistory = (manualMap[raw] || matchedHistory.has(skuN)) && !unlinkedSet.has(skuN)
```

**`boundSet.delete` ليست تفصيلاً:** `okItem` يستبعد كل ما في `boundSet` من التحليل، فبلا حذفه **يعود الصنف إلى «يحتاج ربط» غير مرئي**. أمسكه الحارس السلوكي (فحص الشارة) لا القراءة.

**★ الثغرة أُصلحت (كانت «مقبولة/مؤجَّلة»):** كان `unlinkedSet` **للجلسة فقط**، فإعادة تحميل الصفحة قبل إعادة الربط تمسحه ⇒ `matchedHistory` يبقى يذكر الصنف ⇒ يُصنَّف «غائباً» ⇒ **يُصدَّر بصفر بصمت** (وقع فعلاً بعد تنظيف ٢٢ رابطاً). **الحلّ الدائم:** `dbDelMapping` يحذف الصنف من `matchedHistory` (＋`saveMatchedHistory` ⇒ يدوم في `HIST_KEY`) — فبعد إعادة التحميل لا «تاريخ مطابقة» ⇒ «يحتاج ربط» لا «غائب» ⇒ لا تصفير. `unlinkedSet` يبقى **درعاً للجلسة** (الحماية الفورية قبل الحفظ). حارس `tests/perf/unlink-persist.mjs` (G-UNLINK-PERSIST، بأسنان): يحاكي إعادة التحميل (يمسح `unlinkedSet`/`manualMap`) ويؤكّد `hasHistory=false` ＋ `saveMatchedHistory` استُدعي ＋ `bootLocalMap` نُظّف؛ `--broken` يزيل حذف `matchedHistory` ⇒ يعود «غائباً» ⇒ يرسب.

**علّة migrateBar (أُصلحت):** بعد حذف الروابط كان `migPending` يعرض «رحّل الروابط (22)» من `bootLocalMap` ⇒ نقرة تُعيد استيراد المحذوف. الآن `dbDelMapping` يحذف من `bootLocalMap` (الذاكرة) فلا تُعرض. **النطاق: عمر الصفحة، لا يُمسح `unlinkedSet` عند «طابق وأنشئ»** — نيّة تصحيح لا قرار يُخزَّن كحالة ثالثة.

**🚨 التبعية الحاسمة — بذور الريبو تعيد البذر (الحلّ المحلي لا يكفي وحده):** `loadConfig` (الإقلاع) يبني `matchedHistory = matched-history.json (ملف الريبو) ∪ HIST_KEY (محلي)` — **اتحاد** (السطر 2584)، و`manualMap = mapping.json (ريبو) ∪ MAP_KEY`. فحذفُ `dbDelMapping` من الذاكرة/HIST_KEY **يُبطَل عند إعادة التحميل** لأنّ **ملف الريبو يعيد البذر** (وقع فعلاً: 16 عادت بعد إعادة التحميل، والغياب ارتفع 9). **الحلّ المطبَّق:** تنظيف **مصدر البذر** — `mapping.json`→`{}` (الروابط في القاعدة، الملف بذرة ميتة سبّبت البانر) ＋ إزالة الأصناف الملغاة من `matched-history.json` في المستودع (دفعُهما ينشر عبر GitHub Pages فيصحّ على كل الأجهزة). **الدرس (تكرّر):** حارس `unlink-persist` الأول كان **جوفاء** — حاكى «إعادة التحميل» بمسح `unlinkedSet`/`manualMap` من الذاكرة **دون إعادة تشغيل `loadConfig`**، فمرّ بينما إعادة التحميل الحقيقية (اتحاد الملف) تُخفق. صُحّح ليحاكي اتحاد `loadSet` فعلاً ويوثّق التبعية. **الحلّ الدائم للمستقبل: ترحيل `matchedHistory` إلى القاعدة (مصدر واحد، بلا اتحاد ملفّ)** — لم يعد تحسيناً اختيارياً بل الإصلاح الجذريّ (بند مفتوح، الدفعة القادمة).

### الزرّ ونصّه
**«إلغاء الربط»** في صفّ الصنف داخل «تم تحديثه» — هناك تراه مربوطاً وتكتشف الخطأ. **لا «حذف»**: الأول تصحيح، والثاني يوحي بحذف المنتج. والتأكيد يقولها صراحةً: «لا يُخفى من المتجر ولا تُصفَّر كميته — هذا تصحيح لا إخفاء».

## المعرّف اليتيم — الفشل الصامت في حاوية محذوفة
**النمط:** يُحذف عنصر من الترميز ويبقى الكود يبحث عنه. الحارس `if (!el) return` يمنع الرمي — **والميزة تموت بصمت**. لا خطأ في السجل، ولا شيء يُبلَّغ عنه إلا حين يشتكي المستخدم.

**تكرّر أربع مرات، اثنتان منها في يوم واحد:**

| المعرّف | الكود الباحث | ما مات |
|---|---|---|
| `#batchBody` | `comboPick` | **كل ربط يدوي** — الاختيار يعرض المرشّح ولا يحدّد الصندوق، فيتخطّاه الاعتماد |
| `#pills` | `renderPills` | **زرّ إلغاء الربط** — فلا سبيل لتصحيح رابط خاطئ |
| `#bindAllBtn` | `applyRoleUI` | إعلان «الربط صلاحية المالك» لغير المالك (＋CSS `body.nonowner`) |
| `#zidMiniInfo` | `updateZidMini` | سطر «بيانات زد: N صنف · منذ X» |

### الحارس `tests/dead-ids.mjs`
أي معرّف **حرفي** يُشار إليه من JS (`getElementById("…")` · `querySelector("#…")` · `setBusy("…")`) ولا وجود له في الترميز ⇒ **رسوب**. ساكن، بلا متصفّح، أجزاء من الثانية. `--broken` يحقن `getElementById("ghost-xyz")` ويؤكّد الرصد.

**قائمة استثناء صريحة `ALLOW`** — كل مدخل بتعليله، **وفارغة اليوم**. والحارس يرسب أيضاً على **استثناء لم يعد لازماً**، فلا تتضخّم القائمة بكتم صامت.

**صفر إنذار كاذب** — النمط الوحيد المولِّد لها (عنصر يُنشأ بـJS ثم يُشار إليه بمعرّفه) يستعمل هنا معرّفات **مُركَّبة** (`"whd-" + uid` · `"s-" + uid` · `"m-" + uid`) والحارس يلتقط الحرفية فقط؛ و`.bt-results-float` يُنشأ بلا `id`.

### ⚠ حدّه — يُعلَن ولا يُخفى
> **يمسك «الحاوية غير موجودة» لا «الحاوية موجودة والمُحدِّد خاطئ». لو بقي `#batchBody` في الترميز بدور آخر لمرّ الحارس. تلك تحتاج السلوكي.**

**حارس يُعلن ما لا يغطّيه أصدق من حارس يُوهم بالشمول.** الغطاء الفعلي لهذا النوع هو `tests/manual-link.mjs` — وهو الذي أمسك العطل أصلاً.

### ملاحظة عن `#zidMiniInfo`: حُذف لا أُحيي
معلوماته الثلاث (العدد · العمر · المصدر) ورابط «تحديث ←» معروضة في **ثلاثة مواضع حيّة**: `#zidAgeHead` مع نقطة `#rsDot` في رأس النتيجة · بطاقة الجاهزية 🏪 (`readyCard`) · `#zidDbAge` في لوحة القاعدة. إحياؤه يضيف رابعاً لمعلومة مكرّرة ثلاثاً. **الأصحّ حذف اليتيم لا إحياؤه متى كانت معلومته مكرّرة.**

## وثائق التحليل تتقادم بصمت
> **أي ملف في `analysis/` يصف الكود يجب أن يُحذف أو يُحدَّث عند أول تغيير بنيوي — لا يُترك مرجعاً كاذباً.**
> **القياس: نسبة المعرّفات المذكورة فيه ولم تعد موجودة.**

**لماذا هي أخطر من الكود الميت:** الكود الميت لا يُنفَّذ؛ أمّا الوثيقة القديمة **فتُقرأ ويُبنى عليها**. وهي **الفخّ نفسه الذي كُشف مرتين**: وصفٌ يصف نيّةً لا سلوكاً.

**القياس السريع** — استخرج المعرّفات بين علامتَي `` ` `` وقارنها بـ`index.html`:

| الملف | معرّفات | لم تعد موجودة | المصير |
|---|---|---|---|
| `session-handoff.md` | 44 | **23 (52%)** | حُذف |
| `system-reference.md` | 418 | 41 (10%) | حُذف — كان يصف الثيم الفاتح و`opts.absent` وعنقود `ignored` |
| `decision-flow.md` | 54 | **17 (31%)** | حُذف — كان يصف `cardHtml`/`bindRow`/`ignoreRow`/`toggleUnpublish` |

**العتبة عملياً: أيّ نسبة > 0 على معرّف جوهري تكفي للحذف.** ملف يصف دالّة محذوفة يقود قارئه إلى طريق مسدود، ولو كان 95% منه صحيحاً.

**البديل:** `CLAUDE.md` هو **الوثيقة الحيّة الوحيدة** — يُحدَّث في نفس الـcommit الذي يغيّر السلوك، فلا يتقادم منفصلاً. وأي تحليل عابر يبقى في المحادثة أو في مجلد مؤقّت، **لا في الشجرة**.

## حادثة «رأس عمود السعر» — فشل اكتشاف عمود يُنتج نتيجة خاطئة تبدو سليمة (2026-09)
رفع المستخدم ملف مستودع رأس عمود سعره لم يُطابق نمط الكشف ⇒ **السلسلة:** رأس السعر غير مكتشَف ⇒ كل الأصناف بلا سعر ⇒ `mergeInventory` يضعها في `noPrice` ⇒ `whRows`/`whIndex` تُبنى من **`unified` (المُسعّر) فقط** ⇒ `whIndex` شبه فارغ ⇒ ~صفر مطابقة ⇒ `matchedHistory` (المتراكمة 1921) تحوّل **1717 صنفاً إلى «غائب»** ⇒ قاعدة الدفعة ٥ (الغائب ⇒ صفر+إخفاء) صفّرت **852 صنفاً** في ملف الكميات. **صفر تحديث حقيقي، ملف كلّه تصفير.**
- **لماذا لم يمسكه صمّام:** بوّابة الرفع (`invShrinkCheck`) تقيس **عدد** الأكواد (لم ينقص)؛ صمّام التنزيل (`zeroRatio` عتبة 60%) كانت النسبة 36% فمرّ. المطابقة «عملت» بتقاطع 38% لكنه وهميّ (على `unified+noPrice`؛ `run` يطابق `unified` وحده).
- **الجذر المزدوج:** (أ) كشف السعر في `detectInvCols` كان يشترط `سامل+ضريب`/`incl` فقط — «السعر» المجرّد/«شامل»/«price» تفشل. (ب) **الـfallback الصامت للأعمدة** (`ci=0`/`qi=1`) يقبل عموداً خاطئاً بلا إنذار — أخطر، فقد يُنتج مطابقات خاطئة تبدو سليمة.
- **الإصلاح (الدفعة أ):** أنماط رؤوس مركزية `HEAD_PATTERNS` (مصدر واحد قابل للتوسّع): السعر = `سامل` · `سعر` بلا `قبل` (مجرّد=شامل) · `price` · `incl`؛ قبل = `قبل+ضريب`/`سعر+قبل`/`excl`؛ الباركود ＋ `باركور` (خطأ «الباركورد»)؛ الكمية بـ`normHead` (ة→ه). و`detectInvCols` يُرجع أعلام `found`+`head`. **حماية الرفع `invColError`+`showUploadReject`:** رأس موجود وعمود جوهري (كود/كمية/سعر · زد=sku) غير مكتشَف ⇒ **رفض صريح في بطاقة الملف نفسها** (`#whStatus`/`#brStatus`/`#stStatus`، صنف `.ustatus.rejected`، **دائم حتى ملف صحيح**) برسالة غنيّة (الملف · الأعمدة المقروءة · الربط · الأسماء المقبولة · «لم يُحفظ — المخزن السابق كما هو») — **قبل أي اعتماد محليّ أو كتابة في القاعدة**. الملف بلا رأس يبقى موضعيّاً (صيغة موثّقة، لا رأس يُضلّل).
- **الدرس:** «فشل اكتشاف عمود لا يُنتج خطأً — يُنتج نتيجةً خاطئة تبدو سليمة؛ فيجب رفض الملف صراحةً في موضع رفعه، لا قبوله وإفساد ما بعده.» والـfallback الصامت للأعمدة الجوهرية خطر بذاته — تُشترَط لا تُفترَض.
- **الحُرّاس:** `tests/inv-cols.mjs` (G1: كشف السعر بمرادفاته ＋ found؛ `--broken` يعطّل قاعدة السعر المجرّد) · `tests/perf/upload-reject.mjs` (G2: عمود مفقود ⇒ رفض غنيّ ＋ حالة البطاقة؛ `--broken` يعطّل فحص السعر). **الدفعة ب (لاحقاً):** صمّام التنزيل «updated=0 ＋ تصفير غيابي ≥20» (G3).
- **⚠ دفعة أ-٢ (عمود الكود «الباركورد»):** بعض أنظمة التصدير تسمّي **عمود الكود** «الباركورد»/«باركود» (لا عمود باركود منفصل). كان `detectInvCols` لا يعرفه كوداً (‏`found.code=false`) ＋ نمط الباركود يلتقطه ⇒ **حارس الرفع (أ) يرفض الملف** ⇒ لا أسعار. **الإصلاح:** في `detectInvCols` بعد فشل الكود الأساسي (كود/رقم صنف/code/sku) — `if (ci<0 && bi>=0) { ci=bi; bi=-1; }` — **الأولوية للكود الأساسي**، فإن غاب يصير عمود الباركود كوداً (و`bi=-1` فلا يُنسَخ الكود في خانة الباركود). مشترك بين مساري المستودع والفرع. **الدرس المكرّر: حارس الوحدة (`inv-cols`) يفحص `detectInvCols` وحدها فيمرّ بالمصادفة على المسار الكامل** — أُضيف `tests/perf/inv-upload-fullpath.mjs` يشغّل `onMergeWh`/`onMergeBranch → bulkUpsert` **بالرؤوس الحقيقية للمستخدم** (الكود=«الباركورد» ＋ العمود الخامس الفارغ) ويؤكّد أن **قيمة** `price_incl` (لا وجود الحقل) تصل الكتابة؛ `--broken` يعطّل قبول الباركود-كوداً فيُرفَض الملف ⇒ يرسب. **مقيس (المسار الحيّ لا الوحدة): الرفعة التي أسقطت الأسعار كانت `index.html` مخبّأً قديماً (pre-Batch-A) — بصمة «سعر null ＋ باركود null ＋ كود/كمية موجودان». APP_VERSION معروضة في «بصمة الجلسة» للتحقّق من التخبئة.**

## القاعدة مصدرٌ وحيد — الترحيل الكامل (H1/H2) وإزالة الرمز الكاتب (2026-09)
تمّ نقل **كل القرارات** إلى Supabase مصدراً وحيداً، وإزالة آخر رمز كاتب للريبو. المبدأ الحاكم: **استبدالٌ لا اتحاد** — القاعدة تستبدل الذاكرة، ولا تُقرأ بذرة من ملف/localStorage في الحيّ.
- **H1 — تاريخ المطابقة (`matched_history`):** جدول SQL (`supabase_matched_history.sql`، نمط `aliases`؛ نُفِّذ يدوياً، **بداية نظيفة count=0** — لا ترحيل للـ2104 المتشبّعة، قرار المستخدم). وحدة `db.matchedHistory` (getAll مُصفَّح · bulkUpsert 500 · remove/bulkRemove). `loadMatchedHistoryFromDB` يستبدل الذاكرة؛ فشل الجلب الأوّليّ ⇒ **`histIncomplete=true`** ⇒ `run` يتوقّف ويعطّل التنزيل (لا تصنيف «غائب» ولا تصفير بلا تاريخ موثوق؛ `showHistIncomplete`). الكتابة: تراكم `run()` دفعةً في النهاية (`histAdded`، غير حاجب، idempotent) ＋ حذف في `dbDelMapping` (‏`db.matchedHistory.remove` — إلغاء الربط دائم عبر إعادة التحميل بلا اعتماد `unlinkedSet` للجلسة). `loadConfig` توقّف عن قراءة `matched-history.json`/`HIST_KEY`. حارس `tests/perf/hist-source.mjs` (G-HIST-SOURCE، خادم فعليّ + `loadConfig` حقيقيّ — لا جوف): يثبت أن الحيّ لا يُبذَر من ملف/localStorage، والاستبدال من القاعدة، و`histIncomplete` عند الفشل.
- **H2 — زيادات الأسعار (`price_offsets`) ＋ إزالة «الحفظ في الريبو»:** جدول SQL (`supabase_price_offsets.sql`). وحدة `db.priceOffsets` · `loadPriceOffsetsFromDB` (استبدال، ＋ترحيل تلقائيّ للبذرة إن كانت القاعدة فارغة). `saveOffset`/`editOffsetInline`/`deleteOffset` تكتب في القاعدة (`dbSaveOffset`، القاعدة أوّلاً). **حُذف بالكامل:** `exportConfig`/`saveToRepo`/`putFileToRepo`/`saveToken`/`b64`/`repoStatus`/`setGhDot`/`toggleGhMenu`+توابعها/`GH`/`TOKEN_KEY`/`lastSavedSig` ＋ قائمة «الحفظ في الريبو» (HTML) ＋ نداء `saveToRepo` في `run`. الرمز يُمسح تلقائياً في الإقلاع. `price-offsets.json` حُذف من الريبو (بعد تأكيد الستّ في القاعدة). حارس `tests/perf/offset-source.mjs` (G-OFFSET-SOURCE). **لم يبقَ رمزٌ يكتب الريبو ولا أيّ كتابة للريبو من الأداة.**
- **H3 — نسخ احتياطيّ يوميّ في ريبو خاصّ منفصل** (`zid-sync-backups`، لا artifacts في العامّ — أرشيفات الريبو العامّ يقدر أيّ مستخدم تنزيلها). `pg_dump` عبر السكربت الرسمي `apt.postgresql.org.sh` (لا `gpg --dearmor` يدويّ في CI — يطلب tty) ＋ **`GITHUB_PATH` لمسار 17** (‏`/usr/bin/pg_dump` وسيطٌ يختار 16 حسب العنقود المحلّي). 13 جدولاً · بوابة صفر · Issue تلقائيّ · ثلاثة مستويات استبقاء (يوميّ 8 · أسبوعيّ 8 · شهريّ دائم — تحلّ «النافذة المتدحرجة») · مفتاحا `force_weekly`/`force_monthly` للاختبار · تحصين دفع (rebase عند التعارض). حساب `backup_ro` (قراءة + BYPASSRLS، بلا دور — RLS تمنع كتابته).
- **تنظيف localStorage:** الإقلاع يمسح المفاتيح الميتة (بياناتها في القاعدة): `zid-gh-token`/`zid-offsets-v1`/`zid-waiting-v1`/`zid-matched-history-v1`/`zid-theme`/`zid-excluded-v1`. **الحيّ الوحيد `zid-options-v1`** (تفضيلات جهاز). كتلة `?debug` «تدقيق localStorage» تسرد المفاتيح وتميّز الحيّ/الميت.
- **دروس اليوم:**
  - **وسمٌ تشخيصيّ يقرأ شرطاً مُلغى يكذب على قارئه:** وسم `?trace` كان يقرأ شرط الحصة القديم فيقول «صُفّر» لصنفٍ نهائيّه>0 — صار يعكس القرار الفعليّ. (كذلك «مصفَّر 25 مقابل صُفّر-لانخفاض 21»: الفرق 4 = مُنقَذون بحصّة 0، إعادة تصنيف بريئة — يُقاس بتفكيك الأصفار حسب الباب لا التخمين.)
  - **طلبُ ملفٍّ محذوف يبقى ضجيجاً في الكونسول يخفي أخطاء حقيقية — إزالة المصدر تشمل إزالة الطلب** (حُذف `fetch("price-offsets.json")` من `loadConfig`؛ البذرة صارت `OFF_KEY` وحده).
  - **`emoji-guard` يفحص القوالب أيضاً:** `⛔`/`⏳`/`🚨` في نصوص `?debug` تُرسبه — أبقِ التشخيص ضمن `↩ ⚠ ⚙ ✓ ✕` أو نصّاً.
  - **قاعدة «الغائب» ذاتيّة التصحيح بقياس حالة زد لا علَمٍ محفوظ** (مطبَّقة أصلاً: `stripNoChangeQty` للكمية ＋ حارس `published!=="yes"` للنشر) — أفضل من علَمٍ لا يلتقط تغييرك اليدويّ في زد. حارس G-ABSENT-ONCE يقفلها.
  - **lowValZero يحكم بإجمالي كود المستودع لا بحصّة المتغيّر** (`codeTotalQty = dist ? dist.total : whQtyReal`) — كودٌ إجماله محترم لا يُصفَّر ولو رقّق التوزيع حصصه (الفروة 33 على 20). حارس G-LOWVAL2.

## الصمّام الجيّد يقول ماذا ولماذا وما العمل — لا «فشل» مجرّدة
> **أيّ صمّام أو حاجز يوقف عملية يجب أن يذكر: ماذا حدث (بالأرقام) · لماذا · وما الإجراء المحدَّد.** «توقّف» أو «فشل» مجرّدة تُربك المستخدم وتُفقده الثقة.

أمثلة مطبَّقة تُحتذى: **تعارض التخفيض** (`renderSaleConflicts`/`verifyPrices`): «السعر الجديد 65 · التخفيض المخزَّن 66 — عدّله في زد» (أوقف 7 قبل أن يرفض زد الملف كله) · **الموحّد المبتور** (`showInvIncomplete`): العدد ＋ السبب ＋ «طابق وأنشئ/حدّث» · **صمّاما التصفير** (`zeroGateOk`/`absentZeroGateOk`): النسبة والأعداد ＋ «راجع أعمدة ملفك» ＋ إلغاء افتراضيّ · **كشف مضاعفة الضريبة** (`vatDoubleCheck`): «السعر 369.57 سيصير 425 (＋15%) — إن كان شاملاً فألغِ» ＋ مثالان · **بوّابة الانكماش** (`invShrinkCheck`): السابق/الجديد/النسبة ＋ العاقبة.

**القاعدة عند بناء أيّ صمّام:** رسالته = *ماذا* (قيَم حقيقية من البيانات، لا «خطأ») ＋ *لماذا* (القاعدة المخالَفة) ＋ *ما العمل* (خطوة قابلة للتنفيذ) · و«إلغاء» افتراضيّ للحاسم · ومثالان حقيقيّان حيثما أمكن. صمّامٌ يقول «فشل» بلا هذه الثلاثة **ناقص** ولو منع الضرر.

## شاشة عرض المبيعات (`#page-sales`) — «مبيعات مقدّرة» من فروق الرفعات
شاشة **عرض فقط** تقدّر المبيعات من نقص كمية الأصناف بين رفعتين لنفس الموقع (نقص = بيع مقدّر · زيادة = شراء · لا نقل بين الفروع). معزولة تماماً عن المطابقة/التصدير — تقرأ `sales_*` و`sales_stock` فقط.

### الجداول والعرض (SQL يدويّ)
- **`public.sales_movements`** (`supabase_sales_movements.sql`): حركة كل صنف بين رفعتين — `sku`/`sku_name`/`location`/`captured_at`/`period_days`/`qty_before`/`qty_after`/`delta` (CHECK `<>0`)/`kind` (`estimated_sale`/`purchase`/`new`/`disappeared`)/`unit_price_incl`/`unit_price_excl`/`price_source`/`value_est`/`upload_id`. تُسجَّل في `recordMovements` **بعد نجاح** `bulkUpsert` (fire-and-forget، لا تعطّل الرفع). صمّام الشذوذ (`moveIsAnomaly`، 8%＋أرضية 10): يسجّل ويوسم `suspect` (لا يتخطّى).
- **`public.sales_uploads`**: رأس كل رفعة (`id`/`location`/`captured_at`/`file_name`/`item_count`/`source`/`note`/`suspect`). RLS: قراءة owner+marketing · كتابة owner+admin (admin **يكتب ولا يقرأ**).
- **العرض `public.sales_stock`** (`supabase_marketing_role.sql`، `security_invoker=false` ＋ `where get_my_role() in ('owner','marketing')`): لقطة المخزون الحاليّ (‏`warehouse_items` ∪ `branch_items`) — `location`/`sku`/`name`/`qty`/`price_incl`/**`price_excl`**/**`barcode`** (كلاهما أُضيف بـ`create or replace` مُلحَقاً آخِراً — الأوّل لـ«قيمة المخزون قبل الضريبة»، الثاني للبحث بالباركود). admin يرى 0 صفّ (الشرط).
- الدور الرابع **`marketing`** (`supabase_marketing_role.sql`): يقرأ `sales_*`/`sales_stock` فقط (سياسات restrictive تعزله عن كل جدول آخر). `canSeeSales() = owner||marketing`.

### القواعد المكتسبة (كلٌّ مُنفَّذ)
- **🚨 المستودع مخزن لا نقطة بيع** — البيع في الفروع فقط؛ النقص من المستودع **سحب** يذهب لوجهات متعدّدة (بعضها خارج النظام)، **فلا يُفترَض أن المسحوب = المستلَم**. نقطة مركزية واحدة تمنع عودة الخلط: `branchLocs = locsAll.filter(l => l !== "wh")` مصدر **كل** مقاييس المبيعات (scope/moved/chart/comparison/detail/tables)، و`invLocsShown` (يشمل المستودع) للمخزون فقط. المستودع خارج: المبيعات · قطع بيعت · متحرّكة · جدول المقارنة · الرسم · الأكثر مبيعاً/الراكد/النفاد · المعدّل/التغطية. وله **قسمه الخاصّ** (`renderWarehouseSection`/`#salesWhSection`): «أكثر أصناف سحباً» ＋ قيمة المخزون ＋ أصناف متحرّكة ＋ «إجمالي الأصناف في المستودع». فلتر «المستودع» ⇒ قسمه فقط (تُخفى مؤشّرات/جدول/رسم المبيعات)؛ «جميع الفروع» ⇒ مبيعات الفرعين ＋ مخزون الثلاثة ＋ قسم المستودع أسفلَ منفصلاً.
- **🚨 فخّ `kind` المخزَّن:** حركة سحب من المستودع تبقى في القاعدة بـ`kind='estimated_sale'` لأسباب تاريخية، **ومعناها الحقيقيّ سحب**. التمييز يقع **عند العرض** بـ`location==='wh'` — **أيّ استعلام مباشر على `sales_movements` يجب أن يستثني `location='wh'` قبل جمع المبيعات**، وإلا قرأ السحب بيعاً. (بند مفتوح: ترحيل إلى `kind='withdrawal'` ليصير المعنى في البيانات لا العرض وحده.)
- **المصطلحات تتبع نوع الموقع:** المستودع نقص=**سحب**/زيادة=**توريد** · الفرع نقص=**بيع مقدّر**/زيادة=**استلام** (عرضٌ بـ`location==='wh'`؛ المخزَّن ثابت).
- **المبيعات «مقدّرة» دائماً لا مؤكّدة** — التسمية «مبيعات مقدّرة» في الكود والواجهة (`estimated_sale`/«قطع بيعت (مقدّر)»).
- **`business_date` = اليوم السابق للرفعة** (الملف يعكس رصيد أمس) — **كل عرض تجاريّ** (الرسم · الفلاتر · المقارنات · اليوم/7/30/سنة) يستعمل `salesBizDate`/`salesBizTs`؛ و`captured_at` **لوقت الرفعة فقط** (`latest`/`lastUp`/المشبوهة/سعر الوحدة/آخر حركة). سطر تحت الرسم يشرحه.
- **كل يوم تقويميّ يُشتقّ بتوقيت الرياض (UTC+3) بالدالّة الموحّدة `riyadhDay`/`salesBizDate`** (‏+3س ثمّ قراءة UTC) — **لا اشتقاق رابع** ولا `slice` على UTC ولا توابع متصفّح القارئ.
- **المقارنات «عن السابق» بمعدّل يوميّ لا بمجاميع** (`salesRateSpan`/`salesRateTrend`، كل موقع بفترته Σ(وحدات÷أيامه))؛ الطولان يظهران عند اختلاف >20%؛ و**رفعة التأسيس** (كل حركاتها `new`) مستبعَدة من كل عدّ/مقارنة/معدّل — فترة سابقة تأسيسية فقط ⇒ «—».
- **موقع بلا رفعة في الفترة** (`uploadedInPeriod`) ⇒ «لا رفعة في هذه الفترة» (لا صفر ولا نسبة)، ومستبعَد من الإجماليّ ＋ صمّام `#salesMissBanner` «الإجمالي يشمل N من M».
- **«أصناف متحرّكة» = كود فريد عبر المواقع** (`movedSku`، منتج في فرعين يُعدّ مرّة)، لا زوج موقع·كود؛ البطاقة العليا = التفصيل = صفّ الإجمالي، ووسم «أصناف فريدة — N في أكثر من موقع» (مجموع الأعمدة − الفريد).
- **لا ضرب/قسمة على 1.15 في هذه الشاشة** — القيَم من `price_incl`/`price_excl` و`unit_price_incl`/`unit_price_excl` مباشرةً؛ الصنف بلا `price_excl` يُستبعَد من مجموع «قبل الضريبة» ويُعدّ (لا صفر بصمت).
- **قاعدة «لا رفعة» تسري على قسم المستودع أيضاً** (البند ١-ج): المستودع بلا رفعة في الفترة ⇒ «لا رفعة للمستودع في هذه الفترة — آخر رفعة منذ X»، لا «0 أصناف متحرّكة» ولا «لا سحب» (صفر كاذب سببه غياب الرفعة لا غياب الحركة). قيمة المخزون وإجمالي الأصناف رصيد حاليّ يبقى. **الدرس المتكرّر: إصلاح عدّاد لا يسري على نظيره — فتّش عن كل قارئ (سرى على الفروع في الدفعة ١ وفات قسم المستودع).**
- **وسم شامل/صافي على كل قيمة ماليّة** (كلمة صريحة لا رمز — تفادياً للبس الضريبة المزدوجة): القيَم بالسعر الشامل تحمل «شامل» (رؤوس أعمدة القيمة في الجداول ＋ بطاقات KPI ＋ شرائط التفصيل)، والقيَم قبل الضريبة «صافي». `.incl-tag` (سانس، خافت، بلا letter-spacing، لا يكسر tabular-nums). **كل القيم المعروضة شاملة عدا أسطر «قبل الضريبة» (صافية).**
- **جدول مقارنة المواقع بلا عمود «التغطية»** (حُذف — بطاقة «متوسط أيام التغطية» العلوية تكفي). أعمدته: الموقع · المبيعات(شامل) · الوحدات · متحركة · قيمة المخزون(شامل) · التغيّر.
- **اسم «أكثر مبيعاً» بعرض كافٍ** (`td.nm width:42%` hint في `table-layout:auto`) فلا ينضغط لأعمدة الأرقام؛ و`.s4-3tables` = `auto-fit minmax(460px)` (تلفّ ٢ ثم ١) لجداول ٦-٨ أعمدة. **الدرس: `min-width` على `td` لا يُحترَم في auto-layout — استعمل `width` hint، وقِس العرض الفعليّ في التخطيط الحقيقيّ لا عرضاً مفترضاً.**
- **بحث الجداول الثلاثة** (`match` في `renderSalesTables`) يفهرس `sku + name + barcode` (فهرس `sku→barcode` من `salesCtx.stock`)؛ حقل فارغ/مسافات ⇒ لا فلترة (`normText` يُنهي بـtrim)؛ باركود `null`/فارغ لا يُدرَج فلا يُطابق شيئاً.
- **سعر التكلفة `cost_price` (دفعة ٢) — شاشة المبيعات حصراً · owner+marketing فقط:** عمود اختياريّ في ملفَّي المستودع والفروع (🚫 لا الحراج · لا زد). **يُخزَّن خاماً بلا أي ضرب/ضريبة** (‏`buildInvDbRows`: `cost_price: numOrNull(r.cost)` — لا يمرّ بـ`applyVat`؛ خليّة 100 ⇒ 100 لا 115). كشف الرأس `HEAD_PATTERNS.cost` (`تكلف`/`cost`/`cost_price`) **مع استثنائه من `incl`/`excl`** (وإلّا التُقط «سعر التكلفة» سعرَ بيع لاحتوائه «سعر»، و«cost_price» لاحتوائه «price»). اختياريّ: ملف بلا العمود ⇒ `null` بلا تصفير سعر البيع. `buildInvDbRows(aggList, branchId, withCost)` — `withCost=false` لـxbranch (جدول الحراج بلا العمود). المزامنة التفاضلية تحتسب تغيّره «محدَّثاً» تلقائياً (‏`updated`=كل كود موجود). **العرض (٣ مواضع):** (أ) «ابحث عن منتج» — عمود «سعر التكلفة» بجوار «السعر شامل الضريبة» في جدول التفصيل · (ب) KPI «قيمة المخزون» صار **«قيمة المخزون (تكلفة)»** (Σ كمية×تكلفة) والسطر الفرعيّ «بسعر البيع بدون ضريبة» (Σ×excl)؛ صنف بلا تكلفة مستبعَد بعدّاد «محسوبة على N من M لهم تكلفة» لا صفراً · (ج) «أكثر 10 راكدة» — عمودا «سعر التكلفة» و«السعر شامل». `null` ⇒ «—» لا صفر. **🔒 الصلاحية في العرض `sales_stock` (`case when get_my_role() in owner/marketing then cost_price else null`)** — لا يصل متصفّح غيرهما · 🚫 لا يدخل ملفَّي زد · 🚫 الدالّة الطرفية لا تختاره (يبقى خارج Gemini هذه الدفعة). SQL **`supabase_cost_price.sql`** (عمود nullable ＋ `create or replace view` بلا drop — للمراجعة). حارس `tests/perf/cost-price.mjs` (G-COST): الكشف/الخام(100 لا 115)/الاختياريّة/الحراج؛ `--broken` يُسقط استثناء التكلفة من incl ⇒ تُلتقط سعرَ بيع ⇒ يرسب. (G-AI-PARITY ⑤: المساعد يبقى incl والشاشة تكلفة — تصفيح كليهما كامل، لا تطابق وحدة.)
- **ترتيب العرض بالكمية تنازلياً ＋ «إظهار المزيد» 10→20 (عرض محض):** جداول الأصناف الثلاثة (أكثر مبيعاً `sellers` / راكدة `raka` / سحب المستودع `whsuhb`) تُعرض **بالكمية تنازلياً** (`salesQtyDesc`: `الوحدات` للمبيعات/السحب · `الكمية` للراكدة). **معيار الدخول (أيّ العشرة) لا يتغيّر** — القائمة تُرتَّب بمعيارها الأصليّ (قيمة/أيام ركود/قيمة سحب) ثمّ **تُقصّ** (`salesCap`: 10 افتراضاً · 20 عند التوسيع، لا «الكل») ثمّ يُعاد ترتيب **الشريحة المقصوصة** بالكمية (فرز مستقرّ ⇒ كسر التعادل بالمعيار الأصليّ). زرّ `salesMoreBtn` (‏`min-height:32px` · `aria-expanded`/`aria-controls`) يظهر فقط حين القائمة > 10؛ التوسيع **قصّ عرض بلا استعلام** (‏`salesToggleAll` يعيد الرسم فقط؛ للمستودع `salesCtx._wh` مخزَّن فيُعاد رسم قسمه). **`nafad` (مخاطر النفاد) لا يُعاد ترتيبه** — ترتيبه دلاليّ (الأقرب نفاداً أولاً = تغطية تصاعدياً)، ويأخذ «إظهار المزيد» فقط. جدول مقارنة المواقع خارج النطاق (مقارنة مواقع لا قائمة أصناف). حارس `tests/perf/sales-sort.mjs` (G-SALES-SORT، بأسنان): الكمية تنازلية في الثلاثة · إظهار المزيد 10→20→10 · صفر استعلام عند التوسيع؛ `--broken` يلغي `salesQtyDesc` ⇒ العمود غير تنازليّ ⇒ يرسب.
- **مؤشّرات المعدّل اليوميّ** (متوسط أيام التغطية · مخاطر النفاد) **مبوّبة «التاريخ غير كافٍ — يلزم 14 يوماً»** ما دام الرصد <14 يوم (`salesCovInsufficient`).
- **النقطة المنفردة في الرسم** (موقع بنقطة واحدة، `polyline` بلا طول) تُرسَم **علامة بارزة** (هالة r=6 ＋ قرص r=4) فلا تختفي وله قيمة؛ والفترة المتعدّدة (`period_days>1`) نقطة مجوّفة موسومة بالنطاق «22–23 سبتمبر» (لا تقسيم كمية).
- **شارة حالة الرفع** (`#uploadStatusBar` في الرئيسية، `renderUploadStatus`): **24 ساعة متدحرجة** (حالتان 🟢/🔴)، **owner+admin فقط**، من طوابع رفع المخزون (لا `sales_uploads`)؛ المتأخّر: «آخر رفعة منذ X · أرقام مبيعاته ناقصة حتى يُرفع ملفه».
- **بطاقة البطل**: «المبيعات المقدّرة» بعرض مضاعف؛ وبطاقات تفصيل التبويب الخمس ⇐ **شريط مضغوط** `s4-dstrip` (جدول المقارنة يفصّل لكل موقع).

### الحرّاس (كلٌّ بأسنان، `tests/perf/`)
- **G-SALES-CORE** (`sales-core`): `salesAgg` — القيمة/الوحدات من `estimated_sale` فقط · المختفي منفصل · بلا سعر معدود لا مُقيَّم · المشبوهة مستبعَدة · الفارغة رسالة.
- **G-SALES-DETAIL** (`sales-detail`): التبويب ＋ «الأكثر مبيعاً» ＋ الراكد/النفاد مبوّبان ＋ عمود سعر الوحدة ＋ البحث يفلتر ＋ المكبّر المنسدل.
- **G-SALES-COV** (`sales-coverage`): التغطية مبوّبة عند رصد <14 يوم.
- **G-MOVED** (`sales-moved`): «متحرّكة» = الأصناف الفريدة (بطاقة=تفصيل=إجمالي) ＋ وسم العابر.
- **G-TREND** (`sales-trend`): «عن السابق» مطبَّعة معدّلاً يوميّاً ＋ الطولان؛ لا مقارنة مجاميع.
- **G-BASELINE** (`sales-baseline`): رفعة التأسيس مستبعَدة من العدّ والمقارنة.
- **G-NOUPLOAD** (`sales-noupload`): موقع بلا رفعة ⇒ «لا رفعة» ومستبعَد من الإجماليّ ＋ صمّام.
- **G-TAX** (`sales-tax`): «قبل الضريبة» للمبيعات من `unit_price_excl` لا قسمةً على 1.15.
- **G-TAX-STOCK** (`sales-tax-stock`): «قبل الضريبة» للمخزون من `price_excl`؛ الناقص مستبعَد بعدّاد لا صفراً.
- **G-TZ** (`sales-tz`): اليوم التقويميّ بتوقيت الرياض (فجر/منتصف ليل يُنسبان لليوم الصحيح).
- **G-BIZDATE** (`sales-bizdate`): العرض على `business_date` (اليوم السابق للرفعة) ＋ الحركات المخزَّنة سليمة (عرض بحت).
- **G-MULTIDAY** (`sales-multiday`): الفترة المتعدّدة نقطة واحدة موسومة بالنطاق (لا تقسيم/عدد).
- **G-SINGLEPOINT** (`sales-singlepoint`): موقع بنقطة واحدة وقيمة>0 له علامة بارزة (لا يختفي).
- **G-YESTERDAY** (`sales-yesterday`): زرّ «أمس» ونافذته = `business_date` أمس (لا يوم الرفعة).
- **G-UPLOAD-STATUS** (`upload-status`): شارة الرفع 24 ساعة · owner+admin فقط · رسالة المتأخّر.
- **G-CMP-LAYOUT** (`sales-cmp-layout`): جدول المقارنة بلا تلاصق محتوى (فجوة Range ≥2px) ＋ وحدات عربيّة ملتصقة ＋ محور الرسم بلا بتر (getBBox داخل viewBox).
- **G-WH-EXCLUDE** (`sales-wh-exclude`): المستودع خارج المبيعات (الإجماليّ=الفرعان · جدول/رسم بلا مستودع · أرقام الفرعين ثابتة) وداخل المخزون (الثلاثة).
- **G-WH-SECTION** (`sales-wh-section`): فلتر «المستودع» ⇒ «أكثر سحباً» ＋ «إجمالي الأصناف» ＋ المخزون · المبيعات مخفيّة · بلا تغطية/نفاد/راكد · وبلا رفعة في الفترة ⇒ «لا رفعة» لا صفر «متحرّكة» ولا «لا سحب».
- **G-SELLERS-WIDTH** (`sales-sellers-width`): اسم «أكثر مبيعاً» ≥120px في التخطيط الفعليّ ＋ رؤوس القيمة «شامل» ＋ جدول المقارنة بلا عمود «التغطية».
- **G-ROLE-SALES** (`role-sales`): عزل الشاشة بالدور (admin بلا شاشة · marketing عليها فقط · بنية SQL).

## دفعة ٣ — إغلاق «شاشة عرض المبيعات» على admin نهائياً (٢٠٢٦-٠٩)
قرار نهائيّ: admin لا يرى الشاشة ولا صفّ بيانات مبيعات واحداً. **الإغلاق طبقتان:**
- **الواجهة (كانت شبه مكتملة):** `canSeeSales()=owner||marketing` (admin مستثنى) ⇒ رابط التبويب مخفيّ (`applyRoleUI`) · `goPage("sales")` محجوب بتوست «🔒 شاشة عرض المبيعات لغير المصرّح» ويبقى في مكانه · `applyRoleUI` يحوّل admin عن sales. **الرابط المباشر `#sales` لـadmin ⇒ يُردّ برسالة ويبقى على الرئيسية (لا شاشة فارغة).**
- **`fillUploadStatus` (صفحة المخزون) — المسار الحاسم:** admin كان يستدعي `renderSalesPage` الكامل (يستعلم sales_movements/sales_stock). الآن **admin يستعلم `sales_uploads` فقط** (`renderSalesLastUp` — حالة «آخر رفعة»)، و🚫 صفر استعلام sales_movements/sales_stock (صفر صفّ مبيعات في استجابة الشبكة، لا مجرّد 0 من RLS)؛ كتل التحليل (`#salesMissBanner`/`#salesBanner`/`#salesDisBox` — تحتاج حركات محجوبة) **تُخفى صراحةً لا تُترك فارغة مُضلِّلة**. عمل الرفع كامل (INSERT sales_movements/uploads + قراءة sales_uploads لـprevSyncedAt).
- **SQL `supabase_sales_admin_close.sql` (الحاجز الفعليّ — للمراجعة، بعد `supabase_cost_price.sql`):** `alter policy` قراءة `sales_movements` → owner+marketing (بلا admin) · `sales_uploads` تُبقي admin (الرفع) · `create or replace view sales_stock` شرطه owner+marketing (بلا admin) **مع إبقاء عمود cost_price** (وإلّا فشل الاستبدال بحذف عمود) · **🚫 لا drop · idempotent · الكتابة INSERT owner+admin لا تُمَسّ** (recordMovements يعمل لـadmin). حارس **G-ROLE-SALES** وُسِّع: admin في `fillUploadStatus` يستعلم `sales_uploads` فقط (صفر sales_movements/stock) ＋ بنية `supabase_sales_admin_close.sql` (owner+marketing قراءةً، admin في uploads، العرض بلا admin مع cost_price). **حدّ مُعلَن: منع admin فعلياً في القاعدة (0 صفّ) يُثبَّت بالاستعلام اليدويّ بحساب admin — لا يُختبَر بالمتصفّح.**

### درسان (تخطيط الجدول والرسم)
- **«أُصلح بلا حارس = لم يُصلَح»** — تغيير CSS بلا فحص يمرّ في التقرير ويسقط في اللقطة الحيّة. (حدث في ج-١: حشو 12px ＋ tabular بلا حارس؛ الجذر كان بنيويّاً — نسبة `s4-mid` معكوسة ＋ mono يقطّع «يوم» — فلم يُصلَح شيء. أيّ بند بصريّ يُعلَن منجَزاً بلا حارس يفحص ظاهرته = غير منجَز.)
- **قِس الفجوة بالمحتوى (`Range`) لا بصندوق الخليّة** — الحشو داخليّ فصناديق `<td>` متلاصقة دائماً (فجوة 0) ويخدع الحارس بنجاح زائف؛ التلاصق الحقيقيّ بين **نصّي** خليّتين يُقاس بـ`range.selectNodeContents(td).getBoundingClientRect()`. ونظيره في الرسم: البتر يُقاس بـ`getBBox` داخل حدود viewBox، لا بوجود النصّ (‏`textContent` موجود دائماً ولو كان مقصوصاً بصرياً).

## مساعد المبيعات الذكيّ — Edge Function `ai-assistant` (Gemini) — أوّل اتصال خارجيّ
مساعد سؤال-وجواب بالعربية في شاشة عرض المبيعات، **يقرأ من القاعدة لا من الشاشة**. **owner و marketing** (الدفعة ب، ٢٠٢٦-٠٩: كلاهما يرى شاشة المبيعات فالمساعد واجهة أخرى لنفس المعطيات لا كشفٌ لبيانات جديدة؛ admin خارج الشاشة أصلاً فخارج النطاق). كل جداول الدالّة (`branches`·`sales_uploads`·`sales_movements`·`sales_stock`) مسموحة لـmarketing بـRLS (رُوجعت واحداً واحداً). الطبقة المجانية من Gemini حصراً (🚫 لا مدفوع ولا fallback مدفوع).
- **المعمارية** ([supabase/functions/ai-assistant/](supabase/functions/ai-assistant/)): المتصفّح ⇒ سؤال ← تحقّق دخول ← **owner** (`get_my_role`) ← **سقف يوميّ ذرّيّ** (`ai_usage_bump`) ← **تصنيف** (Gemini #1 ⇒ JSON نيّة+معاملات) ← **تحقّق allowlist** (بنية لا تعليمات) ← **نافذة البيانات** ← **نيّة ثابتة** (`sales_compute.mjs`) ← **صياغة** (Gemini #2). 🚨 Gemini لا يملك مفتاح القاعدة ولا اتصالاً بها ولا يولّد SQL ولا يختار جدولاً.
- **الصلاحية: JWT المستخدم (RLS يُطبَّق)** — 🚫 صفر `service_role` لبيانات المبيعات. العميل يُنشأ بهيدر `Authorization` الممرَّر، فقراءة owner تمرّ بـRLS (جدار ثانٍ لو أخطأ منطق نيّة).
- **النوايا الإحدى عشرة** ([intents.mjs](supabase/functions/ai-assistant/intents.mjs)): `sales_summary · top_sellers · bottom_sellers · product_movement · period_comparison · location_comparison · stagnant_inventory`(مبوّبة <14ي) `· stockout_risk`(مبوّبة) `· inventory_value`(يشمل المستودع) `· data_freshness · biggest_decliners`(«لماذا نزلت»). المعاملات المسموحة: `period · location · product · limit` (الأقصى 200). **لا يغطّي:** التوريد/الجديد/المختفي (بقرار) — والردّ يقول ما يستطيع. **مطابقة المنتج جزئية** (اسم＋كود＋باركود)؛ أكثر من صنف ⇒ قائمة تأكيد (بلا Gemini ثانٍ).
- **الجداول المسموحة أربعة حصراً:** `sales_movements · sales_uploads · sales_stock · branches`. `user_roles` للتحقّق الأمنيّ فقط (🚫 لا يدخل سياق Gemini). 🚫 لا بُرد/معرّفات/أدوار تُرسَل إلى Gemini. **ما يغادر إلى Google** موثّق سطراً فوق نداء الصياغة في [index.ts](supabase/functions/ai-assistant/index.ts): «نتيجة الاستعلام المجمّعة ＋ أسماء الأصناف · 🚫 لا بُرد ولا معرّفات ولا أدوار ولا سؤال مخزَّن».
- **🚨 المساعد يطبّق قواعد الشاشة حرفياً** (لا قاعدة جديدة، عبر `sales_compute.mjs`): المستودع مستبعَد من المبيعات · business_date بالرياض · التأسيس/المشبوهة مستبعَدان · معدّل يوميّ · `price_incl/excl` بلا 1.15 · موقع بلا رفعة ⇒ «لا رفعة» لا صفر.
- **🚨 نقص التغطية بيانٌ لا رفض (تصحيح):** فترة أطول من المرصود ⇒ يُعطى **رقم المرصود نفسه ＋ `coverage_shortfall`** («يخصّ المرصود X يوم لا الأسبوع كاملاً») — 🚫 لا رفض؛ وإلّا خالف الشاشة (تعرض الرقم لنفس الفلتر). المنطق في **طبقة النيّة** (`runIntent`/`coverageShortfall` في [intents.mjs](supabase/functions/ai-assistant/intents.mjs)) لا في `index.ts` — ليختبره G-AI-PARITY فعلاً. يسري على **كل النوايا**. الرفض يبقى فقط عند **صفر رفعات في النطاق** (‏`no_upload`). ⚠ مستقلّ عن **بوّابة الجودة** (راكد/تغطية/نفاد <14 يوم) — تلك حجبٌ مختلف يبقى.
- **🚨 «تكافؤ مُختبَر» لا «مصدر واحد»:** الشاشة (`index.html`) **لا تستورد** `sales_compute.mjs` — هو نسخة موازية. أيّ تعديل على قواعد الحساب في `index.html` **يلزمه تعديل مقابل في `sales_compute.mjs`**، و**G-AI-PARITY هو ما يكشف النسيان** (لا ضمان بنيويّ).
- **العدّاد** ([supabase_ai_usage.sql](supabase_ai_usage.sql)، يُنفَّذ يدوياً بعد `user_roles`): جدول `ai_usage(user_id, usage_day, used_count)` PK مركّب. **🚨 السقف مصدره واحد: `ai_daily_cap()`** (‏`500` — غيّره هناك وحده؛ تقرؤه الزيادة والحالة والواجهة عبر `meta.cap`، 🚫 لا رقم ثابت في `index.html`). `ai_usage_bump()` **security definer، بلا معاملات** (تقرأ `auth.uid()` داخلياً — 🚫 لا `user_id` معاملاً)، السقف من `ai_daily_cap()` (🚫 لا معامل عميل)، **زيادة ذرّية** (`INSERT … ON CONFLICT DO UPDATE … WHERE used_count < cap` — جملة واحدة، فطلبان متزامنان لا يقرآن 19 معاً)، **تمسّ `ai_usage` وحده**. `ai_usage_status()` قراءة غير مستهلِكة `(used, cap)` للواجهة (الرصيد عند الفتح بلا زيادة). يوم الرياض (`utc+3`). **لا سياسة كتابة للعملاء** ＋ `revoke execute from public` قبل المنح. 🚫 لا يخزّن سؤالاً/جواباً/بيانات أعمال.
- **🚨 تعداد فروع الدالّة = فروع زد ＋ الحراج (الدفعة ٥):** `index.ts` يبني `branchList = branches(جدول) ＋ SALES_EXTRA_LOCS` (المصدَّر من `sales_compute.mjs`، يوازي `salesAllLocs` في الواجهة) — فـ`scopeLabel`/`computeScope`/`inventory_value` تشمل الفروع الأربعة (قيمة المخزون تشمل الحراج). فروع زد من الجدول تلقائياً؛ فرعا الحراج ثابتان في `SALES_EXTRA_LOCS` (عدّلهما هنا وفي index.html عند إضافة فرع مبيعات). **المستودع يبقى مستبعَداً من المبيعات** داخل `computeScope`. **الصياغة تمرّ بـ`geminiRobust`** (إعادة ＋ احتياطيّ) كالتصنيف. **النوت مشتقّ من النطاق لا مسار الردّ** (`answerNote` واحد للناجح والمتدهور — حارس **G-AI-NOTE**). ⚠ **بند مفتوح (تشخيص):** «آخر 30» أكبر من «كامل البيانات» — اشتقاق النطاق سليم (all ⊇ أي نافذة)، فالفرق من تصنيف مختلف أو تغيّر بيانات بين السؤالين، لا من الكود (بلا إصلاح حتى يُحدَّد بالتسجيل).
- **🚨 مرونة ازدحام جوجل (503 UNAVAILABLE):** `geminiRobust` — ٣ محاولات على الأساسيّ (فوريّة · ~1s · ~3s ＋ jitter) **على 503 فقط** (🚫 لا إعادة على 400/401/403/404 — خطأ دائم)، ثم **موديل احتياطيّ مرّة** (`GEMINI_MODEL_FALLBACK`، افتراضه المضمَّن `gemini-3.1-flash-lite` — حيّ؛ الازدحام غالباً لموديل بعينه). **السقف يُحتسَب بعد نجاح التصنيف فقط** (‏`ai_usage_bump` نُقل بعد Gemini #1 — 🚫 لا خصم على فشل جوجل). **تدهور رشيق:** فشل التصنيف بعد كل المحاولات ⇒ **200 ok:false** «خدمة المساعد مزدحمة مؤقّتاً» (لا 502 يُظهر خطأ اتصال ولا «تعذّر تحليل السؤال» الموحِمة بخلل السؤال) · **فشل الصياغة (بعد نجاح التصنيف) لا يُفشل الطلب** — المقاييس تُحسب في الخادم فتُعرض مع سطر تمهيديّ ثابت «تعذّرت صياغة الشرح الآن، وهذه الأرقام كما حُسبت:» (وكذلك عند تلفيق رقم في النثر: يُسقَط النثر وتبقى مقاييس الخادم). **تشخيص:** `logGeminiFail` يسجّل `upstream_status·model·attempts·fallback·keyLen·body(300)` (🚫 لا مفتاح/بُرد/سؤال كامل)، وردّ الخطأ يحمل `upstream_status`＋`fail_stage` (http/parse). **429 (الحصّة):** `geminiCall` يبقي سلوكه `{ quota: true }` (بلا إعادة/احتياطيّ — يعود فوراً)، **لكنه يلتقط جسمه أوّلاً** (كان يُرمى): يقرأ `resp.text()` مرّة ويسجّل `quota(429): model·keyLen·retryDelay·details·body(500)`. 🚨 اسم الحصّة في `error.details[]` لا في `message` (‏`QuotaFailure.quotaId` مثل `GenerateRequestsPerDayPerProjectPerModel` · `RetryInfo.retryDelay`)، فـ`details` يُسجَّل **كاملاً** و`retryDelay` **مفصولاً** (ثوانٍ ⇒ RPM · ساعات ⇒ RPD). ردّ الحصّة صار يحمل `upstream_status: 429`. حارس `tests/ai-quota-log.mjs` (G-AI-QUOTA-LOG، بأسنان): يحاكي 429 بجسم معروف ويثبت السجلّ يحمل `quotaId`＋`retryDelay`＋المقتطف بلا مفتاح/سؤال؛ `--broken` يعيد الإرجاع بلا قراءة ⇒ سجلّ بلا جسم ⇒ يرسب.
- **🚨 درسان (ازدحام/سحب الموديلات):** **«موديلات Gemini تُسحب بلا إشعار — عند 404 اقرأ نصّ رسالة جوجل، فهي تسمّي البديل الموصى به»** (‏`gemini-2.5-flash` رُدّ عليه 404 «no longer available… use models/gemini-3.6-flash»). **«سجّل حالة المزوّد وجسمه دائماً عند الفشل — بدونه ضاعت ساعات في التخمين»** (‏`upstream_status`＋`body` في `logGeminiFail` هما ما حسم 503 vs 404). الأسماء من الأسرار لا الكود؛ الافتراضيّ المضمَّن يبقى حيّاً.
- **الحدود المالية:** المفتاح في **Edge Function Secrets** (`GEMINI_API_KEY` ＋ `GEMINI_MODEL` — الاسم من سرّ لا من الكود، افتراضيّ `gemini-1.5-flash`). 🚫 لا في المتصفّح/الريبو/جدول. مفتاح في مشروع Google **بلا حساب فوترة** (تجاوز الحصّة ⇒ 429 لا فاتورة). 🚫 لا إعادة محاولة تستهلك الحصّة. **بلوغ السقف** ⇒ «وصلت إلى الحدّ اليومي (20)» · **429** ⇒ «وصل المساعد إلى حدّ Gemini المجاني — استُهلكت محاولة (N/20 متبقية)» (لا فشل صامت).
- **الواجهة** ([index.html](index.html) `renderSalesAI`/`salesAskAI`): حقل السؤال في `#salesAI` **يُنشأ لـowner و marketing** (🚫 لا مخفيّ بـCSS — `!(myRole==="owner"||myRole==="marketing") ⇒ innerHTML=""` قبل البناء؛ admin/viewer لا يُنشأ لهما فيأخذ البحث العرض الكامل). **بوّابة الدور في الدالّة الطرفية نفسها** (`role !== "owner" && role !== "marketing" ⇒ 403`) هي الحاجز الفعليّ لا الواجهة. **الرصيد معروض دائماً** (`#saiQuota` «N من cap سؤالاً اليوم») **والسقف من القاعدة** (`ai_usage_status` عند الفتح · `meta.cap` بعد الجواب) — 🚫 لا رقم ثابت في الواجهة. **سطر «⚠ تحليل آليّ — تحقّق من الأرقام بنفسك» تضيفه الواجهة برمجياً** تحت كل جواب (لا النموذج). **مخرَج النموذج معقَّم نصّياً** (`aiPlainText` يزيل Markdown خام) ثم `esc()` — 🚫 لا HTML من النموذج. النداء عبر `sb.functions.invoke("ai-assistant")`.
- **التنسيق والوحدات (مصدرها الخلفية):** كل رقم يخرج من `runIntent` مُنسَّقاً بفواصله ووحدته في حقول `display`/`label` جاهزة (`applyDisplay`) — 🚫 النموذج لا يُنسّق ولا يختار وحدة (فيخلط «قطعة/حبة»). **المصطلحات تطابق الشاشة:** المبالغ «ر.س» ＋ «شامل»/«صافي» · الكميات «قطعة» · الأصناف «صنف» · الأيام «يوم».
- **JSON منظّم (لا نصّ حرّ):** النموذج يُرجع **نثراً فقط** `{lead, warning, note}` (‏`responseMimeType:json` ＋ `stripFences`)؛ كسر التحليل ⇒ «تعذّرت صياغة الجواب — حاول مرة أخرى» ＋ تسجيل (🚫 لا شاشة فارغة). الواجهة (`renderAiStructured`) ترسمه بنمط النظام — **كل حقل `esc()` ثمّ `aiPlainText`** (🚫 لا HTML من النموذج؛ التعقيم حارس أخير لا آلية عرض).
- **🚨 المقاييس سلطة الخلفية لا النموذج (لا تكرار وحدة):** `applyDisplay` يبني `res.metrics = [{label, value, unit}]` — **value رقم بفواصله وحده، unit منفصلة** (ر.س شامل/صافي · قطعة · صنف · يوم · %). الخلفية تجمعها في `structured.metrics` (النموذج **لا يُخرج metrics**) فينتفي تكرار الوحدة (كان «26,816 ر.س شامل | ر.س») والتلفيق في البطاقات معاً. الواجهة ترسم label·value·unit منفصلة. **حارس G-AI-METRIC-UNITS** (value رقم وحده؛ `--broken` يدمج ⇒ يرسب).
- **🚨 تحقّق الأرقام بنيوياً (`verifyAnswerNumbers`):** كل رقم في جواب النموذج (‏`lead`＋`metrics`＋`warning`＋`note`) له أصل حرفيّ في نتيجة الاستعلام (`collectSourceNumbers`، تطبيع الأرقام العربية والفواصل)؛ رقم بلا أصل ⇒ **يُرفض الجواب كلّه** «احتوى رقماً لا أصل له — أُلغي» (🚫 لا يُعرَض ولو بدا معقولاً). يحوّل «لا تخترع رقماً» من تعليمة إلى فحص لا يُكسر. **حارس G-AI-NUMBERS**.
- **🚨 الفصل الدلاليّ (`enforceCoverageLead`):** إن كان المرصود < المطلوب ⇒ الجواب **يبدأ** بالنقص بنيوياً «البيانات تغطّي X يوم من Y المطلوبة. خلال المرصود: …»، والأرقام للمرصود لا المطلوب، 🚫 بلا استكمال بالتقدير. **حارس G-AI-COVERAGE-WORDING**.
- **🚨 لا قيمة تقنية في نصّ المستخدم:** مسمّيات بشرية (`periodLabel`/`scopeLabel`): «أمس · آخر 7 أيام · آخر 30 يوماً · آخر سنة · كامل البيانات المتاحة» · «الفروع (الخضرة + العزيزية)»/«الخضرة»/«المستودع» — 🚫 لا `all` ولا اسم نيّة ولا مفتاح معامل. **كل جواب يذكر نطاقه الجغرافيّ** (`scope_label`).
- **🚨 وسم «all» صادق حسب نوع المقياس (٢٠٢٦-٠٩):** `scopeLabel(location, branches, includesWh)` — «all» للمخزون (‏`invLocsShown` يشمل wh) ⇒ «كل المواقع (المستودع + الفروع)» · للمبيعات (wh مستبعَد) ⇒ «الفروع (…)». `index.ts` يحسب `answerIncludesWh = location==="all" && كل النوايا غير مبيعات`. **العلّة المُصلَحة:** كان يسمّي «all» «الفروع» دائماً بينما رقم المخزون يشمل المستودع (تناقض 11,198,982 موسوماً «الفروع»). ＋ مقياس `inventory_value` يحمل وسم «(يشمل المستودع)» كالشاشة.
- **🚨 التنويه (`answerNote`) من نوع المقياس لا مسار الردّ:** مبيعات ⇒ «مقدّرة لا مؤكّدة · المستودع مستبعَد» · مخزون ⇒ «يشمل المستودع» (بلا ذكر استبعاد المبيعات — السؤال عن مخزون). **مصدر واحد فقط:** حُذفت أنوات التنويه المكرّرة من نتائج النوايا (‏`sales_summary`/`top`/`bottom`/`period_comparison`/`biggest_decliners`/`inventory_value`) — كانت تُرسَل للنموذج فيُصداها في `warning` ⇒ سطران متطابقان. تحذير التأسيس الفريد بقي (بلا «مقدّرة+مستبعَد» معاً). حارس **G-AI-NOTE** وُسِّع بعدّ سلاسل «مقدّرة…مستبعَد» المقتبسة عبر الملفّين (يجب == 1؛ `--broken-dup` يعيد نوت المبيعات ⇒ 2 ⇒ يرسب).
- **✅ نطاق «الفروع فقط» — `location="branches"` (٢٠٢٦-٠٩، بإذن المستخدم):** **قيمة موقع جديدة لا نيّة** — `location` توسّع من `all|wh|<فرع>` إلى `all|branches|wh|<فرع>` (النوايا الإحدى عشرة كما هي). الفرق الوحيد عن «all» في `computeScope`: `invLocsShown` — «all» يشمل المستودع (`locsAll`) · «branches» يستبعده (`branchLocs`). **المبيعات ثابتة:** `salesShown = branchLocs` لـ«all» و«branches» معاً (المستودع مستبعَد من المبيعات أصلاً) ⇒ **صفر تغيير في أي رقم مبيعات**. `scopeLabel("branches")` = «الفروع (…) — بلا المستودع»؛ ومقياس مخزونها بلا وسم «(يشمل المستودع)» (‏`includes_warehouse=false`)؛ و`answerNote` لها «الفروع فقط (بلا المستودع)». التصنيف يكشفها من «مخزون الفروع/الفروع بس/بدون المستودع/الفروع الأربعة» (‏`classifyInstruction`)؛ 🚨 **المبيعات تبقى all** (لا branches لسؤال مبيعات). **التوجيه بلا انحدار:** `locRaw⇒location` لكل القيم القائمة ثابت (all/wh/كل فرع)، و«branches» وحدها الجديدة. حارس **G-AI-BRANCHES** (بأسنان): branches=الفروع فقط · all يشمل wh · القيم القائمة ثابتة · مبيعات branches==all · لا وسم wh على branches؛ `--broken` يُسرّب wh إلى `invLocsShown` لـbranches ⇒ ينتفخ ⇒ يرسب.
- **🚨 فصل الاسم عن الأكواد (`cleanName`):** يحذف ذيل قائمة الأكواد (أرقام ≥5 خانات مفصولة بفواصل/نقاط) ويُبقي الأوصاف (`120*200`·`8ك`·`ش14`·رقم مفرد) — تنظيف محافظ، والأصل يبقى في `name`/`original` (لا نفقد معلومة). **حارس G-AI-NAMES**.
- **🚨 التصريح تحت كل جواب (القيد ⑤):** «⚠ تحليل آليّ — تحقّق من الأرقام بنفسك» تضيفه الواجهة **خارج صندوق الجواب** لكل ردّ `ok` **غير مشروط** (‏`if (data && data.ok)` — لا يشترط `structured` ولا وجود مقاييس). **سبب الغياب سابقاً:** كان مقروناً بـ`data.ok && data.structured`، فالردّ المرفوض (‏`ok:false` من تحقّق الأرقام حين كان النموذج يبني المقاييس) يسقط للفرع `else` بلا تصريح؛ ونقل بناء المقاييس للخلفية أزال ذلك الرفض. **حارس G-AI-DISCLAIMER** يغطّي 4 أشكال (مفرد · مركّب · بلا مقاييس · مع تحذير)؛ `--broken` يقرن التصريح بالمقاييس ⇒ يرسب. **الدرس: الحارس القديم فحص حالة واحدة فمرّ رغم الغياب.**
- **الأسئلة المركّبة (٩):** التصنيف يُرجع `intents[]` (بحد **5**، الأنسب عند التجاوز)؛ كل نيّة تُتحقَّق من allowlist وتُشغَّل، وفي المركّب تُلخَّص (`summarizeResult`: أعلى 3 ＋ إجماليات — 🚫 لا آلاف الصفوف إلى Gemini). **فتحة عدّاد واحدة** (نداء صياغة واحد). 🚫 لا SQL مولّد · لا جداول جديدة · لا توسيع مسموح. **بوّابات الجودة تبقى** (الراكد/النفاد <14ي ملاحظةٌ داخل الملخّص، لا تتسرّب). **G-AI-PARITY يمتدّ للمركّب** (الفحص ④: إجماليّ `location_comparison` == الشاشة؛ `--broken` يُدخل المستودع في المفرد والمركّب معاً ⇒ يرسب).
- **العزل:** حذف المساعد كلّه لا يمسّ الشاشة ولا المطابقة/التصدير. الكتابة الوحيدة = عدّاد الاستخدام (بلا سؤال/جواب/بيانات أعمال).
- **🚨 النشر بملفّ واحد مُولَّد آلياً (بعزل نطاق):** محرّر Supabase يُنشئ نسخة ثانية عند إضافة اسم موجود (تكرّرت الملفات مراراً). لذا `scripts/build-edge-bundle.mjs` يدمج المصادر الثلاثة في **`ai-assistant.bundle.ts`**. **🚨 كل وحدة في IIFE مستقلّة تُصدّر ما يحتاجه الباقي فقط** (esm.sh أعلى · `const __m_sales_compute=(()=>{…return{…}})()` · `__m_intents` يفكّ ما يستورد منها · index IIFE تنفيذيّة) — **لا لصق متسلسل في النطاق العامّ**، فلا تصادم أسماء داخلية بين الملفات مهما تكرّرت. **المصدر يبقى ثلاثة ملفات** (الحرّاس تستوردها)، **والمنشور واحد يُلصَق في `index.ts` ثم Deploy — 🚫 بلا Add File**. ⚠ `index.ts` مكتوب **JS نقيّ (بلا أنواع TS)** ليعمل في Deno ويُقلع محلّياً في node.
- **🚨 حارس G-EDGE-BUNDLE يُقلع المدمج فعلاً لا يطابقه فقط** (`tests/edge-bundle-sync.mjs`، بأسنان): ① مطابقة المصادر · ② صفر `import` محلّيّ (‏`./`) · ③ **إقلاع فعليّ** (يُستورَد في node بعد استبدال esm.sh وتهيئة Deno) ⇒ صفر خطأ صياغة/تصادم/نطاق. `--broken` يحقن تصريحاً مكرّراً ⇒ الإقلاع يرمي «already declared» ⇒ يرسب (يمسك عطل `payload` الذي مرّ على المطابقة الصامتة) · `--broken-sync` انحراف عن المصدر ⇒ يرسب. **الدرس المتكرّر: «الضمان البنيويّ يثبت أن الكود لم يُحذف لا أنه يُقلع» — المطابقة وحدها مرّت على انهيار الإقلاع.**
- **الحارسان:**
  - **G-AI-PARITY** (`tests/perf/ai-parity.mjs`، **أهمّ حارس**، بأسنان، ٤ فحوص): ① رقم المساعد (`computeScope`) = رقم الشاشة بالضبط (36,588، المستودع مستبعَد) · ③ **فترة أطول من المرصود** ⇒ نفس رقم الشاشة ＋ `coverage_shortfall` لا رفضاً · ④ **المركّب** (‏`location_comparison`) إجماليّه == الشاشة. `--broken` يكسر استثناء المستودع ⇒ المفرد والمركّب 56,268 ≠ الشاشة ⇒ يرسب · `--broken-window` يُعيد الرفض القديم ⇒ الفحص ③ بلا رقم ⇒ يرسب. **يبقى في CI دائماً — يكشف انحراف الشاشة عن `sales_compute.mjs`.**
  - **G-AI-NUMBERS** (`tests/ai-numbers.mjs`، بأسنان): جواب أرقامه من المصدر يُقبل · رقم مُلفَّق يُرفض ويُرصد. `--broken` يُعطّل التحقّق ⇒ المُلفَّق يمرّ ⇒ يرسب.
  - **G-AI-COVERAGE-WORDING** (`tests/ai-coverage-wording.mjs`، بأسنان): يبدأ بالنقص ثم «خلال المرصود» · بلا نقص لا يُعدَّل. `--broken` يُعطّل البادئة ⇒ يرسب.
  - **G-AI-NAMES** (`tests/ai-names.mjs`، بأسنان): الأكواد تُحذف · الأوصاف (`120*200`/`8ك`/`ش14`/رقم مفرد) تبقى · الأصل محفوظ. `--broken` تنظيف نهم يُتلف وصفاً ⇒ يرسب.
  - **G-AI-SCOPE** (`tests/perf/ai-scope.mjs`، بأسنان): **owner و marketing** يريان الحقل · **admin/viewer لا يُنشأ لهما** · **الدالّة الطرفية نفسها ترفض غيرهما (403)** لا الواجهة وحدها (فحص ساكن على index.ts) · الواجهة تستدعي `ai-assistant` وتقرأ `ai_usage_status` فقط (لا كتابة/جدول أعمال/مساس بالمطابقة). `--broken` يُنشئ الحقل لكل الأدوار ⇒ admin/viewer يريانه ⇒ يرسب.
  - **G-AI-CAP** (`tests/perf/ai-cap.mjs`، بأسنان): سطر الرصيد يتبع السقف من القاعدة (‏`ai_usage_status` عند الفتح · `meta.cap` بعد الجواب؛ cap=500 ⇒ «496 من 500»)، 🚫 لا «20» ثابت. `--broken` يُثبّت 20 في `salesAIQuotaText` ⇒ «من 20» رغم cap=500 ⇒ يرسب.
  - **حدّ مُعلَن:** منع admin/الكتابة **فعلياً في القاعدة** (RLS ＋ security definer) يحتاج Supabase حيّاً — لا يُختبَر بالمتصفّح. الحارسان يغطّيان الواجهة والنطاق الساكن؛ منع القاعدة يُثبَّت بالاستعلام اليدويّ (نمط G-ROLE-SALES).

## فرعا شاشة المبيعات المقدّرة فقط (الحراج) — معزولان عن زد  ⚠️ SQL يدوي (`supabase_sales_branches.sql`)
فرعان إضافيّان **لشاشة المبيعات وحدها** (مطابقة نقص موقع بزيادة آخر — تمييز التحويل عن البيع): **الحراج مفروشات** (`haraj_maf`) · **الحراج رحلات** (`haraj_reh`). 🚨 **لا علاقة لهما بمزامنة زد الآن.**
- **🚨 العزل التامّ (القيدان ١/٢):** خارج جدول `branches` ⇒ خارج `invBranches` ⇒ خارج `loadInventoryFromDB`/`mergeBranches`/`mergeInventory`/`run()` ⇒ **لا يمسّان ملفَّي الكميات/الأسعار المولَّدَين لزد إطلاقاً.** رمزاهما **سلسلتان** (`haraj_maf`/`haraj_reh`) — ليستا uuid فلا تطابقان `branches.id` (حارس بذاته). ثابتتان في `SALES_EXTRA_LOCS` (لا جدول سجلّ).
- **الجدول `sales_branch_items`** (SQL في `supabase_sales_branches.sql`، بعد `supabase_marketing_role.sql`): نفس أعمدة `branch_items` ＋ `zid_scope boolean default false` (خامل للإدراج المستقبليّ) · **فارق مقصود:** `branch_id` نوعه `text` (بلا FK، `CHECK` على الرمزين) لا uuid. مفتاح مركّب `(branch_id, code)`. RLS: قراءة موثّق · **كتابة owner حصراً** ＋ restrictive تمنع marketing من الجدول مباشرةً (يقرأ عبر العرض). العرض `sales_stock` وُسِّع بـ**`create or replace`** (لا drop — يحفظ صلاحية `backup_ro`) ليضمّ اتحاداً ثالثاً؛ الشرط `get_my_role() in ('owner','marketing')` و`security_invoker=false` محفوظان.
- **مسار الرفع (`xbranch`):** `INV_TABLE.xbranch = "sales_branch_items"`. `onMergeSalesBranch` (بطاقة `data-xbranch`) ⇒ `syncInventoryToDB("xbranch", …, code)` — **يمسّ `sales_branch_items` وحده:** 🚫 لا `invMeta`/`branches`/`resyncNeeded`/سجلّ نشاط زد/`loadInventoryFromDB`/`autoAdopt`. `recordMovements` يعطي `location = code` (‏`sales_movements` بلا قيد على location). `prevSyncedAt` من آخر `sales_uploads` للموقع. `db.inventory` عرف `sales_branch_items` كجدول فرع (‏`branch_id` في `getAll`/`onConflict`). ميتا العرض (عدد/آخر رفعة) مشتقّة من القاعدة في `db.salesBranches.meta()` (لا جدول ميتا).
- **صفحة الرفع:** `#invCardsRow` جُمِّعت: «المستودع» (بطاقة) · «الفروع» (‏`renderBranchCards` من invBranches ＋ `renderSalesBranchCards` للحراج، بطاقات موحّدة المظهر). عنوانا المجموعة `.inv-grp-lbl` (‏`flex:1 1 100%` يفرض لفّ صفّ).
- **إضافة فرع زد من الواجهة معطّلة بعلم `SHOW_ADD_BRANCH`** (‏`false`) — تُفعَّل بتغيير القيمة إلى `true` (زرّ «فرع جديد» `#addBranchCard` مخفيّ؛ `addBranch`/جدول `branches`/مسار زد بلا مساس).
- **🚨 حماية الملف الخاطئ `invOverlapGate`** (صفحة تحديث القاعدة فقط): رفعة غير تأسيسية بتقاطع أكواد الملف مع الموقع **<40٪** ⇒ `confirm` يذكر اسم الموقع · النسبة · عدد الصفوف التي ستُحذف. على القيمة الدقيقة (لا تدوير). مطبَّق على `onMergeBranch` (زد) و`onMergeSalesBranch` (الحراج) — لا المستودع (له `invShrinkCheck`).
- **الدفعة ٣ (تمّت) — تعداد الشاشة `salesAllLocs()`:** دالّة واحدة `salesAllLocs() = invBranches ＋ SALES_EXTRA_LOCS` تقرأها **المواضع السبعة**: (١) `salesLocName` · (٢) `salesLocList` · (٣) `salesLocColor` (لوحة ٦ توكنات: az→green · kh→gold · haraj_maf→amber · haraj_reh→green-2) · (٤) `salesLocIcon` (عامّة بلا تعداد — بلا تغيير) · (٥) `renderSalesLastUp` (شارة «لم يرفع» تشملهما) · (٦) تبويبات `renderSalesTabs` (‏`branchLocs`) · (٧) `renderSalesPage` (‏`locsAll`). 🚨 **مسار زد لا يستدعي `salesAllLocs` في أي سطر** (تحقّق: 0 في `loadInventoryFromDB`/`autoAdopt`/`run`). الفرعان يظهران كأي فرع: جدول مقارنة ٥ صفوف · رسم حتى ٥ خطوط · التبويبات · الراكد/النفاد. القواعد المقدّسة تسري تلقائياً (المستودع مستبعَد · «متحرّكة» كود فريد عبر الخمسة · business_date بالرياض · التأسيس مستبعَد — كلها عبر `branchLocs`).

- **الدفعة ٤ (تمّت) — حراس العزل الخمسة (بأسنان، بقيَم حيّة):** **G-SALES-LOCS** (`sales-locs.mjs`) مواقع المبيعات ٤ فروع (زد ＋ الحراج) · المستودع خارجها؛ `--broken` يُسقط `SALES_EXTRA_LOCS`. · **G-ZID-ISOLATION** (`zid-isolation.mjs`) رفع الحراج يمسّ `sales_branch_items` وحده (صفر مسّ لـ`branch_items` ⇒ العدد الحيّ **6653** ثابت)؛ `--broken` يوجّه `INV_TABLE.xbranch` لجدول زد. · **G-ZID-NO-EXTRA** (`zid-no-extra.mjs`) رمز غير-uuid يُسقَط من `mergeBranches`/`mergeInventory`؛ `--broken` يمرّ `loadInventoryFromDB` على `salesAllLocs`. · **G-ZID-OUTPUT** (`zid-output.mjs`) مخرج مسار زد (‏`mergeInventory`: total ＋ أكواد unified) **متطابق قبل/بعد** حقن صفّ حراج؛ `--broken` كذلك. · **G-BASELINE** (`zid-baseline.mjs`) **4390 حركة `new`** (haraj_maf 1261 ＋ haraj_reh 3129) لا ترفع أي مؤشّر (المبيعات 9,000 · القطع 90 · **المتحرّكة 2** — «متحرّكة» لا تُصفّى بالنوع فهي المؤشّر الحسّاس)؛ `--broken` يُفرّغ `baselineIds` ⇒ تنتفخ لـ4392. (تطابق مجاميع «طابق وأنشئ» قبل/بعد على القاعدة الحيّة = تحقّق المالك — أُثبت بنيويّاً بـG-ZID-OUTPUT/NO-EXTRA.)
- **الدفعة ٣ب (تمّت، ثم نُقلت للشريط المُدمج — راجع «شريط الأدوات المُدمج» أدناه):** «استعلام عن منتج» صار حقلاً في شريط الأدوات أعلى الشاشة ومنسدلةً عائمة (كان `<details id="salesQuery">` مطويّاً — **حُذف الغلاف، بقي `#sqInput`/`#sqResults`**). **عرضٌ فقط** (🚫 لا كتابة/ربط/نشر). حقل واحد (كود/باركود/جزء اسم) عبر `normText`/`normCode` (نفس منطق البحث القائم). **المصدر: `salesCtx.stock` (العرض `sales_stock`) ＋ `salesCtx.movs` — محمّلان أصلاً، بلا استعلام قاعدة إضافيّ** (🚫 لا `branch_items`/`warehouse_items`/`sales_branch_items`/`zid_products`). نتيجة واحدة ⇒ الجدول مباشرةً؛ عدّة ⇒ قائمة اختيار. الجدول: صفّ لكل موقع من الخمسة (المستودع ثم `salesAllLocs`) بأعمدة الموقع·الكمية·شامل·قبل الضريبة ＋ **مبيعات مقدّرة في الفترة** (متاحة من `salesCtx.movs` — المستودع «سحب» لا «بيع») ＋ صفّ إجماليّ للكميات. موقع بلا الصنف ⇒ **«غير موجود»** (لا صفر) · بلا سعر ⇒ **«—»** بعنوان مفسّر · 🚫 لا ضرب/قسمة على 1.15. حارسان: **G-SALES-QUERY-READONLY** (ساكن — لا كتابة/قاعدة/زد؛ `--broken` يحقن `sb.from().insert`) · **G-SALES-QUERY** (متصفّح — الكود⇒الموقع بكميته · الخالي «غير موجود» · السعر مباشر؛ `--broken` يقسم على 1.15). ⚠ التطبيق **داكن فقط** (أُزيل الفاتح سابقاً) فاللقطة داكنة.

## قصّ الطلب الواحد عند سقف الخادم (max-rows=1000) — المساعد كان يقرأ جزءاً بصمت (٢٠٢٦-٠٩)
> **خادم PostgREST يقصّ أي طلب عند `max-rows` (=1000 هنا) مهما كان `.limit` — القراءة بطلب واحد لجدول يتجاوز الحدّ تُرجع 1000 صفّاً بصمت. صفِّح كل قراءة قد تتجاوز الحدّ، أو اقرأ جزءاً وأنت لا تدري.**

**العطل:** الدالّة الطرفية `ai-assistant` كانت تقرأ `sales_stock` (12,189 صفّاً) و`sales_movements` (5,474) بطلب واحد `.limit(100000)` ([index.ts:217-218]) — والخادم قصّهما إلى **أوّل 1000 صفّ**. فـ`inventory_value` جمع أوّل 1000 صفّ من العرض (وترتيبه `warehouse_items` أولاً) = **5,281,061** (جزء المستودع وحده، من أصل 11,198,982) — رقمٌ يطابق لا موقعاً ولا مجموعاً، وثابت قبل/بعد توسيع المواقع (القصّ قبل الحساب)، وقصّ `sales_movements` فسّر تناقض المبيعات (المساعد 47,590 مقابل الشاشة 52,187). **الشاشة سليمة لأنها تُصفّح** (‏`salesStockRows`/`db.sales.movements` حلقة `.range`).
- **الإصلاح:** `fetchAll(makeQuery, label)` في index.ts يدور بـ`.range(from, from+999)` حتى دفعة < 1000، مطبَّق على `sales_stock` و`sales_movements`؛ `sales_uploads` (قليل) يبقى طلباً واحداً. **سقف أمان `MAX_PAGES=100` (100k صفّ) مع `console.error` صريح عند بلوغه — 🚫 لا لا-نهاية ولا قصّ صامت ثانية.** 🚫 لا مساس بـ`computeScope`/المعادلة/التصفية (سليمة). 🚫 لا رفع `max-rows` للمشروع (مخاطرة عامّة).
- **🚨 درس الحارس (الفخّ الذي أخفى العطل شهراً):** `G-AI-PARITY` كان على بيانات وهميّة **< 1000 صفّ** فلم يبلغ سقف النظام أبداً. **حارسٌ على بيانات أصغر من حدّ النظام لا يرى الحدّ — اجعل بيانات الحارس تتجاوز كل سقف حقيقيّ.** وُسِّع G-AI-PARITY إلى **1203 صفّ مخزون** (>1000) مع موك شاشة يفرض سقف 1000/طلب (يختبر تصفيح الشاشة فعليّاً) ＋ تكافؤ قيمة المخزون (الشاشة==المساعد==520000).
- **حارس `tests/ai-paginate.mjs` (G-AI-PAGINATE، بأسنان):** `sb` وهميّ يفرض سقف 1000/طلب وبيانات 12,189 صفّاً؛ يؤكّد أن `fetchAll` تجمع الكل (‏`inventory_value` كامل لا مقصوص) ＋ أن المصدر يستعمل `fetchAll` لـstock+movements ＋ وجود سقف الأمان المعلَن؛ `--broken` يعيد الطلب الواحد ⇒ 1000 صفّ فقط ⇒ رقم مقصوص ⇒ يرسب.
- **تدقيق بقيّة القراءات (طلب واحد بلا تصفيح):** الشاشة تُصفّح `sales_stock`/`sales_movements`/`zid_products`/الجرد. **`waiting_items` (812 صفّاً — قرب السقف، خطر مستقبليّ إن تجاوز 1000)** أُبلغ عنه ولم يُصفَّح بعد (بند مفتوح). البقيّة صغيرة أو محدودة بـ`.limit` مقصود (‏`activity_log` 50/200) أو صفّ واحد (`.eq("id",1)`).

## نقل كتل حالة الرفع من شاشة المبيعات إلى أعلى «المخزون» (دفعة هـ ثم دفعة النقل الثانية، ٢٠٢٦-٠٩)
ثلاث كتل «حالة رفع» نُقلت من شاشة المبيعات (`#page-sales`) إلى **أعلى صفحة «المخزون»** (`#page-inventory` ← `#invUploadStatus`، بعد رأس الصفحة قبل صفّ KPI): **آخر رفعة/التغطية** (`#salesLastUp`+`#salesCoverage`) · **بلا رفعة** (`#salesMissBanner`) · **المفقود** (`#salesBanner`+`#salesDisBox`). عرضٌ منقول لا منطق منقول. (المرحلة الأولى وضعتها في صفحة الرفع `#dbUploadStatus`؛ ثمّ نُقلت إلى المخزون — 🚫 لا نسخة باقية في صفحة الرفع.)
- **الآلية (لا تكرار):** العناصر تُملأ **بالمعرّف** (`getElementById`) من `renderSalesPage`/`salesShowDisappeared`، فتعمل أينما وُضعت في الـDOM. `goPage('inventory')` يستدعي **`fillUploadStatus()`** — دالّة مستقلّة ببوّابة دورها (🚫 لا تعتمد بوّابة شاشة المبيعات/`canSeeSales`) تُعيد استعمال حساب `renderSalesPage` (بلا بوّابة دور فيه — علّة الدفعة أ كانت `renderSalesAI` owner + حجب RLS، لا `renderSalesPage`). لا معرّف مكرّر ولا مستمع مزدوج (نُقلت العناصر نفسها).
- **🚨 صلاحية «المخزون» + RLS (فُحصت قبل النقل):** المخزون مرئيّ لـ**owner · admin · viewer** (‏`show=!mkt`؛ marketing محصور بالمبيعات). الكتل تقرأ `sales_uploads`/`sales_movements`/`sales_stock` — مسموحة لـowner+admin (والدفعة هـ أضافت admin) لكن **viewer محجوب بـRLS** (الثلاثة owner+marketing+admin). لذا `fillUploadStatus` يملأ لـ**owner+admin فقط** و**يُخفي الحاوية لـviewer** (‏`host.style.display="none"` — لا نصف صامت). التغطية تبقى owner فقط (admin مخفيّ).
- **🚨 القاعدة المقدّسة ٨ محفوظة في الشاشة:** صفوف «لا رفعة في هذه الفترة» في جدول مقارنة المواقع **باقية**، ＋ سطر ثانويّ `.tot-note` «يشمل N من N فروع» في **صفّ الإجمالي** (بلا لون تحذير/أيقونة/لافتة، فقط حين N<الكل) — فالرقم صادق بلا ضجيج بعد نقل اللافتة.
- **🚨 صلاحية admin (فُحصت قبل النقل — سابقة `branches`):** صفحة الرفع لـowner+admin، والكتل تشتقّ من `sales_movements`/`sales_stock` اللذين كانا محجوبين عن admin (owner+marketing فقط) ⇒ نقلها كان سيجعلها فارغة لـadmin (محمد، من يرفع). **الحلّ:** مُنح admin قراءةَ `sales_movements` و`sales_stock` (＋`sales_uploads`) — `supabase_marketing_role.sql` (السياسة والعرض القديم) ＋ `supabase_sales_branches.sql` (العرض الحيّ) ＋ سكربت حيّ منفصل `supabase_admin_sales_read.sql` (‏`ALTER POLICY` ＋ `create or replace view` — 🚫 لا drop، يحفظ صلاحية `backup_ro`). الكتابة تبقى owner+admin. **المبرّر:** محمد يرفع الإكسل يوميّاً فكل الكميات/الأسعار تمرّ عليه، و`sales_stock` = محتوى `branch_items` الذي يقرأه/يكتبه؛ الحجب كان فخّ فشل صامت لا أماناً.
- **استثناء العرض (لا الصلاحية):** شريط **التغطية** (معدّل الريال/يوم) يُخفى عن admin — يُكتب `#salesCoverage` فقط لـ`owner`/`marketing`. الكتلتان الأخريان (بلا رفعة · المفقود) تظهران لـowner+admin (شغل تشغيليّ لمن يرفع).
- **الحارس `sales-clean.mjs` (G-SALES-CLEAN، بأسنان، يفحص المحتوى لا الغياب):** لا كتلة في `#page-sales` ولا `#page-db` · الثلاث في `#invUploadStatus` **ومملوءة فعلاً** (محتوى «آخر رفعة» ＋ جدول المفقود يفتح) · «يشمل N من N» في صفّ الإجمالي بالشاشة · التغطية owner فقط · **viewer الحاوية مخفيّة**. `--broken` يُظهر التغطية لـadmin ⇒ يرسب · `--broken-empty` يمنع الملء (حاوية فارغة، لا «آخر رفعة») ⇒ يرسب (إثبات فحص المحتوى). G-NOUPLOAD يفحص «يشمل N من N» في صفّ الإجمالي (بيان القاعدة ٨ في الشاشة). G-ROLE-SALES: قراءة المبيعات owner+marketing+admin (admin محجوب عن **شاشة** المبيعات — يقرأ بياناتها لكتل الرفع فقط).

## صفّ شارات المواقع في منسدلة «استعلام عن منتج» (دفعة د، ٢٠٢٦-٠٩)
منسدلة البحث في شاشة المبيعات تعرض كل نتيجة كصفّ شارات بنمط «بحث في المخزن» (الرئيسية): الكود بارزاً ＋ الاسم، ثم `.q-meta` بشارات **إجمالي · موقع لكلّ كمية>0 · السعر**.
- **🚨 التنسيق مُعاد لا المصدر:** أُعيد استعمال **أصناف الشارات وتوكناتها** (`.q-chip`/`.q-meta`/`src-wh`/`src-br`/`qty-in`/`price`) من `runWhQuery`، **لا مصدر بياناته** (‏`runWhQuery` يقرأ `lastMerge` = مسار زد بلا فرعَي الحراج). المصدر هنا يبقى `salesCtx.stock` (العرض `sales_stock`، خمسة مواقع منها الحراج) — فلو نُسخ مصدر المرجع لاختفى الحراج من الشاشة التي بُني لها. **لا تكرار منطق:** `sqStockByLoc(sku)` دالّة مصدر موحّدة تقرأها المنسدلة (`sqBadgeRow`) وجدول التفاصيل (`salesQueryDetail`) معاً.
- **القائمة دائماً** (كالمرجع): أُزيلت قفزة «نتيجة واحدة ⇒ الجدول مباشرةً»؛ المنسدلة تعرض قائمة الشارات لكل عدد نتائج، والنقر ⇒ `salesQueryDetail` (جدول ٥ مواقع بـ«غير موجود» صريحة — **بلا تغيير**). المواقع الخالية **لا تظهر في المنسدلة** (كالمرجع) لكنها تبقى في جدول التفاصيل. السعر `price_incl` (🚫 لا 1.15)؛ تعذّره ⇒ «—». حُذفت عبارة «(يشمل المستودع)» — شارة المستودع ظاهرة بذاتها.
- **الحارس `sales-query-badges.mjs` (بأسنان):** مجموع شارات المواقع == شارة الإجمالي · صنف حراج ⇒ شارته تظهر (إثبات أنّ المصدر `sales_stock` لا مسار زد) · موقع واحد ⇒ شارة موقع واحدة · السعر `price_incl`/«—». `--broken` يجعل الإجمالي = المجموع+1 ⇒ يرسب. G-SALES-QUERY عُدّل لينقر النتيجة (التدفّق الجديد) بلا تليين تأكيداته.
- **شارة سعر التكلفة (owner+marketing فقط):** بجوار شارة السعر مباشرةً؛ السعر صار **«شامل: N ر.س»** والتكلفة **«تكلفة: N ر.س»** (تفادي لبس الرقمين، ＋`aria-label` صريح لكلٍّ «السعر شامل الضريبة N ريال»/«سعر التكلفة N ريال»). القيمة **المستودع أولاً، فإن غاب فأوّل موقع له قيمة** (🚫 لا متوسّط/جمع)؛ اختلافها بين المواقع ⇒ `title` «التكلفة تختلف بين المواقع — المعروضة من <المصدر>» ＋ `.cost-diff` (حدّ متقطّع، بلا إيموجي). بلا تكلفة ⇒ «تكلفة: —» (لا صفر/إخفاء). توكن محايد `.q-chip.cost` (نصّ `--muted`؛ الذهبيّ محجوز للسعر). **🔒 البوّابة `canSeeSales()`** (نفس مصدر `cost_price` المقصور بـcase في العرض — بلا تكرار منطق): غير owner/marketing ⇒ لا شارة تكلفة. حارس `tests/perf/sq-cost.mjs` (G-SQ-COST، بأسنان): شارتان بـaria · المستودع أولاً ＋ tooltip الاختلاف · «—» بلا تكلفة · حجب admin/viewer؛ `--broken` يُسقط البوّابة ⇒ يرسب. (‏`sales-query-badges` حُدّث `parse` ليستثني «شامل»/«تكلفة» من عدّ المواقع.)

## ثلاثة إصلاحات عرض في شاشة المبيعات (دفعة ج، ٢٠٢٦-٠٩)
- **ج-١ لافتة الموقع الغائب لا تتناقض (`renderSalesPage`/`#salesMissBanner`):** كانت تقول «بلا رفعة في هذه الفترة … (آخر رفعة منذ X)» لموقع رفعته **تأسيسية فقط**. الآن تُجمَّع الفروع الغائبة في ثلاث مجموعات بحسب السبب، **السبب يُذكر مرّة لكل مجموعة** (لا يُكرَّر بعد كل فرع)، والذيل بحسب الحالة: **رفعة تأسيسية** (الملف مرفوع، الناقص رفعة ثانية) ⇒ «حتى تأتي رفعة ثانية تُقارن بها» — 🚫 **لا** «تُرفع ملفاتها» · **لم يُرفع بعد / خارج الفترة** ⇒ «حتى تُرفع ملفاتها». الصرف يطابق العدد (مثنّى/جمع): «فرعان رفعتهما تأسيسية … أرقامهما» · «N فروع رفعتها … أرقامها». مثال حيّ: «فرعان رفعتهما تأسيسية: الحراج مفروشات · الحراج رحلات — أرقامهما تظهر حين تأتي رفعة ثانية تُقارن بها. الإجمالي يشمل 2 من 4 فروع.» حارس `sales-display-fixes.mjs`: مقطع «تأسيسية» يحمل «رفعة ثانية تُقارن بها» و**لا يذكر «ملف»** إطلاقاً، و«تأسيسية» تظهر مرّة واحدة (لا تكرار)؛ `--broken` يُدخل ذيل رفع الملفات للتأسيس ⇒ يرسب.
- **ج-٢ شريط التغطية مرّة واحدة (`#salesCoverage`):** كان `lu.innerHTML +=` **بعد `await`**، فتشغيلان متداخلان (تغيير فلتر أثناء التحميل) يتراكمان ⇒ الشريط مرّتين. الآن عنصر مستقلّ `#salesCoverage` (يبنيه `renderSalesLastUp` فارغاً في كل رسم) يُكتب بالاستبدال (SET) لا الإلحاق، فالتشغيل الثاني يستبدل. **الدرس: `innerHTML +=` بعد `await` في دالّة قد تتداخل = تراكم — اكتب في عنصر يُستبدَل.** حارس `sales-coverage-once.mjs` **يشغّل تشغيلين متداخلين فعلاً** (تأخير حقيقيّ في الموك) ويعدّ الظهور == 1 (‏`--broken` يعيد الإلحاق ⇒ 2 ⇒ يرسب).
- **ج-٣ عمود «اسم الصنف» في جدول المختفي (`salesShowDisappeared`):** الصنف مفقود من المخزن فاسمه غير متاح في `sales_stock`؛ يُقرأ من **الحركة نفسها `m.sku_name`** (مخزَّن وقت الرصد في `sales_movements`) — 🚫 لا استعلام إضافيّ. تعذّره ⇒ «—» صراحةً. عمود بعد SKU مباشرةً. حارس `sales-display-fixes.mjs --broken-name` يحذف الخليّة ⇒ يرسب.

## سياسة restrictive على جدول أسماء كسرت شاشة كاملة لدور كامل (دفعة أ، ٢٠٢٦-٠٩)
> **سياسة restrictive على جدول بيانات وصفية كسرت شاشة كاملة لدور كامل، ولم تُكتشف إلّا بأول دخول حقيقيّ لذلك الدور — اختبر كل دور حيّاً قبل إعلان النجاح.**

**العطل:** `supabase_marketing_role.sql` أدرج `branches` ضمن جداول العزل، فأنشأ `branches_nomkt_sel` (‏`get_my_role() IS DISTINCT FROM 'marketing'`) تمنع marketing من قراءة **أسماء** الفروع. النتيجة: `db.branches.getAll()` ⇒ `invBranches=[]` ⇒ `salesAllLocs()` يعيد `SALES_EXTRA_LOCS` وحدها (الحراج) ⇒ **اختفت العزيزية والخضرة من شاشة المبيعات كلياً** لدور marketing. الدليل الحيّ قبل الإصلاح: تبويبان (الحراج فقط) · جدول مقارنة صفّان · قيمة المخزون 2,454,028 (الحراج وحده). بعد حذف السياسة: أربعة فروع · الإجمالي 52,187 · 1,305 قطعة · 705 صنف · مخزون الفروع 5,503,765.
- **الإصلاح (المصدر لا العَرَض):** أُزيل `'branches'` من مصفوفة العزل في `supabase_marketing_role.sql` ＋ `drop policy if exists branches_nomkt_sel` (idempotent). `branches` بيانات وصفية (id/name/counts) وأرقام المخزون تأتي من العرض `sales_stock` (المسموح لـmarketing)، فقراءته آمنة. بقيّة الجداول تبقى معزولة (مطابقة زد لا يراها marketing).
- **الدرس على الحراس:** حراس الأدوار كانت تفحص **الإخفاء** (هل يُخفى المساعد لغير owner؟) ولم تفحص **الظهور** (هل تظهر البيانات نفسها للدور المصرّح؟). `G-ROLE-BRANCHES` (`tests/perf/role-branches.mjs`) يسدّ ذلك: marketing بفروع زد محمَّلة ⇒ `salesAllLocs()`=4؛ وبفشل التحميل ⇒ تحذير صريح لا صمت.
- **أ-٢ صمّام صمت النقص (`#salesBranchWarn`، عرضٌ محض):** `renderSalesPage` يقارن مواقع البيانات (`stock`/`movs`) بالتعداد (`locsAll`)؛ أي موقع في البيانات وخارج التعداد ⇒ بانر «تعذّر تحميل قائمة الفروع — الأرقام قد تكون ناقصة». **مستقلّ عن الدور** (يُقاس بالبيانات لا بالصلاحية)، فيغطّي أي فشل تحميل (صلاحية/شبكة) لا marketing وحده. **صمت النقص أخطر من الخطأ الظاهر.**

## «بلا مرشّح موثوق» لا يعرض مرشّحاً ضعيفاً (٢٠٢٦-٠٩) — عرض بحت، صفر تغيير مطابقة
بطاقة «يحتاج ربط»/«غائب» الحمراء (‏`classifyBatch` = «red» ⟺ `!best || best.score<40`) كانت تعرض `r.best` الضعيف (حبل→براد، الرابط سعرٌ ＋ لون) فتغري بربط فاسد. الآن:
- **`weakUnreliable = chkCls === "bt-rd" && (!r.chosen || r.chosen === r.best)`** (في `batchCardHTML`): عمود المستودع يعرض «لا مرشّح موثوق — ابحث يدوياً» (لا المرشّح الضعيف)، `matchLine` يعيد "" (لا تكرار)، وصندوق «اعتماد الربط» **`disabled`**، والمرشّح الضعيف **خارج `data-search`** أيضاً (لا ربط وهميّ في الفهرس).
- **الاختيار اليدويّ يستعيده:** `comboPick` يجعل `r.chosen ≠ r.best` ويستدعي `batchWhInner(r,false)`/`matchLine(r,"bt-g",false)` ويُفعّل الصندوق ⇒ يظهر المرشّح المختار ويُعتمَد. وبعد إعادة الرسم الكامل يبقى `chosen≠best` فلا يُخفى.
- 🚫 صفر تغيير في خوارزمية المطابقة/عتباتها — **عرض فقط**. حارس **`nocand.mjs` (G-NOCAND، بأسنان):** الأحمر بلا كود/اسم المرشّح الضعيف ＋ رسالة ＋ صندوق `disabled`؛ الأخضر الموثوق مرشّحه معروض وصندوقه مفعّل؛ `--broken` يعرض الضعيف ⇒ يرسب. (b2-invariants: مرساة `matchLine(r, chkCls` أُرخيت لتقبل الوسيط `weakUnreliable` — تحرس المعنى لا القوس.)

## بطاقة «غائب عن المخزن» — «الكود السابق» (البند ٢ ⇒ أ) — عرض بحت
البطاقة الغائبة (`r.isAbsent`) في «يحتاج ربط»/«غائب» تعرض تحت الاسم سطر `.mc-absent-info`: **«الكود السابق: <mappedCode>»** (من `absentList[].mappedCode = manualMap[rawKey] || null` — بلا استعلام)؛ و**«آخر موقع: —» · «اختفى: —»** صريحتان (`.q-na`) لأنّهما تحتاجان `sales_movements` غير المحمَّل في الرئيسية؛ ＋ سطر إحالة `.mc-absent-ref` «لتفاصيل حركة هذا الصنف استعمل بحث «حركة الصنف»». 🚫 لا زرّ «أين اختفى؟» ولا استعلام في هذه البطاقة. الحارس نفسه `nocand.mjs` يغطّيه (الكود السابق ＋ «—» ＋ الإحالة).

## «حركة الصنف» — البحث الثالث (خطّ زمنيّ عرض-فقط) — دفعة ٣-أ
بحثٌ ثالث في قسم ③ (بجوار «بحث في المخزن» ＋ «بحث في متجر زد»)، **بالضغط لا حيّ** (شريط `.ih-searchbar`: حقل ＋ زرّ «بحث»؛ Enter يشغّله). يعرض خطّاً زمنيّاً تنازليّاً لصنف واحد.
- **المصدران عند الضغط حصراً** (🚫 صفر استعلام عند التحميل — حارس بذاته): `db.activity.getBySku(sku)` (أحداث activity_log الحاملة `zid_sku`، مصفّحة `.range` بحدّ 1000) ＋ `db.sales.movementsBySku(sku)` (حركة الكمية/الاختفاء، RLS owner+marketing+admin — **viewer محجوب**). الحلّ من الإدخال إلى sku عبر `buildZidIndex` (بلا استعلام؛ يُحمَّل زد كسولاً عند الضغط إن غاب — استثناء «عند الطلب»). عدّة نتائج ⇒ قائمة اختيار `.sq-hit`.
- **`runItemHistory`→`ihLoad`→`renderItemTimeline`:** سطر حالة `.ih-state` (الاسم · مربوط؟ من `manualMap` · النشر/الكمية/السعر من `buildZidIndex` · **«آخر موقع» مشتقّ من أحدث حدث موقعيّ في الخطّ** — `salesLocName(movSorted[0].location)`، بلا استعلام؛ وإلا **«غير معروف»** 🚫 لا إحالة لأداة أخرى) ＋ **بيان دائم `.ih-note`** «لا يشمل … التسجيل بدأ من `IH_LOG_SINCE`=٢٠٢٦-٠٩-٢٥، وما قبله غير مسجَّل».
- **قسمان منفصلان لا خطّ مختلط** (قرار المستخدم — المحور «ما فعلته الأداة في زد» لا حركة المخزون): **① `.ih-sec-primary` «في متجر زد — قرارات الأداة»** (الأبرز، حدّ ذهبيّ): أحداث `activity_log` عبر `ihActEvent`/`actDescribe`＋`ACT_META` (ربط/نشر/تصفير/سعر/انتظار/عودة؛ `before←after` من `ihBeforeAfter`). **② `<details class="ih-more">` «حركة المخزون (للسياق)» مطويّ افتراضياً**: حركات `sales_movements` عبر `ihMovEvent` (بيع مقدّر/اختفاء/ظهور). **🚫 لا قسم أوّل فارغ بلا تفسير:** غير مربوط (`!manualMap[sku]`) ⇒ بيان `.ih-unlinked` «هذا الصنف غير مربوط بمنتج زد — فلم تتّخذ الأداة فيه أي قرار نشر أو تسعير»؛ مربوط بلا قرارات ⇒ «مربوط، لكن لم تُسجَّل الأداة أي قرار بعد `IH_LOG_SINCE`». **`.ih-viewer`** «حركة المخزون محجوبة عن دورك» داخل الثانويّ حين يُحجب `sales_movements` (وآخر موقع «غير معروف»).
- **`riyadhDateTime`** يعيد استعمال `SALES_TZ_OFF` (لا اشتقاق توقيت جديد). **`ihRound1`** يقرّب `period_days` لمنزلة واحدة («على 1.3 يوم» لا الخام) — بقيّة الأداة تقرّب أصلاً بـ`Math.round` (التغطية/الراكد/النفاد/اتّجاه المبيعات/`salesCovGateSub`)، فالخام كان **حصراً** في `ihMovEvent`. `ACT_META` وُسِّع بأحداث صنفيّة (offset/excluded/positional) ＋ أحداث تحوّلات ٣-ب (qty_zeroed/republished/unpublished/price_changed/returned_stock/link_added_batch) — تُحلّ أيقوناتها وتظهر متى سُجّلت.
- **الفتح التلقائيّ للقسم الثانويّ حين لا أحداث في الأوّل** (`autoOpen = primEvents.length===0 ? " open" : ""`): 🚫 لا بطاقة تبدو فارغة وأحداثها تحت الطيّة؛ الطيّ يبقى فقط حين القسم الأوّل ممتلئ. **حركة الكمية كاملة لا الاختفاء وحده:** `sales_movements` يحمل **كل** التغيّرات (بيع مقدّر `estimated_sale` · استلام/توريد `purchase` delta>0 · ظهور `new` · اختفاء `disappeared`) و`ihMovEvent` يرسمها كلها (لا فلترة في `movementsBySku`). **العدّادان منفصلان لفظاً:** «N أصناف مطابقة» (البحث متعدّد النتائج) · «N حدث للصنف» (الصنف المختار). **تجميع الأحداث المتطابقة المتتالية «×N»** (`ihGroupConsecutive` + `ihCountBadge`، عرض فقط 🚫 لا حذف من القاعدة) — يخفّف تكرار التسجيل حتى قبل إصلاح جذره (البند أ).
- **#5 رسالة «غير مربوط» تتبع وجود الأحداث لا حالة الربط:** إن كان للصنف غير المربوط **أحداث**، تُعرض الأحداث ＋ شرح `.ih-unlinked` «منتج زد بلا ربط بالمخزن — لذلك تُصفّره الأداة وتُخفيه (غائب عن المخزن)»؛ ورسالة «فلم تتّخذ الأداة فيه أي قرار» **فقط حين لا أحداث فعلاً**. (كان الشرط يفحص `!linked` فيناقض عشرة قرارات تحته.)
- **#6 الفجوة بين قرار الأداة وواقع زد:** وسم `.ih-mirror` «الحالة أعلاه من مرآة زد — آخر مزامنة: <منذ X> (<تاريخ>)» (من `zidSyncedAt`) ＋ تنبيه `.ih-mismatch` حين يخالف **آخرُ قرارٍ مسجَّل** الحالةَ المعروضة (‏`qty_zeroed` وكمية المرآة≠0 · `unpublished`/`republished` ونشر المرآة معاكس): «⚠️ آخر قرار للأداة: <القرار> — ولم يصل زد بعد. استورد ملف الكميات ثم أعد مزامنة المرآة». مبنيّ من `primEvents` (‏`.find` لأحدث كلّ نوع) و`z` من `buildZidIndex` — بلا استعلام.
- **#7 دور صاحب الحدث بدل «مستخدم آخر»:** `ihWho` يعرض «أنت · <الدور>» للحاليّ، و`roleLabel` من `ihRolesCache` (‏`db.roles.all()` — `user_roles` صغير وRLS يسمح بقراءته، يُجلب **مرّة/جلسة** best-effort في `ihLoad`) لغيره (admin ⇒ «مشرف»). 🚫 لا بريد لأي طرف خارجيّ · لا استعلام لكل حدث.
- **#9 لا قسم بلا نصّ (ثالث فراغ صامت في هذه البطاقة):** نصّ القسم الثانويّ الفارغ صريح ومتمايز: غير مربوط ⇒ «هذا SKU زد لا كود مخزن — فلا حركة مخزون تخصّه» · مربوط بلا حركات ⇒ «لا حركة مخزون مسجَّلة في الفترة».
- عرضٌ محض (🚫 لا كتابة). `.query-row` صار ٣ أعمدة (٢ عند ≤1100px · ١ عند ≤720px). حارس **`item-history.mjs` (G-ITEM-HISTORY، بأسنان ستّية):** صفر استعلام عند التحميل · الضغط يستعلم الجدولين · قسمان · فتح تلقائيّ عند فراغ الأوّل ومطويّ عند امتلائه · تجميع ×N · **#5 الرسالة عند غياب الأحداث فقط** · **#6 وسم المرآة ＋ تنبيه الفجوة** · **#7 دور صاحب الحدث** · **#9 لا قسم بلا نصّ** · آخر موقع مشتقّ · الزيادة معروضة · لا عشريّ خام · عدّادان؛ `--broken` (الفتح التلقائيّ) · `--broken-expl` (تفسير الفراغ) · `--broken-group` (التجميع) · `--broken-msg` (#5 الرسالة مع أحداث) · `--broken-sec` (#9 قسم بلا نصّ) ⇒ خمستها ترسب.

## تسجيل تحوّلات run() في سجل النشاط — دفعة ٣-ب (يغذّي «حركة الصنف»)
تسجيل **التحوّلات** التي تصنعها المطابقة في `activity_log` — **إضافيّ محض**، فتصير مرئيّة في «حركة الصنف». يبدأ التسجيل من ٢٠٢٦-٠٩-٢٥ (`IH_LOG_SINCE`)؛ ما قبله غير مسجَّل (يقوله بيان الخطّ).
- **كتلة قراءة-فقط في آخر `run()`** (بعد `lastClassify`، محروسة `if (!reprocess && dbOnline && sb)`): تُشتقّ من البِنى **النهائيّة** (`zeroDoor`＋`finalQtyByRaw`＋`rowByRawSku` للتصفير · `updatedList` للسعر/إعادة النشر · `lastAutoUnpubSimple`/`lastAutoUnpubParents` للإخفاء · `lastParentPublish` لنشر الأب · `lastReactivated` للعودة) وتبني `runLog[]` ثمّ `db.activity.bulkInsert(runLog).catch(()=>{})`. **🚫 لا تُقرأ في مسار القرار ولا تغيّر منطقاً** — أُثبت بحياد المشغّل السلوكيّ كاملاً (صفوف الملفّين متطابقة).
- **الشروط الثمانية مستوفاة:** (١) إضافيّ بعد القرار، **fire-and-forget** (فشل الكتابة لا يُفشل `run` — try/catch＋`.catch`، **و`.catch` يترك أثراً في الكونسول عبر `console.warn` لا صامتاً**) · (٢) **تحوّلات فقط لا حالات**: `qty_zeroed` يتخطّى `before===0` (المصفَّر أصلاً)، والكمية العاديّة (غير الصفر) لا تُسجَّل · (٣) كل صفّ `zid_sku · نوع · before←after · reason` في `details` · (٤) **`dbSetMappings` الدفعيّ صفّ لكلّ صنف بـzid_sku** (‏`bulkInsert` per-item، لا `link_added` بـ`null`) · (٥) صفر تغيير منطق (حياد سلوكيّ) · (٦) **append-only** · (٧) حارس G-RUN-LOG · (٨) لا عمود جديد — `details jsonb` يكفي؛ الفهرس **`supabase_activity_log_sku_index.sql`** = **مركّب جزئيّ** `(zid_sku, created_at desc) where zid_sku is not null` (‏**نُفّذ في القاعدة** — يخدم الترشيح والترتيب معاً بلا خطوة فرز، والشرط الجزئيّ يستبعد صفوف `zid_sku IS NULL` القديمة).
- **الأنواع المسجَّلة** (في `ACT_META` بأيقونات): `qty_zeroed`(low/waiting/absent/matched0) · `price_changed` · `republished`(بسيط ＋ أب) · `unpublished`(بسيط بكلّ الأبواب ＋ أب بالإجماع) · `returned_stock`. يُسجَّل في المطابقة الكاملة فقط (`!reprocess`) فلا تُكرَّر مع كل قرار حيّ.
- **🚨 منع التكرار (البند أ) — `dedup_key` ＋ `mirror_ts` ＋ درع الجلسة:** كل تشغيلة `run()` كاملة تعيد رصد نفس التحوّل (‏`before` مصدره **مرآة زد** `stData`←`zid_products`، لا يتغيّر حتى تُعاد المزامنة) فكانت تُسجّله من جديد (نفس القرار ٣ مرّات خلال ثوانٍ عبر `launchMatchIfReady` من رفع المستودع/الفرع/«طابق وأنشئ» — كلٌّ يستدعي `run()`). الحلّ: مفتاح حتميّ `dedup_key = zid_sku|event_type|before|after|reason|mirror_ts` حيث `mirror_ts = zidSyncedAt`. التحوّل نفسه على المرآة نفسها ⇒ صفّ واحد؛ تتغيّر المرآة (استيراد الملف في زد ثمّ إعادة المزامنة) ⇒ `mirror_ts` جديد ⇒ يُسجَّل مرّة (**ذاتيّ الشفاء**؛ وبعد الاستيراد الحقيقيّ يصير `before=0` فلا يُرصَد أصلاً). **درع الجلسة `runLogSentKeys`** (‏`Set`) يمنع الإرسال المكرّر داخل الجلسة؛ والفهرس الفريد على `dedup_key` يمنعه عبر الجلسات/الأجهزة عبر `db.activity.bulkInsert` ⇒ `upsert(onConflict:"dedup_key", ignoreDuplicates:true)` (=`ON CONFLICT DO NOTHING`). **السبب يدخل المفتاح عمداً** (اختلاف السبب اختلاف قرارٍ حقيقيّ يستحقّ صفّاً). **صفر تغيير في مخرجات run()** (حياد المشغّل السلوكيّ). SQL: **`supabase_activity_log_dedup.sql`** (عمود `dedup_key` nullable ＋ فهرس فريد **غير جزئيّ** — NULLات الصفوف الـ74 القديمة متمايزة فلا يفشل الإنشاء، والجزئيّ لا يعمل مع `ON CONFLICT` عبر PostgREST؛ **نُفّذ للمراجعة**).
- حارس **`run-log.mjs` (G-RUN-LOG، بأسنان رباعية، خادم localhost يشغّل `run()` الحقيقيّ ويلتقط إدراجات/upsert activity_log ＋ الكونسول):** التصفير/السعر/النشر/العودة بـbefore←after · المصفَّر أصلاً والكمية العاديّة **لا تُسجَّلان** · الربط الدفعيّ صفّ/صنف بلا null · **منع التكرار: `dedup_key` يحمل `mirror_ts` · تشغيلة ثانية بنفس المرآة لا تُعيد الإرسال · تغيّر المرآة يُعيده** · فشل الكتابة **يترك أثراً في الكونسول** ولا يُفشل `run`؛ `--broken` (حذف `before===0`) · `--broken-silent` (‏`.catch` صامت) · `--broken-dedup` (إلغاء درع الجلسة ⇒ تكرار) ⇒ ثلاثتها ترسب. **⚠ puppeteer يُصنّف `console.warn` نوعاً `"warn"` لا `"warning"`** (الالتقاط يشمل الثلاثة) · والمسار الجديد يمرّ عبر `upsert` لا `insert` (الموك يلتقط الاثنين).

## تقويم اختيار المدى في شاشة المبيعات (٢٠٢٦-٠٩) — واجهة نطاق، صفر تغيير في التجميع
فلتر الفترة صار: أزرار سريعة (**أمس · 7 أيام · 30 يوماً · منذ البداية** — حُذفت «سنة» فقط) ＋ **زرّ تقويم** (`#salesCalBtn`) يفتح منبثقاً (`#salesCalPop`) لاختيار مدى `business_date` من←إلى.
- **مصدر واحد للنطاق:** `salesManualRange = null | {start,end}` بجانب `salesPeriod`. `salesRange()` تُرجع `{sinceIso, prevSinceIso, untilIso}` — اليدويّ له الأولويّة؛ الزرّ السريع `untilIso=null` (النهاية «الآن»). زرّ سريع يمسح اليدويّ والعكس.
- **🚨 حدّ النافذة الأعلى (آليّة المدى لا تغيير معادلة):** `renderSalesPage` يقصّ `rawMovs` مرّة إلى `salesBizTs <= curUntil` ⇒ كل مصفّيات `>= curSince` الـ10 تصير `[curSince, curUntil]` **بلا مسّها**. ＋ حدّ أعلى في `uploadedInPeriod` (القارئ الثاني). `salesShowDisappeared` (قارئ مستقلّ) يفرض `[since,until]` **للمدى اليدويّ فقط** (الزرّ السريع سلوكه ثابت). **صفر تغيير في `salesAgg`/value_est/الاستثناءات/استبعاد المستودع/التأسيس/inventory valuation.** الفترة السابقة للمدى اليدويّ = `[start−len, start)` بحدّين (طول len بالضبط، لا تداخل).
- **🚨 علامات الأيام من `ups` لا `movs` (تجنّب «النفي الكاذب»):** `salesCtx.movs` مُصفّى بالفترة، لكن `salesCtx.ups` (‏`sales_uploads`، `.limit(3000)`، **كل الرفعات، غير مُصفّى**) يغطّي كل business_dates. `salesAvailableDates()` = `{ salesBizDate(u.captured_at) }` — **بلا استعلام جديد** (‏`ups` أُضيف إلى salesCtx: تخزين لا استعلام). فالتنقّل لشهر قديم يعرض ذهبيّه الصحيح، لا نفياً كاذباً ⇒ **لا حاجة لحالة ثالثة**. حدود البيانات (min/max) من `ups` للـdisabled.
- **التاريخ بالمسار الموحّد حصراً:** `salesBizDate` (business_date، اليوم السابق بالرياض) · `riyadhDay` · `shiftYmd` · `salesBizTs`. 🚫 لا `slice(0,10)` على ISO خام ولا `toISOString` لاشتقاق يوم العمل ولا طريقة رابعة.
- **التمييز البصريّ (مقيس):** فيه بيانات = خلفية `color-mix(--gold 16%)` فاتحة · محدَّد (start/end) = تعبئة `--gold` صلبة نصّها `#1A1206` (تباين حادّ) · ضمن المدى = `color-mix(--gold 8%)` · اليوم = حلقة `outline` مستقلّة. Legend بالحالات الأربع. `latest` (آخر رفعة) لا يُقصّ (يبقى الحقيقة الحيّة).
- **الوصولية:** أسهم (RTL: يسار=التالي) · Enter/Space اختيار · Esc يغلق ويعيد التركيز للزرّ · `role=grid`/`gridcell` · `aria-label` لكل يوم («… تتوفّر بيانات»/«لا توجد رفعة»/«خارج البيانات») · disabled بـ`aria-disabled`. الجوال ≤400px: منبثق 300px مثبَّت للحافّة، بلا تمرير أفقيّ، لمس 34px.
- **الحارس `date-range.mjs` (G-DATE-RANGE، بأسنان):** ذهبي=business_date(ups) · مدى شامل الطرفين · حدود disabled · الأزرار الأربعة ثابتة · السابقة بحدّين · Riyadh؛ `--broken` يشتقّ بـ`riyadhDay`(captured_at) بدل `salesBizDate` ⇒ الأيام تنزاح ⇒ يرسب. (G-YESTERDAY حُدّث: «سنة» حُذفت · G-MOVED: قراءة ملاحظة العابر من `td.n` لا خليّة الإجمالي.)

## شريط الأدوات المُدمج في شاشة المبيعات (المساعد ＋ البحث) — تجميل بحت، صفر تغيير منطق
أُعيد تصميم **موضع** «مساعد المبيعات» و«استعلام عن منتج» من بطاقتين ضخمتين ظاهرتين دائماً إلى **شريط مُدمج واحد** (`.s4-tools`/`#salesTools`، **أسفل الفلاتر `.s4-top` وقبل المؤشّرات `#salesKpis`**) — 🚫 صفر تغيير في الحساب/Supabase/الدالّة الطرفية/Gemini/توجيه النوايا/المطابقة/البيانات الظاهرة/العدّاد. **أُعيد استعمال الدوال نفسها** (`salesAskAI`/`renderAiStructured`/`aiPlainText` · `onSalesQuery`/`salesQueryRun`/`salesQueryPick`/`salesQueryDetail`) والمعرّفات (`#saiInput`/`#saiAnswer`/`#saiAsk`/`#sqInput`/`#sqResults`) — لا تكرار منطق. لا استعلام Supabase إضافيّ (القائمة تجمع من `salesCtx.stock` المحمَّل أصلاً).
- **الشريط نصفان متوازنان (ارتفاع ~52px، مقيس):** نصف المساعد `#salesAI` (زرّ `#saiToggle` «اسأل مساعد المبيعات…») ＋ نصف البحث `#salesSearchTool` (حقل `#sqInput` داخل `label.stool`). **النصف الفارغ يختفي:** `renderSalesAI` يملأ `#salesAI` لـowner و marketing (الدفعة ب)، ويتركه `""` لـadmin/viewer؛ قاعدة `.s4-tools > #salesAI:empty { display: none }` تُخفي النصف الفارغ **فيأخذ البحث العرض الكامل بلا فراغ ولا حدّ معلّق** (مقيس: عرض البحث == عرض الشريط للأدوار بلا مساعد).
- **⚠ حقول الشريط تصطدم بقاعدة `input[type=text]` العامّة (تخصيص أعلى):** حشوها ٤١px يرفع الشريط إلى ٦٥px. الحلّ `.stool .stool-input` (كلاسان يتغلّبان على element+attribute) بحشو/حدّ صفر. **الدرس: أي حقل جديد داخل عنصر مخصّص يرث القاعدة العامّة ما لم يتغلّب تخصيصه.**
- **اللوحتان عائمتان لا تُزيحان المؤشّرات (القيد ٣):** `.sai-panel` و`.sq-drop` = `position:absolute; top:calc(100%+8px); z-index:45` أسفل الشريط مباشرةً (🚫 لا modal). **معتمتان بتركيب توكنيّ** `linear-gradient(var(--panel),var(--panel)), var(--bg)` (‏`--panel` شفّاف 50% وحده يُشِفّ ما خلفه). الافتراضيّ `hidden` (الشكل مُدمج). `#saiPanel` مطلقة ضدّ `#salesTools` (relative) فتملأ عرض الشريط رغم كونها داخل نصف المساعد؛ `#sqResults` مطلقة ضدّ `#salesSearchTool` (تحت البحث وحده).
- **⚠ فخّ «إعادة بناء innerHTML تفصل e.target» في مستمع النقر-خارج:** نقر خيار بحث يعيد بناء `#sqResults` فيُفصَل الهدف ⇒ `contains(e.target)` يكذب «خارج» ⇒ إغلاق خاطئ للمنسدلة. الحلّ **`e.composedPath()`** (يحفظ المسار الأصليّ وقت الإطلاق) لا `e.target`. الإغلاق: البحث دائماً عند النقر خارجه · المساعد **فقط إن كان خاملاً** (‏`saiPanelHasContent()` = لا سؤال غير مُرسل ولا إجابة ظاهرة) — 🚫 لا يمسح عمل المستخدم. و`Esc` يغلق ويعيد التركيز بلا مسح.
- **الوصولية:** المنسدلة `role="listbox"` وخياراتها `role="option"` (بمعرّفات `sqOpt{i}`) · تنقّل أسهم/Enter/Esc (`sqKey`) ＋ `aria-activedescendant` · الزرّ `aria-expanded`/`aria-controls`. أمثلة قابلة للنقر (`AI_EXAMPLES`/`saiFill`) تملأ الحقل فقط (🚫 لا تمسّ توجيه النوايا). سطر المنسدلة: **الاسم · الكود · «الإجمالي (يشمل المستودع): N»** (مخزون لا مبيعات فالمستودع مشمول، والوسم يمنع سوء القراءة) — `sqTotalQty` مجموع `salesCtx.stock`. 🚫 لا صورة/شارة (‏`sales_stock` بلا عمود صور). الجوال (≤640px): `.s4-tools { flex-direction: column }` تتراصّان بعرض كامل بلا تمرير أفقيّ.
- **الحارس `tests/perf/compact-tools.mjs` (G-COMPACT-TOOLS، بأسنان ثلاثيّة، 1200＋360):** الترتيب فلاتر→أدوات→مؤشّرات · مُدمج افتراضياً · الشريط 50-60px · اللمس ≥32px (يفتح اللوحة قبل القياس) · **آخر نتيجة في قائمة ٤٠ مرئية فوق المؤشّرات** (`elementFromPoint` بعد تمرير المنسدلة — إثبات لا اقتطاع ولا حجب) · بلا تمرير أفقيّ · اللوحة العائمة لا تُزيح المؤشّرات (إحداثيّ الوثيقة المطلق لا `top` النسبيّ، فلا يُخلط تمرير `focus()` بإزاحة). **الأسنان:** `--broken` (اللوحتان ظاهرتان افتراضياً = التخطيط القديم) · `--broken-clip` (سلف `overflow:hidden` يقتطع المنسدلة) · `--broken-collapsed` (لا يفتح اللوحة ⇒ أبعاد صفرية ⇒ يُثبت أنّ الحارس يفتح فعلاً). ＋ `G-AI-CAP`/`G-AI-DISCLAIMER` حُدِّثا ليفتحا اللوحة قبل القياس (لوحة مطويّة تُخفي التسريب/التصريح فيمرّ الحارس كاذباً).

## بنود معلّقة
- **ترحيل `kind='withdrawal'` لحركات المستودع (جولة قادمة):** حالياً السحب مخزَّن `estimated_sale` والمعنى يُشتقّ عند العرض بـ`location==='wh'`. الترحيل يضيف قيمة `withdrawal` (＋تعديل CHECK في `supabase_sales_movements.sql`) ويرحّل الحركات القديمة، فيصير المعنى في البيانات لا العرض — حتى لا يقرأ استعلامٌ مباشر السحب بيعاً. **لا يُنفَّذ إلا بقرار صريح** (يمسّ القيد والبيانات).
- **تفصيل المصادر في بطاقة المطابقة (تمّ):** `batchWhInner` يستدعي `srcLineCompact(c.code)` — سطر مختصر تحت الكمية: «المصادر: العزيزية 6 · الخضرة 11» (الأسماء دائماً `.mc-src-list`)، ＋ **الأسعار المختلفة بالأرقام** «⚠ أسعار مختلفة: 250 / 280» (بلا أسماء فروع — يقصر السطر؛ >3 ⇒ «⚠ N أسعار مختلفة»). يظهر عند **مصدرين فأكثر بكمية**؛ مصدر واحد ⇒ لا سطر؛ غائب ⇒ «غائب عن كل المصادر». **نفس `sourceBreakdown` — صفر تكرار.** عرضٌ محض (G-THROUGH أخضر). حارس `tests/perf/card-sources.mjs` (G-CARDSRC، بأسنان): يفحص **القيمة** (الأسماء والأرقام والأسعار)، لا وجود العنصر؛ `--broken` يستبدل السطر بـ«مصادر متعددة» بلا أرقام ⇒ يرسب.
- **شريط «نسخة جديدة» (تمّ):** `checkAppVersion` (في الإقلاع، fire-and-forget) يجلب `version.txt?t=Date.now()` (`cache:no-store`) ويقارنه بـ`APP_VERSION` المخبوز؛ اختلاف ⇒ شريط `#versionBar` «لديك X · المتاح Y — حدّث (Cmd+Shift+R)» ＋ زرّ إغلاق. **الإغلاق للجلسة فقط** (`vbDismissed` متغيّر ذاكرة، لا تخزين دائم — لو نسي التحديث يعود الشريط في الفتحة التالية). **فشل الجلب صامت** (`.catch`). `version.txt` مُلتزَم = **نفس قيمة `APP_VERSION`**، وحارس `tests/version-sync.mjs` (ساكن) يرسب على أي انحراف بينهما (بدل كاتب آليّ من CI — أبسط وأوثق؛ أُحدّثهما معاً في كل commit).
- ~~تجاهل صفوف المجاميع~~ **(تمّ):** `parseInvRows` يتخطّى صفّاً `INV_TOTALS_NAMES.has(normHead(name))` — **مطابقة تامّة** (`المجموع`/`الاجمالي`/`الاجمالى`/`total`)، لا `includes` (الواقع أكّده: «مجموعة سكاكين المحترف» و«مجموعة سكاكين 5قطع» صنفان حقيقيان في العزيزية لحُذفا بـincludes). ＋ `list.totalsSkipped` ⇒ تنبيه `.totals-note` «تُجوهل صفوف مجاميع: N» في سطر الاستيراد (`onMergeWh`/`onMergeBranch`). **التنظيف:** إعادة الرفع ⇒ الصفّ يصير «غائباً عن الملف الجديد» فيحذفه `removeCodes` المحصور (المستودع 1168 · الخضرة 3330 — العزيزية بمعزل). حارس `tests/totals-filter.mjs` (G-TOTALS، بأسنان): يفحص القيمة — صفوف المجاميع تُستبعَد و«مجموعة سكاكين» تبقى؛ `--broken` يحوّلها `includes` فيحذف الصنف الحقيقيّ ⇒ يرسب.
- **صمّام ②-ب «updated=0 ＋ تصفير غيابيّ» (تمّ):** حادثة 852 (كل الملف أصفار · تم تحديثه 0) نسبتها 36% < 60% فمرّت من `zeroGateOk`. المعيار الحاسم في `absentZeroGateOk`: يوقف حين `updatedCount==0 ∧ absentZeros≥20` (‏`absentZeros` = `zeroDoor==="absent"` حصراً — لا ⏳ ولا رخص، فبضعة قرارات لا تُحسب). **يفصل الثلاث حادّاً:** اليوم 954≠0 يمرّ · حادثة 852 (0∧852) يوقف · 0/0/0 (0∧0) يمرّ — لا نطاق ضيّق. الحوار يذكر السبب بالأرقام «تم تحديثه 0 … سيُصفَّر 852 صنفاً غائباً … راجع أعمدة ملفك» و«إلغاء» افتراضيّ (صمّام لا يقول لماذا يُتجاوَز بلا تفكير). مربوط بـ`dlQty` قبل `zeroGateOk` (الأخيرة تبقى خلفيّةً للكوارث). عرضٌ محض. حارس `tests/valve-absent.mjs` (G-VALVE، بأسنان): الحالات الثلاث ＋ الحدّ 20 ＋ بضعة قرارات؛ `--broken` يُسقط شرط `updatedCount==0` ⇒ اليوم يوقف ⇒ يرسب.
- ~~شريط «نسخة جديدة متاحة»~~ **(تمّ — راجع «شريط نسخة جديدة» أعلاه).** الدرس المُحرِّك الباقي: «التخبئة تجعل التشخيص يقيس شيئاً غير ما يراه المستخدم؛ و`APP_VERSION`=هاش الكوميت **السابق** فالنجاح يُرى كهاش أقدم».

## التطوير
- كل الكود في `index.html`. التحقق السريع: فتح الصفحة headless والتأكد من غياب أخطاء JS، ولقطات شاشة للتحقق البصري.
- git add ← commit ← push إلى `origin/main` (يُنشر عبر GitHub Pages).
